# 버전 관리

현재 버전: **v3.10.0**

`VERSION.md`와 `package.json`의 `version`은 항상 같은 값을 쓴다.

## 번호 규칙

`주.부.패치` 형식이며, 변경에 필요한 **가장 낮은 자리만** 올린다.

- **패치(0.0.x)** — 버그 수정, 밸런스 조정, 연출 손질, 리팩터링, 문서, 에셋 교체
- **부(0.x.0)** — 새 시스템·화면·콘텐츠 묶음. 예: 새 보스, 거점 신규 칸, 개발 환경 개편
- **주(x.0.0)** — 저장 데이터나 핵심 계약의 비호환 변경, 또는 출시 단계 변경.
  에나 저장 키 스키마 변경, `ENA_FEATURE_COUNT` / `ENA_ACTION_SPACE` 차원 변경이
  여기 해당한다(구버전 정책이 거부되고 재학습이 필요하다).

여러 성격이 섞이면 가장 큰 변경을 기준으로 고른다.

## 기록 규칙

- 이력은 **이 문서에만** 쌓는다. `CLAUDE.md`에 완료 보고를 추가하지 않는다.
- 항목은 **무엇을 왜** 바꿨는지 쓴다. 무엇만 적으면 6개월 뒤 되돌릴 수 없다.
- **되돌린 변경은 되돌린 이유를 반드시 남긴다.** 같은 시도를 두 번 하지 않기 위한 기록이다.
- 커밋 제목은 Conventional Commits(`feat(scope): 요약`)를 유지한다. 부 이상 버전을
  올리는 커밋은 `package.json`과 이 문서를 함께 갱신한다.

## 변경 이력

### v3.10.0 — 2026-08-14

**첫 조우 설명은 이제 '넘겨야 지나간다' — 설명끼리 덮어써서 둘 다 못 읽던 문제 수정.**

- 문제: 캐모마일 부활 설명이 뜨는 도중 트리플 설명이 덮어써 **둘 다 못 읽었다**.
  원인은 연출이 아니라 **등급**이었다 — 첫 조우 설명이 일반 상황 바크와 같은 눈금
  (`situation`)이라, 큐가 최소 노출(1.5~3초)만 지나면 읽었든 말든 다음으로 넘어갔다.
- `BARK_IMPORTANCE`에 **`teach`** 눈금을 새로 뒀다(잡담·상황보다 위, 위급·클러치보다 아래).
  위급 경고는 여전히 설명을 뚫고 들어온다 — 위험 알림이 설명에 막히면 안 된다.
- `BarkSequencer`에 **홀드**를 넣었다. 설명은 시간이 아니라 **플레이어의 다음 행동**으로
  넘어간다(`releaseHeldBark()` ← `handleCardAction`). 그동안 도착한 대사는 교체하지 못하고
  전부 큐에서 기다리므로, 설명 → (넘김) → 다음 설명이 순서대로 하나씩 읽힌다.
- 두 안전판을 함께 뒀다. **바닥 `TEACH_HOLD_FLOOR_MS`(1.2초)** — 설명을 띄운 바로 그
  행동의 입력이 곧장 넘겨 버리면 한 글자도 못 읽는다. **상한 `TEACH_HOLD_MAX_MS`(12초)** —
  판을 들여다보며 가만히 있는 사람에게 영원히 붙어 있으면 안내가 아니라 모달이다.
- 넘긴 순간에는 남은 최소 노출도 채운 것으로 보고 다음 대사를 곧바로 잇는다 —
  넘겼는데 또 뜸을 들이면 "넘겼다"는 조작감이 사라진다.
- 큐가 넘칠 때 설명은 잡담보다 늦게 버려진다(중요도 낮은 것부터 드롭하는 기존 규칙에 자동 반영).
- 승격된 설명: 필드 첫 조우(거미줄·폭탄·포자·꽃 등) · 트리플 합성 · 콤보 게이지 만충 ·
  레시피 첫 발동 · 첫 보스. 검사 6종을 `BarkSequencer.test.ts`에 세웠다.

### v3.9.3 — 2026-08-14

**레바테인을 대상 최대체력 비례 → 플레이어 공격력 비례로 전환.**

- 최대체력 비례를 버렸다. 체력이 두꺼운 보스에게만 폭발적으로 세져(30F 보스 기준 한 방
  68) 곡선이 대상마다 갈렸고, 무엇보다 `damageProfile` 양식 밖이라 **보스 화력 모델·에나
  판단·학습 시뮬 어디에도 잡히지 않는 사각지대**였다.
- 공격력 비례로 옮기고 상수는 낮췄다: 단일 `3.0공+6`, 트리플 `6.0공+12`. 흐름 비용
  (단일 1턴 / 트리플 없음)이 있는 카드라 참격(2.0공+2, 비용 없음)보다 확실히 위에 둔다.
- 이제 `damageProfile`이 붙어 세 소비처에 모두 잡힌다. 다만 실제 피해 적용은 여전히
  index.ts다(적 행동 시뮬을 먼저 돌려야 한다) — 정합 테스트에 **위임 목록**
  (`DELEGATED_DAMAGE`)을 새로 두어 "프로필은 있어야 하지만 표적 체력으로는 재지 않는다"를
  분명히 했다. 프로필이 없으면 그 목록 자체가 거짓말이 되므로 그것도 검사한다.

### v3.9.2 — 2026-08-14

**구역 커튼 불투명도 0.7로 · 제목은 국소 암막으로 선명하게 유지 + 레바테인 버프.**

- 커튼 상단 불투명도를 0.88 → 0.70으로 더 낮췄다. 대신 커튼 전체를 어둡게 하는 대신
  **글자가 앉는 자리에만** 흐린 타원 암막(`.zone-curtain-inner::before`)을 깔았다 —
  보드는 계속 비치면서 제목만 또렷하게 남는다. 글자 그림자도 검정 한 겹을 더해 대비를 올렸다.
- **레바테인 버프.** 이 카드의 대가는 '적이 먼저 움직인다'인데 단일 2턴은 그 대가가 너무
  커서 잘 쓰이지 않았다. 단일은 1턴으로 낮추고 **트리플은 아예 대가 없이(0턴)** 터지게 해
  세 장을 모을 이유를 만들었다. 피해도 함께 올렸다:
  단일 `0.30♥+10 → 0.35♥+12`, 트리플 `0.45♥+15 → 0.60♥+20`.
- 주의: 레바테인은 `damageProfile`이 없는(대상 최대체력 기반) 카드라 **보스 화력 모델에
  잡히지 않는다**(`balance:boss`의 '미모델' 항목). 실제 보스전은 리포트 수치보다 그만큼 세다.

### v3.9.1 — 2026-08-14

**열쇠로 연 보물칸의 손패 토큰이 엉뚱한 자리에서 나오던 것 + 급소 추가 피해에 체력 롤링이 빠지던 것 수정.**

- 손패를 늘리는 효과의 토큰 출발점이 `{ kind: 'chain' }`(화면 상단 중앙 체인 자리)로 굳어
  있었다. **손패는 얻은 칸 자리에서 나와야 한다** — 열쇠·열쇠공이 연 보물칸이 그 자리다.
  제거된 필드 카드 중 보물칸의 rect를 소멸 연출 **전에** 잡아 그 자리에서 쏜다.
  판에 근거가 없는 생성형 손패(의식 양초·칼집)는 잡을 칸이 없으므로 체인 자리를 유지한다.
- 급소(에나 클러치)는 대형 황금 수치만 띄우고 **적 체력은 굴리지 않았다** — "-N은 떴는데
  체력은 그대로"로 읽혔다. 일반 타격과 같은 창구(`rollEnemyHealthNumber`)를 태워 굴리는
  모양까지 맞췄다. 수치 하나에는 롤링 하나가 붙는다는 규칙을 급소만 비켜 가고 있었다.
  (레바테인은 이미 `tickCardHealthDown`으로 굴리고 있었다 — 급소만 빠져 있던 것이다.)

### v3.9.0 — 2026-08-14

**구역 전환 커튼을 어둡고 반투명하게 + 종이 결 질감 + 체류 2초 연장.**

- 색을 더 어둡게(먹빛 `4,2,8`), 불투명도는 더 낮게 잡았다(0.97 → 0.88). 판을 완전히 덮으면
  '가림막'이지만 아래 보드가 비쳐야 '내려온 천'으로 읽힌다. `backdrop-filter: blur(2.5px)`가
  그 반투명을 거든다.
- 낡은 종이 결을 얹었다 — SVG 프랙탈 노이즈를 데이터 URI로 깔아 **전용 에셋 없이** 미세
  그레인을 만든다(자기완결·네트워크 없음). 커튼과 **같은 세로 그라데이션으로 마스크**해
  하단에서 결도 함께 사라진다. 안 하면 커튼은 흐려지는데 결만 사각형으로 남아 경계가 드러난다.
- 제목 체류를 1.4초 → 3.4초로 늘렸다(`ZONE_CURTAIN_HOLD_MS`). 구역 이름은 한 런에 네 번뿐인
  드문 알림이라 충분히 읽힐 시간이 필요하다.

### v3.8.4 — 2026-08-14

**밀랍상 발동 배너에서 ✧ 마크 제거.**

- 이름과 효과문만 남긴다. 배너가 이미 밀랍상 발동임을 자리(탭 위)와 색으로 말하고 있어
  마크가 뜻을 더하지 않았다.
- 마크가 사라졌으므로 이로치 색 덮어쓰기에서도 `.chain-event-mark` 규칙을 함께 지웠다 —
  아무것도 안 잡는 CSS 규칙을 남겨 두지 않는다.

### v3.8.3 — 2026-08-14

**밀랍상 발동 배너가 체인 항목 마크업을 그대로 재사용 + 임시보관함 무효과를 테스트로 못 박음.**

- 배너 생김새를 직접 그리던 것을 걷고 **체인 배너의 유물 항목 마크업을 그대로 쓴다**
  (`.chain-event.chain-event-relic` + `.chain-event-mark`(✧) + `.chain-event-copy` 안의
  이름/효과문). 전용 양식을 새로 그리면 같은 종류의 사건이 화면마다 다른 틀로 보인다 —
  이제 이 배너가 정하는 것은 **어디에 어떻게 떠 있다 사라지는가**(부유 + 암막)뿐이고,
  글꼴·발광·간격은 체인 규칙을 따라온다. 밀랍상 종 아이콘은 뺐다.
- 이로치만 색을 덮어쓴다(옥빛). 체인의 금빛 규칙 위에 색만 갈아 끼우는 형태다.
- **임시보관함 밀랍상은 효과를 발동하지 않는다** — 코드는 이미 그렇게 돼 있었다
  (`waxFigureEffectStar`가 영구 밀랍상함만 읽고, 임시보관함은 메모리 전용 `runHold`다).
  다만 그걸 지켜 주는 검사가 없어 조용히 깨질 수 있는 자리라 테스트 3종을 세웠다:
  한도 초과로 흘러간 것은 성급 0 · 옮겨 넣으면 그때부터 붙음 · 런 종료로 임시보관함이
  비워져도 영구 보유분은 그대로.
- 곁가지: CSS 주석에 백틱을 넣어 템플릿 리터럴을 깨뜨렸다가 고쳤다(같은 실수 두 번째 —
  스타일 문자열 안 주석에는 백틱을 쓰지 않는다).

### v3.8.2 — 2026-08-14

**밀랍상 발동 배너를 클러치 배너(에나의 자아) 양식으로 통일하고 위로 뺐다.**

- v3.8.1의 카드형(테두리 두른 판)을 버리고 **클러치 배너와 같은 양식**을 쓴다: 흐린 원형
  암막 위에 아이콘 → `『 제목 』` → 그 아래 한 단 작은 효과문. 판을 두르면 UI 조각처럼
  읽혀 "지금 무언가 발동했다"가 살지 않는다. 게임 안에서 같은 종류의 사건(동료/수집물이
  대신 움직였다)은 같은 어휘로 말한다.
- 클러치보다 한 단 작다(제목 20px vs 29px). 매 발동마다 뜰 수 있는 잦은 사건이라 같은
  크기면 소음이 된다.
- 등장 박자도 클러치와 같게 맞추되 짧게 줄였다(솟아오름 → 체류 → 흐려지며 상승, 2.9초).
- 위치를 탭 아이콘에서 **86px 위**로 뺐다. 12px일 때는 효과문이 아이콘 판 상단에 걸치고
  암막이 아이콘을 덮었다.

### v3.8.1 — 2026-08-14

**이로치 표식을 아이콘으로 · 색조를 눅임 · 밀랍상 발동 표기를 체인 배너 양식으로.**

- `이로치` 글자를 **반짝이 아이콘 하나**로 바꿨다(전시관 썸네일·정보창·조합 목록 공용).
  이름 옆에 붙는 짧은 표식이라 글자로 두면 종 이름이 밀리고, 색이 이미 이로치를 말하고
  있어 글자는 중복이다. 뜻은 `data-tooltip`으로 남긴다.
- 색조를 전반적으로 눅였다(필드 판 0.9→0.55·채도 1.5→1.25, 전시관 0.72→0.5). 옥빛이
  밀랍 위에 얹힌 것으로 읽혀야지 네온으로 튀면 안 된다.
- **밀랍상 효과 발동 표기를 한 줄 텍스트에서 체인 배너 양식의 카드로 바꿨다**: 아이콘 +
  굵은 제목(어느 밀랍상이 움직였는가) + 그 아래 작은 효과문. 한 줄로 흘리면 무엇이
  발동했는지가 배경에 묻히고, 하단 아이콘 줄을 가로질러 글자가 안 읽혔다 — 아이콘
  **위쪽**으로 띄워 겹침도 없앴다.
- 발동에 소리를 붙였다(변환/변신 음색). 밀랍상이 깨어나 대신 움직인 사건이라 그 어휘를
  쓰고, 이로치는 4반음 올려 같은 사건의 다른 개체임을 귀로도 가른다.

### v3.8.0 — 2026-08-14

**이로치는 합체되지 않는 한 마리 · 스탯 두 배 + 색조를 화면 전체에서 통일.**

- 이로치는 **항상 1칸으로 남는다**(`Card.canMergeWith`). 합쳐지면 색과 스탯 두 배가
  여럿분과 뭉개져 무엇이 특별한 개체였는지 화면에서 사라지고, 합체 스탯 배수까지 겹쳐
  난이도가 튄다.
- 대신 스탯을 두 배로 얹는다(`WAX_FIGURE_SHINY_STAT_SCALE`). 시련·유물 보정까지 포함한
  **최종 수치**에 곱하므로 층이 올라가도 같은 비율로 세진다. 합체가 막혀 있어 합체 배수와
  곱해지지 않으니 난이도가 튀지도 않는다.
- 이로치 표기를 **색조 갈아끼우기**로 바꿨다. 기존 `saturate(1.15) hue-rotate(-14deg)`는
  사실상 원본과 같아 보였고(키틴 일러스트가 크림/황토라 -14도로는 안 옮겨간다), 발광으로
  말하고 있어 "빛난다"였지 "다른 개체"가 아니었다. `mix-blend-mode: color` 오버레이는
  명암·질감을 그대로 두고 색조만 옥빛으로 바꾼다 — 적마다 원본 색이 달라도 **같은 옥빛**으로
  통일돼 '이로치'라는 규칙이 읽힌다(hue-rotate는 적마다 다른 색으로 튄다).
- 필드 카드 · 전시관 썸네일 · 전시관 큰 초상 셋이 같은 규칙을 탄다. 다만 전시관은 판이
  필드보다 밝아 같은 값이면 네온으로 튄다 — 밝기 보정을 빼고 색조를 눅여 맞췄다.
- 필드 이로치 CSS가 `.card-content`에 `position: relative`를 걸어 **수치가 이름 밑으로
  딸려 올라가던 버그**를 고쳤다(`.card-content`는 `absolute` + `space-between`으로 이름을
  위, 수치를 아래에 붙인다 — position을 건드리면 그 배치가 통째로 무너진다).

### v3.7.2 — 2026-08-14

**되돌림: v3.7.0에서 추가한 이벤트 3종 제거(사용자 판단). 구조 정리는 남긴다.**

- `event_004 밀랍 우체부` · `event_005 굳어 가는 악사` · `event_006 제 몫의 심지`를
  뺐다. 이벤트는 다시 3종이다. **되돌린 이유는 콘텐츠 판단**이지 구현 문제가 아니다 —
  같은 시도를 다시 하기 전에 이벤트를 늘릴지부터 정한다.
- 함께 들어온 **효과 종류 통합(`resource`)과 시뮬 choice형 일반화는 남겼다.** 둘 다 새
  이벤트가 아니라 기존 코드의 결함을 고친 것이라, 이벤트를 빼도 값이 그대로다:
  `stat`/`randomHand`로 쪼개져 있으면 축을 걸치는 선택지에서 문구와 실제 효과가 어긋나고,
  시뮬은 event_001만 하드코딩으로 겪어 새 choice형 이벤트를 영영 학습하지 못한다.
- 쓰는 이벤트가 없어진 `relic` 효과 종류와 그 처리부(런타임·시뮬)는 지웠다 — 아무도
  안 부르는 분기를 남겨 두지 않는다.
- `Events.test.ts`는 남긴다. 남은 3종에도 그대로 적용되는 계약이다.

### v3.7.1 — 2026-08-14

**이로치를 '빛나는 개체'에서 '색이 다른 개체'로 + 첫 실행도 구역 커튼을 탄다.**

- 이로치 표현이 `saturate(1.15) hue-rotate(-14deg)` + 발광이라 원본과 거의 구분되지 않았다.
  이름 옆 반짝이 아이콘(`.card-shiny-mark`)을 달고, 색조를 옥빛 판
  (`mix-blend-mode: color`)으로 통째로 갈아끼운다.
- **hue-rotate를 쓰지 않는 이유**: 결과가 원본 색에 좌우돼 키틴은 청록, 거미는 보라 하는
  식으로 적마다 다른 색이 나온다 — '이로치'가 규칙이 아니라 우연으로 읽힌다. color 블렌드는
  명암을 원본에서 그대로 가져오므로 **어떤 적이든 같은 옥빛 개체**가 되고 밀랍 질감도 남는다.
- 색조 판은 의사요소가 아니라 실제 엘리먼트(`.card-shiny-tint`)다 — `.card-face::before/::after`는
  굳음 연출이 이미 쓰고 있어 **굳은 이로치**에서 둘이 서로를 덮는다. 블렌드는
  `isolation: isolate`로 카드 안에 가둔다(밖으로 새면 레일 배경까지 물든다).
- 색조 전에 `brightness(1.22)`를 넣는다. color 블렌드는 명도를 건드리지 않으므로 어두운 밀랍
  일러스트에 그냥 얹으면 옥빛이 아니라 탁한 이끼색으로 가라앉는다.
- **첫 실행도 정규 런과 같은 순서로 연다.** 기본 무대 배경에서 시작해 레일이 생기고 칸이
  다 내려온 **뒤에** 구역 커튼이 내려오며 새싹 온실로 전환된다. 그동안은 인트로가 처음부터
  구역 배경을 깔아 두고 커튼을 건너뛰어서, "어디에 도착했다"는 비트가 통째로 없었다.
  브라우저에서 확인: 0~5.5s 기본 배경 → 5.5s 레일 → 6.4s 칸 → 7.3s 커튼 '새싹 온실' + 배경 전환.

### v3.7.0 — 2026-08-14

**이벤트 3종 추가(3 → 6종) + 선택지 효과 종류 통합 + 시뮬 choice형 일반화.**

- 이벤트가 3종뿐이라 100층 런에서 문이 평균 3~4번 열리는 동안 거의 확정 반복이었다.
  choice형 3종을 더해 6종으로 늘렸다. 셋 다 세계관 팔레트 안에서 축을 하나씩 맡는다:
  `event_004 밀랍 우체부`(지식에의 탐욕) · `event_005 굳어 가는 악사`(머무는 값) ·
  `event_006 제 몫의 심지`(자기연소).
- **선택지 효과 종류를 하나로 합쳤다.** `stat`(영구 스탯) / `randomHand`(손패)로 쪼개져
  있어서 "방패 +4 · 공격력 +1"처럼 축을 걸치는 선택을 적을 수 없었다 — 실제로 새 이벤트를
  쓰다가 **문구는 두 가지를 약속하는데 효과는 하나만 도는** 상태를 만들었다. 수치로 끝나는
  효과 전부를 `resource` 하나로 모으고(영구 스탯·자원·랜덤 손패), 유물 지급은 `relic`으로
  분리했다. 기존 event_001도 같은 종류로 옮겼다.
- **학습 시뮬의 choice형 처리를 일반화했다.** 전에는 `playCandleDollEvent`가 event_001의
  선택지를 `effect.kind`로 찾아 하드코딩돼 있어, 새 choice형 이벤트를 넣어도 에나는
  event_001만 겪는 세계에 머물렀다. 이제 `playChoiceEvent`가 **선택지 데이터에서 직접**
  대가와 이득을 읽어 고르고 적용한다 — 손패·유물 테이블과 같은 규칙(데이터만 늘리면 학습에
  자동 반영)이다. 위험 추정(`estimateEventRisk`)도 같은 방식으로 데이터에서 읽는다.
- `src/data/Events.test.ts` 신설. 이벤트는 코드가 아니라 표로 느는데, 잘못 적어도 타입 검사를
  통과하고 화면에서만 이상해진다 — 아무것도 안 바꾸는 `resource` 선택지, 선택지가 하나뿐인
  choice형, illu 키가 id와 어긋난 정의, 표에 안 물린 이벤트를 잡는다.
- **일러스트는 아직 없다**(`event_004~006.webp` 미제작). 지금은 어두운 방사형 폴백으로
  뜬다 — 파일을 넣으면 자동 연동된다.

### v3.6.2 — 2026-08-14

**만료로 사라진 함정이 '처리'로 잡히던 것 수정 + 상자를 여는 소리 복원.**

- 덤불이 수명 2턴을 다해 저절로 사라져도 함정 처리음이 났다. 원인은 신호 층이다 —
  `onCardRemoved`가 함정 카드 제거를 전부 `onTrapResolved`로 넘겨, 만료 제거까지
  '플레이어가 함정을 처리했다'로 읽혔다. 만료 구간(`fieldExpiryInProgress`)에서는
  **신호 자체를 내지 않는다**. 소리만 막으면 '함정 수집' 유물이 가만히 기다리기만 해도
  보상하는 문제가 남는다 — 기다려서 사라진 것은 처리가 아니다.
- 상자·잡동사니를 열어도 상자 소리가 안 들리던 것: 사실 나고 있었지만 그게 **자원
  트레일 착탄의 잔짤랑임**(볼륨 0.34)이라 상자를 열었다는 사건으로 안 읽혔다.
  **여는 순간**에 또렷한 한 방을 넣고, 뒤따르는 불빛 잔짤랑임은 throttle 창을
  260 → 700ms로 늘려 묶었다(트레일 비행 ~330ms보다 넉넉해야 같은 소리가 0.5초 간격으로
  두 번 난 것처럼 들리지 않는다). 화폐는 드문 값이라 묶지 않고 따로 울리게 뒀다 —
  "열림(0반음) → 돈(+5반음)"으로 이어져 읽힌다.
- 브라우저에서 확인했다. 가드를 뺀 빌드는 덤불이 만료된 턴마다 **+3반음**(덤불 함정 음색)이
  찍히고, 가드를 넣으면 만료 2개가 겹친 턴에도 그 음색이 사라진다. 보물 클릭은 이전
  2회(30ms 여는 소리 + 474ms 잔짤랑임) → 1회로 줄었다.

### v3.6.1 — 2026-08-14

**난이도 카드 일러스트(level_001~003) 매핑 + 새싹 병아리 전용 구역 배경.**

- 난이도 카드 아트를 `SpriteUrls.difficultyLevels`로 모으고 `level_001~003`을 새싹 병아리 /
  쉬움 / 보통에 **순서대로** 물렸다. 배열 순서가 곧 매핑이라 `HEARTH_DIFFICULTIES` 순서와
  어긋나면 카드 그림이 바뀐다 — 두 배열의 순서가 계약이다. 그동안 새싹만 `player_000`을
  빌려 쓰고 쉬움·보통은 빈 문자열이라 CSS 플레이스홀더로 비어 있었다.
- 새싹 병아리 런의 구역 배경을 `background_010`(새싹 온실)로 갈랐다(`SPROUT_ZONE`).
  온보딩은 30층 한 구역뿐이라 목록이 아니라 상수 하나로 두고, 정규 런의 1구역
  (오래된 저택)과 배경·이름을 나눠 첫 모험이 다른 곳임을 화면이 말하게 했다.
  구역 커튼·첫 실행 인트로 배경·부팅 프리로드 세 곳이 같은 상수를 지난다.

### v3.6.0 — 2026-08-14

**에나 기본 성향 재피팅 CLI(`npm run ena:fit`) 신설 + 재피팅 실행 결과: 현재 스냅샷 유지.**

- `CLAUDE.md`는 "보스 수치를 바꾸면 `SIM_FITTED`를 다시 피팅한다"고 적고 있었지만
  `EnaDispositionFitter`를 돌릴 수단이 없어 그 규칙은 **실행될 수 없는 규칙**이었다.
  `src/tools/ena-fit-report.ts`가 그 수단이다. 값을 파일에 직접 쓰지 않는다 — 비교표와
  붙여 넣을 블록만 찍고 채택은 사람이 정한다(자동으로 덮어쓰면 나쁜 후보가 조용히 배송된다).
- 평가 시드(90000~)는 피팅 시드(3000~)와 **겹치지 않는다**. 같은 시드로 맞추고 같은
  시드로 재면 그 시드에만 맞춘 값이 늘 이겨 비교가 의미를 잃는다.
- 출발점 기본값은 손-튜닝 기본 성향이 아니라 **현재 동봉 스냅샷**이다. ① 재피팅의 질문이
  "지금 배송 중인 값보다 나은가"라서 그 값이 출발점이어야 하고 ② 기본 성향은 아래 표기
  대역 밖이라 거기서 출발하면 후보가 전부 탈락해 탐색이 헛돈다. `--cold-start`로 옛 동작.
- **피터에 '배송 가능성' 제약을 넣었다.** 시뮬은 생존만 본다 — 생존에 좋다는 이유로 각성을
  상한까지 밀면 불굴 축만 튀어 경험 탭 육각형이 한 축짜리로 보인다. 대역표
  `EXPERIENCE_CALIBRATION`(`ExperienceAxes.ts`)을 피터(`isShippable`)와 테스트가 **함께
  읽고**, 대역 밖 후보는 점수와 무관하게 −∞로 탈락시킨다. 대역을 두 곳에 적으면 한쪽만
  고쳐져 제약이 조용히 풀린다.
- **첫 실행 결과 — 채택하지 않았다.** 제약 없이 돌리면 후보가 held-out 300시드에서 +1.50
  앞서지만 그 우세는 표기 대역을 부수고서야 얻어지는 것이었다. 제약을 걸면 탐색 시드
  1·7·13 전부 −0.86·−0.88·−0.74로 **현재 스냅샷을 못 이긴다** — 지금 값이 대역 안에서
  도달 가능한 최선 근처다. 다만 오래된 블로커(`predictBaseChance`가 하한으로 밀린다)는
  **풀린 것을 확인했다**: 이제 후보가 예지를 0.738까지 올린다. 상세는 `EnaDisposition.ts` 주석.
- 곁가지: `blendSimFitted`/`fromSimFittedSnapshot`/`toSimFittedSnapshot`/`buildRookieFrom`을
  뽑아 스냅샷↔성향 변환을 단일 출처로 모았다. CLI가 배송되는 변환과 **같은 함수**로
  후보를 평가해야 채택 판단이 실제 배송값과 어긋나지 않는다.

### v3.5.4 — 2026-08-14

**`damageProfile` 정합 계약 테스트 신설 — 그리고 그 테스트가 잡아낸 세 가지 수정.**

- `src/systems/DamageProfile.contract.test.ts`. 문서 대조가 아니라 **실행 대조**다.
  실제 `GameState`에 체력 9999 표적을 세우고 진짜 `HandSystem.useSingle()`을 돌려
  깎인 체력을 재서 테이블의 근사와 맞춘다 — 검사용 사본을 만들면 그 사본이 먼저 썩는다.
  표적을 하나만 두면 광역·무작위·분배 카드도 전부 그 하나에 꽂히므로 카드 종류마다
  다른 측정 코드를 쓰지 않아도 된다.
- 검사 넷: ① 공격 카드에 프로필이 빠지지 않는다(면제는 이유와 함께 선언) ② 면제 목록에
  죽은 항목이 남지 않는다 ③ 확정 피해는 실제와 **정확히 일치**한다 ④ 무작위 피해는
  실측 기댓값을 **넘지 않는다**(과대평가하면 에나가 못 잡을 적을 잡는다고 판단한다).
- **불화살 근사가 상한으로 적혀 있었다** — 실제 분포는 단일 1~(공+3) · 트리플 5~(3공)
  균등인데 프로필은 각각 공+3 / 3공, 즉 최댓값이었다. 공격력 12에서 근사 36 대 실측
  평균 18.8로 **두 배 가까이 부풀어** 있었다. 기댓값 아래로 다시 잡았다(0.5공+1 / 1.5공+1).
- **참격 트리플(즉사)에 기계가 읽을 표기가 없었다.** 숫자는 의도적으로 단일식을 남긴
  보수적 하한이라 그대로 두고, "실제로는 그 이상"이라는 사실만 `tripleExecutes` 플래그로
  실었다. 표기와 실제 즉사 동작이 어긋나면 테스트가 잡는다.
- **손거울에 프로필이 없던 것은 정상**이었다 — 피해원이 대상 적의 공격력이라 플레이어
  공격력의 함수가 아니다. 방패 밀치기·레바테인과 함께 이유를 적어 면제 목록에 넣었다.
  이제 이유 없이 빠진 공격 카드는 테스트가 막는다.

### v3.5.3 — 2026-08-14

**바위 스프라이트의 낡은 `TODO(art)` 주석 정리.**

- `Sprites.ts`의 바위 항목에 "전용 일러스트로 교체, 현재는 임시 폴백"이라는 주석이
  남아 있었지만 실제로는 `enemy_019`가 물려 있고 `spriteForCard`가 1/2/3칸 전용
  아트(`rockGroups`)까지 분기하고 있다. 주석만 옛 상태에 머물러 있어 코드를 읽는
  쪽이 미구현으로 오해했다 — 실제 구조(폭별 아트는 별도 표)를 가리키도록 고쳤다.

### v3.5.2 — 2026-08-14

**함정 처리음 타이밍 교정 + 불빛 획득음 비중 축소.**

- 함정 처리음이 한 박자 늦게 들리던 원인은 재생 지점이었다. 소리는 모델에서 카드가
  빠지는 `onTrapResolved`에 걸려 있었는데, 그 제거는 소멸 연출(`animateCardConsume`)이
  **전부 끝난 뒤**에 일어난다. 직접 클릭 경로는 연출이 시작되는 박자에 미리 울리도록
  옮기고, `suppressTrapClearSfx` 가드로 뒤이은 `onTrapResolved`의 중복 재생을 막았다.
  연출 훅이 없는 경로(손패 광역 제거·보스 칸 함정)는 기존대로 그 창구가 맡는다.
- 불빛 획득음이 잦아 거슬린다는 지적에 따라 불빛만 볼륨 0.34·잔향 0.08로 낮추고
  `throttleMs: 260`으로 연속 재생을 묶었다. 화폐는 드물게 들어오는 값이라 또렷한
  기존 음색(볼륨 0.9·잔향 0.45)을 유지해 둘이 구분되게 남겼다.

### v3.5.1 — 2026-08-13

**잿빛 굴레(별빛 턴 진행)에서 굳음 해동 연출이 빠지던 것 수정.**

- 90~100F 잿빛 굴레는 별빛 수집만 턴을 올린다. 그 경로도 `gameState.nextTurn()`을
  지나므로 밀랍 굳음은 **정상적으로 1턴씩 줄고 있었다** — 다만 일반 턴 경로에 있는
  해동 스냅샷/연출(`snapshotFieldFreezeState` → `animateWaxThawByIds`)이 빠져 있어
  굳은 카드가 소리 없이 풀렸다. 같은 스냅샷과 연출을 태워 두 경로의 어휘를 맞췄다.

### v3.5.0 — 2026-08-13

**상자/함정/변환 사운드 연동 + 열쇠 손패 드롭 연출 + 반격 처치 전리품.**

- **새 사운드 3종을 라이브러리에 연동**했다(`chest_001/002` · `trap_001/002` · `transform_001`).
  체인음과 같은 방식으로 **피크 대비 dB 다듬기 + 끝 60ms 가드 + 페이드**를 그대로 타고,
  같은 사건에 여러 장이 있으면 `SFX_VARIANTS`에서 **무작위로 골라** 반복 청각 피로를 던다.
- **상자 소리는 불빛·화폐가 들어오는 그 박자에 울린다**: `playResourceTrail`(트레일 착탄 =
  수치 롤링 = 버스트)이라는 단일 창구에 걸어, 상자·잡동사니·꽃·이벤트 어디서 온 재화든
  같은 beat에 소리가 난다. 화폐는 불빛보다 5반음 높게 울려 귀한 값임을 가른다.
  상점·제단 구매도 같은 음색을 한 단계 높여 쓴다(`playShopPurchaseImpact`).
- **함정 처리음은 종류로 갈린다**: 거미줄 -2 · 폭탄 -6(길게) · 포자 +5 · 덤불 +3.
  `GameState.onTrapResolved`가 함정 종류를 함께 넘기도록 계약을 넓혀, 필드 카드와 보스 칸
  함정이 같은 단일 창구를 계속 공유한다.
- **변환음**은 미믹이 정체를 드러낼 때(-4반음)와 꽃이 시들어 괴물꽃이 될 때(+2반음)에 건다.
- **★ 열쇠로 연 보물칸의 손패에 드롭 연출이 붙는다**: `열쇠`는 `character.addHandCard`로
  직접 넣어 `itemGainedNames`를 타지 않아, 손패가 소리 없이 늘고 있었다. `useSingle`이
  효과 전후 손패 수를 재 `gainedHandCards`로 보고하고 index가 카드 토큰을 띄운다 —
  열쇠뿐 아니라 손패를 늘리는 모든 효과가 함께 고쳐진다.
- **★ 에나의 반격으로 잡아도 전리품이 나온다**: 반격 처치는 `onEnemiesDefeated`만 부르고
  전리품·밀랍상 굴림을 아예 하지 않아 아무것도 안 떨어졌다. `awardHandKillDrops`를 태워
  손패 처치와 같은 보수 굴림/행동당 1장 상한으로 지급한다(턴을 더 쓰지 않고 얻는 처치라
  직접 타격의 0~N을 그대로 주지는 않는다).
- 체인 배너를 조금 위로 올렸다(`top: 20vh` → `15vh`).

### v3.4.0 — 2026-08-13

**광역 한 방의 칸 파괴 상한 + 준비 화면을 셔터 레이어로.**

- **★ 광역 한 방이 격자를 통째로 쓸지 못한다**: 단두대(필드 전체 확정 피해)는 칸마다
  한 번씩 꽂혀서, 상한이 없으면 첫 타격에 아홉 칸을 전부 부수고 부위 파괴 보너스까지
  아홉 번 챙겨 보스가 그 자리에서 죽었다. 이제 한 스윕이 깨는 칸은
  `BOSS_GIMMICK_MAX_BREAKS_PER_SWEEP`(2)까지고, 성한 칸은
  `BOSS_GIMMICK_MIN_CELLS_AFTER_SWEEP`(3) 아래로 내려가지 않는다 — 단단한 칸이 버텨
  광역기의 값어치(칸마다 들어가는 배율 피해)는 남되 한 방에 판이 끝나지는 않는다.
- 상한을 **행동 단위가 아니라 `strikeAllCells`(광역의 유일한 입구)에** 둔 이유:
  행동 단위로 두면 `beginAction` 선언을 빠뜨린 경로에서 칸이 영영 안 깨지는 잠금이 된다
  (실제로 그렇게 만들었다가 테스트가 무한 루프로 잡아냈다). 단일/무작위 타격은 어차피
  한 번에 한 칸이라 상한이 필요 없고, 그래서 '부위를 하나 더 깨야 열리는' 페이지
  리미트도 막히지 않는다.
- `balance:boss` 읽는 법에 이 상한을 적었다 — 표의 부위 파괴 보너스는 전투 전체에
  상각한 값이라 단발 광역의 순간 피해는 표보다 낮다.
- **준비 화면(해금 온오프 · 시작부터 해금)이 셔터 레이어 안에서 뜬다**: 전면 오버레이
  대신 난이도 선택과 같은 자리(`.hearth-prep`)를 돌려 쓴다. 잘림 방지를 위해 바깥 틀은
  셔터 폭·높이에 가두고(`min(96%, 780px)` · `max-height`), 목록만 스크롤시키며
  (`min-height: 0`이 없으면 flex 자식이 안 줄어 버튼이 잘린다) 카드를 압축했다.

### v3.3.0 — 2026-08-13

**테마별 카드 타격음 + 팩 강조를 유리 반짝임으로.**

- **카드 타격음이 테마로 갈린다**: 전용 파일을 종류마다 만들지 않고 하나의 타격음을
  음정·잔향·반복·**역재생**으로 변주한다(`HIT_TONE_BY_TAG`). 칼날은 높게 두 번 그어지고,
  불씨는 파형을 뒤집어 부풀다 터지며, 제물은 낮고 길게 울린다. 방패는 둔탁하게 끊고
  성물/정화는 높고 맑게 남는다. 한 카드에 태그가 여럿이면 `HIT_TONE_PRIORITY` 서열로
  정체성이 강한 쪽이 울린다(제물 > 칼날 > 불씨 …).
- 역재생은 **다듬은 구간만 뒤집은** 사본을 쓴다 — 원본을 통째로 뒤집으면 앞쪽 무음이
  뒤로 가 소리가 늦게 시작한다. 사본은 키마다 한 번만 만들어 캐시한다.
- **★ 팩 강조를 유리 반짝임으로 바꿨다**: 팩 아트는 네 귀퉁이가 투명한 PNG라
  (실측 알파 0 · 투명 화소 7~8%) `box-shadow` 발광이 그림 없는 사각 경계를 빛내 어색했다.
  이제 ① 알파 실루엣을 따라가는 `drop-shadow` 발광과 ② 팩 모양으로 오려 낸
  (`mask-image: var(--pack-art)`) 대각선 빛 띠가 훑고 지나가는 두 가지로 낸다.

### v3.2.0 — 2026-08-13

**준비 화면을 로비로 · 난이도 뒤로가기 수정.**

- **해금 온오프 · 시작부터 해금 드래프트가 로비에서 돈다**: 두 화면 모두
  `startGame()` 안(=레일이 이미 깔린 뒤)에서 떠서, 모험 중에 짐을 싸는 그림이었다.
  거점 출발 버튼이 `onPrepareLoadout` 핸들러로 먼저 치르고(배경을 걷기 전),
  거기서 고른 '시작부터 해금' 카드는 런 시작 시 풀에 들어간다.
- **난이도 화면에서 뒤로가면 로비로 나간다**: `raiseShutter()`가 `is-character-confirmed`를
  보고 캐릭터 선택 캐러셀로 되돌리고 있었다 — 그 선택은 의도적으로 꺼져 있어
  (`installFixedCharacter`) 뒤로가기가 숨겨 둔 화면을 되살리는 꼴이었다. 그 분기를 걷고
  셔터를 그대로 올린다(캐릭터 선택을 다시 켜는 날 되살릴 자리는 주석으로 남겼다).

### v3.1.0 — 2026-08-13

**단두대 메타 잠금 · 칸 내구도 흩어짐/고조 · 브래킷 힌트 타이밍.**

- **단두대를 난이도가 아니라 메타로 가른다**: `ONBOARDING_BANNED_CARDS`에서 빼고
  `BASIC_UNLOCK_PACK_CARDS`에 넣었다 — 이제 거점 무역에서 해금해야 비로소 해금팩
  후보가 된다(해금 전에는 어느 난이도에서도 만날 수 없다).
- **보스 극딜은 격파로 이어진다**: 마지막 페이지에는 게이트가 없어 하한이 0이므로
  체력이 0까지 깎여 그대로 죽는다. 즉사가 아닌 이유는 페이지 리미트가 경계마다
  한 번씩 걸어 세우기 때문이다(v2.18.0).
- **★ 칸이 깨질수록 남은 칸이 단단해진다**: 칸마다 단단함을 흩어 굴리고
  (`BOSS_GIMMICK_TOUGHNESS_MIN/MAX`), 하나 깨질 때마다 남은 칸 내구도가
  `BOSS_GIMMICK_DURABILITY_ESCALATION`만큼 오른다. 광역기에 쓸려도 단단한 칸이
  살아남아 때릴 자리가 남는다.
- **한 행동은 마지막 성한 칸을 깨지 못한다**: 광역 한 방이 격자를 통째로 비워
  더 때릴 곳이 없어지는 일을 막는다. 다음 행동에서 단독으로 때리면 깨지므로
  '부위를 하나 더 깨야 열리는' 페이지 리미트가 막히지도 않는다.
- 흩어짐/고조는 **평균을 건드리지 않는다**: 매니저가 `bossGimmickCellSpreadFactor()`로
  나눠 실효 내구도를 내므로 "칸의 절반을 깨면 보스가 쓰러진다"는 관계식과 학습 시뮬
  요약(`bossGimmickBreakBonusFactor`)이 그대로 성립한다. 정합 테스트가 절반 구간
  평균으로 이를 검사한다.
- **레시피 브래킷 힌트가 손패 정렬 뒤에 뜬다**: 방금 받은 지원 카드가 아직 내려오는
  중에 좌표를 재서 힌트가 어긋나 있었다. 대상 칸의 유한 애니메이션이 끝나기를 기다렸다가
  그린다(상한 1.2초).

### v3.0.0 — 2026-08-13

**★ 주 버전: 에나 관측 계약 373 → 380 (재학습 필요).** 밀랍상·손패 낭비를 관측에 실었다.
구버전 저장 정책은 `EnaPolicyStore`가 거부하고 교사 정책으로 폴백한다.

- **★ 밀랍상이 에나의 세계에 들어왔다.** 밀랍상 상시 효과(거미줄/폭탄 피해 무시 ·
  포자 피해가 회복 · 직접 타격 +1)는 런을 넘는 성장인데 조언에도 학습에도 없었다 —
  에나는 이미 막아 내는 함정을 계속 최우선으로 치우라고 조르고 있었다.
  - 확률의 단일 출처로 `waxMitigationChances()`를 열고(굴리지 않고 확률만 읽는다),
    `HandCardAdvisor.waxMitigation`이 거미줄/포자 기대 피해를 그만큼 깎는다.
  - 학습 시뮬도 같은 규칙으로 실제로 굴린다(`takeTrapDamage`) — 미지정이면 에피소드마다
    0~35%에서 흩어 굴려 "있을 때/없을 때"를 조건부로 배우게 한다(`EnaSimWaxEffects`).
- **손패 한도 초과로 타 버린 드랍**을 세어 관측에 실었다(한도·남은 자리·낭비율).
  기존에는 가득 차면 조용히 사라지기만 해서 "지금 한 장 더 받는 것이 의미가 있는가"를
  판단할 근거가 없었다.
- 관측 스칼라 72 → 79 (밀랍상 4 + 손패 한도/여유/낭비율 3). `npm run ena:pretrain`으로
  380차원 정책과 `Ena_Pretraining_Report.md`를 같은 고정 시드에서 재생성했다 —
  전체 관측 정책이 괴물꽃 우선순위 50.0%(구 관측 한계 26.7%)로 앞선다.
- 이미 관측에 있던 것(보스 약점 칸 배율·격자 칸 수·페이지 게이트·부가물 칸)과 공유
  데이터로 자동 반영되는 것(적 밸런스 — 시뮬이 `ENEMY_DEFINITIONS`를 직접 import)은
  그대로 두었다. **미반영으로 남긴 것**: 보스 수치 변경(v2.18.0) 이후의 에나 기본 성향
  `SIM_FITTED` 재피팅 — 피팅 CLI가 없어 이번 커밋에서는 돌리지 않았다.

### v2.19.0 — 2026-08-13

**체인음 도입 + 효과음 라이브러리 정리.**

- **효과음 라이브러리 분리**: 목록(`src/audio/SfxLibrary.ts`)·다듬기 판정
  (`src/audio/SfxTrim.ts`)·재생(`SfxManager`)을 갈랐다. 호출부는 URL이 아니라 키를
  넘기고, 새 효과음은 라이브러리 표 한 줄이면 붙는다.
- **★ 재생 구간 다듬기**: 다섯 개 mp3 전부 **파일 맨 끝 10ms에 인코더 패딩 잡음**
  (피크 대비 -23~-45dB)이 박혀 있었고, 뒤쪽 무음도 270~660ms씩 남아 있었다. 디코딩
  직후 창(10ms) 단위 최대 진폭을 **피크 대비 dB**로 재서 실제 소리 구간만 재생하도록
  했다(원본 파일은 그대로 둔다). 맨 끝 60ms는 후보에서 빼 패딩 잡음이 소리의 끝으로
  잡히지 않게 하고, 자른 끝에는 30ms 페이드를 걸어 단차 '툭'을 없앴다.
- **체인음**: `chain_001`=손패 사용 · `chain_002`=레시피 · `chain_003`=유물.
  체인이 쌓일수록 **한 단계마다 1.5반음씩** 올라가고(상한 14반음) 잔향(되먹임 딜레이)과
  볼륨이 함께 자라, 이어 갈수록 소리가 고조된다. 기록과 소리를 `pushChainEvent()`
  단일 창구로 묶어 체인 배너에 줄이 붙는 순간이 곧 소리가 나는 순간이 되게 했다.
- 콤보 게이지 만충은 체인의 결산이라 유물 음색을 **한 옥타브 위**에서 잔향과 함께 울리고,
  에나의 개입(클러치)은 같은 음색을 **5반음 낮게** 길게 울려 무게를 준다.

### v2.18.0 — 2026-08-13

**보스 리미트 강화 + 단두대 새싹 잠금 + 툴팁 통합 + 상점 팩 미리보기/추천.**

- ★ **`cell-break` 리미트가 무조건 한 번 걸린다.** 예전에는 부위를 깬 행동이 리미트를
  함께 뚫고 하한이 1까지 풀려서, 단두대(필드 전체 확정 피해)가 첫 타격에 격자를 전부
  부수며 HP를 0으로 만들고 그 하한이 다시 1로 되돌리는 그림이 나왔다. 이제
  `bossPageFloor()`는 게이트 하한만 돌려주고, 기준선(`halfPageBrokenMark`)은 도달
  타격의 파괴분까지 포함해 잡는다 — 페이지는 그 뒤 부위를 하나 더 깨야 열리고,
  열릴 때도 HP는 경계에서 멈춘다. 학습 시뮬(`EnaTrainingSimulation`)도 같은 규칙으로
  함께 고쳤다(CLAUDE.md ★ 규칙).
- 이 변경으로 쓰이지 않게 된 `BossGimmickManager.brokeCellThisAction`과 그 전용
  테스트를 제거했다.
- **단두대 새싹 잠금**: `ONBOARDING_BANNED_CARDS`에 `guillotine`을 넣었다. 필드 전체
  확정 피해라 30F 격자를 한 번에 쓸어 새싹 구간 밸런스를 부순다 — 졸업 후 정규 런에서만
  나온다(해금팩 후보에서도 함께 빠진다).
- **툴팁 통합**: 브라우저 기본 `title=`로 남아 있던 11곳(체인 초기화 ✕·체인 이벤트 3종·
  악마 다이아·게이지 모드 2곳·유물 미니카드·불씨 경계선 2곳·불씨 카운트다운)을 전부
  `data-tooltip`으로 옮겨 게임 공용 툴팁(`src/ui/Tooltip.ts`)을 타게 했다. 손패 슬롯의
  `title`은 같은 정보를 이미 `hand-recipe-preview`가 더 자세히 보여 주므로 지웠다.
- **상점 팩 호버 미리보기**: 팩 타일에 커서를 올리면 예시 카드 3장이 봉투 뒤에서
  부채꼴로 솟는다(`shop-pack-fan`). 애니메이션이 아니라 **트랜지션**으로 내서, 접히는
  도중에 다시 올리면 남은 자리에서 자연스럽게 되살아난다. 미리보기 표본은 실제 뽑기와
  다른 경로(`packPreviewArts`)라 결과를 스포일러하지 않는다.
- **에나 팩 추천(초회 1회)**: 상점에서 판을 보고 팩 하나를 짚어 준다 — 체력/불씨가
  모자라면 자원팩, 조합을 굴려 본 런이면 조합팩, 넉넉하면 해금팩. 종류마다 태어나서
  한 번씩만 나가고(`encounterIntroLineOnce`), 짚은 팩은 촛불빛 발광으로 강조한다
  (`emphasizeShopPack`).

### v2.17.4 — 2026-08-13

**밀랍상 전시관 3종 수정 — 조합 폰트 · 클릭 시 전체 새로고침 · 남는 툴팁.**

- **조합 폰트**: `GameBoardWaxFigureStyles.ts`에 `font-family` 선언이 하나도 없어,
  폼 요소는 폰트를 상속하지 않는 브라우저 기본 규칙 때문에 조합 리스트·마법진 코어·
  담기 버튼의 글자만 UA 산세리프로 튀었다. `.wax-figure-overlay button { font-family:
  inherit; }` 한 줄로 모달 전체 타이포와 맞췄다.
- **칸 클릭 시 전체 새로고침**: 선택만 바뀌는 클릭에도 `render()`가 모달을 통째로
  `innerHTML` 교체해 마법진 회전·코어 맥동이 0프레임으로 되감기고 화면이 번쩍였다.
  소장품이 안 바뀌는 경로는 `updateSelection()`/`updateComposeSelection()`으로 갈라
  선택 표시와 해당 패널만 제자리에서 고친다(담기/버리기/합성처럼 데이터가 실제로
  바뀌는 경로는 그대로 전체 렌더).
- **닫아도 남는 툴팁**: 마법진 코어에 커서를 둔 채 창을 닫으면 `mouseout`이 오지 않아
  '눌러서 합성.' 문구가 화면에 남았다. `Tooltip.ts`가 앵커를 들고 있다가 DOM에서
  빠지면 스스로 걷도록 하고(재렌더로 교체되는 경우 전반에 적용), DOM에 남은 채
  클래스만 떼고 닫는 경우를 위해 `hideGlobalTooltip()`을 열어 닫기 경로가 부르게 했다.

### v2.17.3 — 2026-08-13

**첫 트리플 소개 대사가 한두 턴 늦게 뜨던 문제 수정.**

- `maybeIntroduceFields()`가 필드 대상(바위/덤불/거미줄 등)이 화면에 하나도 없으면
  트리플 확인까지 건너뛰고 조기 반환했다 — 에나 지원(`tryCompanionPrediction`)이 3장째를
  채워 조용히 트리플을 완성해도, 그 턴에 필드 대상이 안 보이면 다음 기회까지 소개가
  밀렸다.
- 게다가 `tryCompanionPrediction()` 자체가 `maybeIntroduceFields()` 스캔보다 **뒤에** 돌아,
  지원이 만든 트리플은 그 지원을 준 바로 그 턴에도 못 잡히고 최소 한 턴은 늦을 수밖에
  없는 구조였다.
- 트리플 확인을 `maybeIntroduceFormedTriple()`로 분리해 필드 대상 유무와 무관하게 항상
  돌게 하고, `tryCompanionPrediction()` 직후에도 한 번 더 호출해 지원이 트리플을 완성한
  바로 그 beat에서 소개하도록 고쳤다.

### v2.17.2 — 2026-08-13

**플레이어존 유틸리티 버튼 순서 변경.**

- 캐릭터 카드에 가장 가까운 자리(오른쪽 끝)부터 도감 → 경험 → 밀랍상 순으로
  재배치했다. 기존에는 경험 → 도감 → 밀랍상이었다. `GameBoardRenderer.renderPlayerZone()`
  마크업 순서만 바꿨다(레이아웃은 `justify-content: flex-end`라 DOM 순서 = 시각 순서).

### v2.17.1 — 2026-08-13

**밀랍 회고록 탭 아이콘 제거.**

- `밀랍 회고록` 탭 앞에 붙었던 아이콘을 뺐다. 옆 `모험일지` 탭이 아이콘 없이 텍스트만
  쓰는데 회고록 탭만 아이콘을 달아 톤이 어긋났다 — 이제 두 탭 모두 텍스트만 남긴다.
  `HearthScene.ts`에서 미사용이 된 `waxFigureIcon` import와, `HearthStyles.ts`의
  `.hearth-library-tab-icon`/`.hearth-library-tab-icon .icon` 죽은 CSS도 함께 지웠다.

### v2.17.0 — 2026-08-12

**새싹 병아리 튜토리얼 드랍 확정 안전판 + 밀랍 회고록(서고 복구 탭) 신설.**

- **튜토리얼 드랍 안전판**: 11층부터 스폰 풀이 넓어지며 양초 키틴벌레 비중이 옅어져
  실제로 못 잡고 클리어할 수 있었다. `runOnboardingClear()`(30F 클리어 시점)에서
  `onboardingWaxFigureTutorialDropDone`이 아직 안 켜져 있으면 그 자리에서 한 번 더
  확정 지급한다 — 처치 시점 확정 드랍은 그대로 두고, 그걸 놓쳐도 첫 판 클리어에서는
  반드시 받게 하는 이중 안전판.
- **밀랍 회고록 신설**: 버려지거나(밀랍상함 ✕) 정리 못 하고 사라진(임시보관함) 밀랍상이
  이제 완전히 소멸하지 않는다. `WaxFigureCollection.ts`에 회고록 저장소
  (`unmelting.waxfigures.archive.v1`, 최대 200개, 최신순)를 추가하고
  `discardWaxFigurePermanent`/`discardWaxFigureCatch`/`resetWaxFigureRunHold` 세
  경로 모두 삭제 직전에 기록을 남기도록 배선했다. 밀랍상함에서 직접 버린 항목은
  레이어 1(성급 유지), 임시보관함에서 사라진 항목은 레이어 2(항상 ★1)로 나뉜다.
- 서고에 `밀랍 회고록` 탭을 새로 달았다(`HearthScene`) — 상단 분류가 처음으로
  2개(모험일지/밀랍 회고록)가 됐다. 레이어별로 나눠 최신순 카드 그리드로 보여주고,
  카드마다 `복구` 버튼으로 밀랍상함에 자리가 있으면 되돌린다(없으면 안내만 뜨고
  회고록에 그대로 남는다 — 임시보관함처럼 어중간하게 걸치지 않는다).
- 종 이름 → 일러스트 해석 로직을 `WaxFigureView.ts` 내부 중복 함수에서
  `spriteForEnemyName()`(`src/ui/Sprites.ts`)로 뽑아 단일 출처로 만들고, 서고
  회고록도 같은 함수를 쓴다.

### v2.16.2 — 2026-08-12

**조합 바 하단 공백 정리 — 마법진·리스트를 실제로 키워서 채움.**

- 구역 높이를 `min-height`(내용만큼만 차는 값)에서 `height: 260px` 확정값으로
  바꿔 이 구역이 항상 그 높이를 실제로 쓰게 했다.
- 마법진을 150px → 220px로 키우고, 재료 노드(32→46px)·가운데 결과(지름
  62→92px)·둘레 링 안쪽 간격도 같은 비율로 키웠다. 결과 라벨/성급 배지 폰트도
  12~13px로 올렸다.
- 좌측 합성 가능 리스트 버튼도 함께 키웠다(아이콘 26→40px, 패딩 6px 8px →
  10px 14px, 버튼 사이 간격 6→10px) — 마법진만 커지고 리스트만 작은 채로 남으면
  다시 불균형해 보인다.
- 조합 바 전체 여백(패딩 14px 18px → 20px 26px, 요소 간 gap 10→14px)도 키워
  전시관 규모에 맞춰 "초라해 보이지 않게" 했다.

### v2.16.1 — 2026-08-12

**임시보관함을 작은 인벤 칸으로, 일괄 정리 버튼 제거, 좌측 정보창에서 함께 다룸.**

- 임시보관함 항목을 행 목록(아이콘+이름+담기/버리기 버튼)에서 **갤러리 카드와 같은
  어휘의 더 작은 칸**(`.wax-hold-card`, 64px)으로 바꿨다 — 이름/효과 문구 없이
  아트만 보여준다. 클릭하면 좌측 정보창에서 상세를 본다.
- "전부 정리" 일괄 버튼을 없앴다. 정리(담기)/버리기는 이제 좌측 정보창에서
  선택한 항목 하나씩만 한다(밀랍상함 항목의 버리기와 같은 자리·같은 어휘).
- 좌측 정보창(`renderInfoPanel`)이 밀랍상함 항목과 임시보관함 항목을 함께 받는
  판별 유니온으로 바뀌었다. 임시보관함 항목을 고르면 "정리하지 않으면 사라짐"
  안내 + 담기/버리기 버튼이 뜬다 — 결국 같은 밀랍상이고 표시 칸만 작을 뿐이라는
  게 이번 정리의 요지다.

### v2.16.0 — 2026-08-12

**조합을 "좌측 리스트 + 큰 마법진"으로 재구성하고, 눌러서 합성하는 애니메이션을 붙임.**

- 조합 바를 좌(합성 가능 리스트) / 우(마법진 무대) 2단으로 나눴다. 좌측은 재료가
  이미 다 찬 조합만 버튼으로 나열한다(`renderComposeList`) — 예전처럼 갤러리에서
  아무 항목이나 골라야 "부족합니다" 문구를 보는 방식이 아니라, 지금 당장 합성할 수
  있는 것만 고르는 목록이다.
- 마법진(`.wax-compose-circle`)을 150px로 크게 키우고, 가운데 결과가 곧 **합성
  버튼**이다(`<button data-wax-merge>`). 숨쉬듯 맥동하는 발광(`is-pulse`)을 얹어
  누르고 싶게 만들었다 — 재료 슬롯 채움 표시나 "합성하기" 텍스트 버튼은 뺐다
  (좌측 리스트가 이미 "재료 다 찼음"을 보장하므로 필요 없어졌다).
- 클릭 시퀀스: ① 링이 0.6초간 빠르게 가속하며 밝아지는 "위잉" 연출
  (`.is-merging`) → ② 실제 합성(`mergeWaxFigures`) → ③ 재렌더로 재료 수량이
  갤러리에서 즉시 줄어들거나 사라짐 → ④ 결과가 마법진 자리에서 튀어나와 갤러리의
  새 칸으로 날아가 꽂히는 토큰 연출(`playComposeFlight`, 착지 시 사각 블라스트).
  연출이 도는 동안 같은 클릭이 겹치지 않게 막는다(`merging` 플래그).

### v2.15.1 — 2026-08-12

**밀랍상 남은 칸을 투명 칸으로, 상단 표기를 보관/최대로 분리.**

- 갤러리 그리드가 이제 `capacity - used`개의 투명 점선 칸을 실제 전시물 카드 뒤에
  덧붙인다(`.wax-exhibit-card-empty`) — 클릭 반응 없이 "몇 자리가 남았는지"만
  칸 개수로 보여준다. 기존에는 완전히 비었을 때만 안내 문구 한 줄이 떴다.
- 상단 표기를 `used/capacity`(예: `6/6`)에서 `보관 <b>N</b> / 최대 <b>M</b>`으로
  바꿔 숫자만 보고도 어느 쪽이 현재고 어느 쪽이 최대인지 헷갈리지 않게 했다.

### v2.15.0 — 2026-08-12

**조합을 "밀랍 조각진" 도형으로 재표현 + 밀랍상함 항목도 직접 버리기.**

- 하단 조합 바의 슬롯 3개 → 화살표 → 결과 나열을 걷어내고, 재료 3개가 원 둘레
  정삼각형 위치에 놓이고 그 힘이 가운데 결과로 모이는 **원형 조각진**으로 바꿨다
  (`.wax-compose-circle` — 이중 점선 링이 서로 반대 방향으로 천천히 돈다,
  `prefers-reduced-motion`에서는 정지). 슬롯 나열보다 이 시스템의 봉인/제물 테마에
  맞는 표현이라는 판단.
- `WaxFigureCollection.discardWaxFigurePermanent(enemyName, variant, star)` 신설 —
  밀랍상함에 이미 담긴 항목도 1개씩 버릴 수 있다(마지막 하나면 항목 자체가 사라짐).
  좌측 정보창 하단에 작은 ✕ 버튼으로 노출(`WaxFigureView`). 기존에는 넘친 몫(임시
  보관함)만 버릴 수 있었다.
- 정보창 통계 폰트(★성급 표기·보유/효과/확률 행)가 10.5~11.5px로 최소 12px 원칙을
  어기고 있던 걸 이 김에 12px로 맞췄다.

### v2.14.2 — 2026-08-12

**밀랍상 전시관 레이아웃 재조정: 갤러리는 다시 작게, 조합은 하단 바로, 우측은 임시보관칸으로.**

- 갤러리 카드를 전시물답게 키웠던 것을 되돌려 다시 작게 잡았다(최소 폭 210px → 128px,
  간격 16px → 10px) — "더 많은 요소를 담을 수 있게"가 우선이라 카드 자체의 크기보다
  한 화면에 몇 마리가 보이는지가 더 중요하다는 판단.
- 조합 패널을 우측 세로 칸에서 **하단 가로 바**로 옮겼다(재료 슬롯 → 화살표 → 결과 →
  합성 버튼이 한 줄로 나열).
- 우측 칸은 조합이 빠진 자리에 **임시보관함**(한도 초과로 밀려난 넘친 봉인)을 배치했다
  — 기존에는 갤러리 위 가로 스트립이었던 것을 그리드 우측 세로 칸으로 옮기고 슬롯도
  더 작게 잡았다(정리/버리기 버튼만 남기고 효과 설명은 뺐다 — 넘친 몫은 잠깐 머무는
  자리라 상세 정보까지 필요 없다).
- `.wax-figure-hall`을 `grid-template-areas`(`info gallery hold` / `compose compose
  compose`)로 재구성. 좌측 정보창 폭은 유지.
- 갤러리 카드 이름/성급/보유수 폰트가 12px 미만이던 걸(11~11.5px) 이 김에 12px로
  맞췄다(CLAUDE.md 최소 12px 원칙).

### v2.14.1 — 2026-08-12

**밀랍상 전시관을 세로로도 키움.**

- 모달을 `max-height`만 있던 상태(내용만큼만 차는 짧은 박스)에서
  `height: min(840px, 90vh)`로 바꿔 실제 세로 공간을 확보했다.
- 갤러리 카드도 함께 키웠다(최소 폭 168px → 210px, 간격 12px → 16px) — 늘어난
  세로 공간에 작은 타일만 촘촘히 채우면 오히려 전시관보다 표처럼 보인다.
- 좌/우 패널 폭도 커진 모달 비율에 맞춰 소폭 확장(220/240px → 250/270px).

### v2.14.0 — 2026-08-12

**밀랍상 탭을 "전시관" 레이아웃으로 재설계.**

- 작은 아이콘 타일 그리드를 걷어내고 좌(정보창) · 중(갤러리) · 우(조합) 3단 구성으로
  바꿨다. 모달 폭도 720px → 1180px로 넓혀 전시관다운 규모를 냈다.
- 갤러리 카드는 전용 아이콘이 아니라 **필드 카드와 같은 함수**(`spriteForCard()`)로
  실제 인게임 일러스트를 풀 사이즈로 띄운다(`WaxFigureView.spriteForSpeciesName()`이
  종 이름 → `ENEMY_DEFINITIONS` → 더미 `Card` → `spriteForCard()` 경로로 해석). 카드
  비율도 필드 칸과 같은 4:3.2를 쓴다 — 전시관 전용 아트를 새로 만들지 않았다.
  카드를 클릭하면 선택되고(`data-wax-select`), 좌/우 패널이 그 항목을 따라간다.
- 좌측 정보창: 선택된 전시물의 큰 초상 + 성급 + 보유 수 + 효과 + 발동 확률.
- 우측 조합 패널: 재료 슬롯 3칸(채워진 만큼 실제 아트로 표시) → 화살표 → 합성 결과
  미리보기(다음 성급 아트 + 확률). 합성 성공 시 선택이 결과 항목으로 자동 이동한다.
- "넘친 봉인"(임시보관함) 카드는 갤러리 위 가로 스트립으로 분리하고 담기/버리기
  버튼을 유지했다 — 전시관 본편(영구 소장)과 넘친 몫(정리 안 하면 사라짐)을 시각적으로
  가른다.
- 상태는 액션(선택/합성/정리/버리기) 사이에도 유지된다 — 밀랍상 탭 렌더 상태의 단일
  출처(`@core/WaxFigureCollection`)는 그대로고, 선택 키만 뷰가 자체 소유한다.

### v2.13.0 — 2026-08-12

**밀랍상 미룬 항목 마저 배선: 튜토리얼 확정 드랍 + 봉인 비행 연출 + 효과 발동 배너 + 효과 4종 실전 배선.**

- **30F 새싹 병아리 튜토리얼 확정 드랍**: 이번 런에서 처음 잡은 양초 키틴벌레는 확률
  없이 확정 봉인된다(`WAX_FIGURE_TUTORIAL_SPECIES`, `onboardingWaxFigureTutorialDropDone`
  — `resetForNewRun()`에서 매 런 초기화). 종 목록에 양초 키틴벌레를 추가했다
  (정상 = 폭탄 피해 무시, 이로치 = 직접 타격 +1).
- **봉인 비행 연출**: 처치한 시체 자리에서 별이 튀어나와 표류하다 "칭!" 밀랍상으로
  변신해 탭 아이콘으로 빨려 들어간다(`ResourceTrailFx.animateWaxFigureCaptureToken` →
  `GameBoardRenderer.fireWaxFigureCapture`). `SquareBurst`에 `wax-figure`(금빛)/
  `wax-figure-shiny`(옥빛) 테마를 추가했다. rect는 카드가 DOM에서 지워지기 전에
  미리 캡처한다(직접 클릭 경로는 행동 시작 시점, 손패 처치 경로는 처치 직후).
- **효과 발동 체인 배너**: 보유한 밀랍상 효과가 실제로 발동하면 탭 아이콘에서 작게
  카드가 뿅 튀어나와 2초짜리 배너로 알리고 사그라든다
  (`GameBoardRenderer.showWaxFigureEffectChain`) — 클러치 배너보다 작고 짧다(잦은
  사건이라 그만큼 조용해야 소음이 안 된다).
- **효과 4종을 실제 게임 로직에 배선**: `WaxFigureCollection.rollWaxFigureEffect(effectId)`
  단일 판정 창구(보유 성급 조회 → `waxFigureEffectChance()` 굴림)를 만들고
  `web-trap-ignore`/`spore-heal`은 `ActionSystem.evadeTrap`에,
  `bomb-trap-ignore`(자기 피해만 무시, 스플래시는 그대로)는
  `TurnManager.applyBombExplosions`에, `direct-hit-bonus`(+1)는
  `ActionSystem.attackEnemy`에 배선했다. 이전까지는 데이터/라벨만 있고 실제 판정에
  안 걸리는 상태였다.
- 디버그 명령 `/밀랍상 <종> 이로치`로 변종을 강제 봉인할 수 있게 했다(정상은 기존과
  동일하게 `/밀랍상 <종>`).
- `Unmelting_Game_Concept.md` §12-6을 이번 배선 내용으로 갱신.

### v2.12.0 — 2026-08-12

**밀랍상 보관 모델 정정(임시보관함 → 넘침 전용) + 필드 이로치 사전 표시.**

- **보관 모델 정정**: v2.11.1에서 "봉인은 전부 임시보관함에 먼저 쌓이고 플레이어가
  직접 담아야 영구 저장된다"로 잘못 구현했던 것을 "**자리가 있으면 곧장 영구
  밀랍상함에 들어간다. 한도를 넘겼을 때만** 임시보관함(넘친 몫)으로 흘러 들어간다"로
  바로잡았다(`captureWaxFigure()`가 `totalWaxFigureCount() < waxFigureCapacity()`를
  먼저 확인). 평소에는 밀랍상 탭에서 정리할 게 없고, 밀랍상함이 가득 찬 순간에만
  넘친 몫이 생긴다. `WaxFigureView.ts`의 "이번 모험" 섹션은 "넘친 봉인"으로 문구를
  바꾸고 비어 있으면 아예 숨긴다. `WaxFigureCollection.test.ts`도 이 모델에 맞춰
  다시 썼다.
- **필드 이로치 사전 표시**: 이로치 여부를 처치 시점이 아니라 **스폰 시점**에 미리
  굴린다(`CardSpawner.generateEnemy()` → `Card.waxFigureShiny`). 걸린 개체는 필드에서부터
  옥빛으로 발광해(`.is-wax-figure-shiny`, `GameBoardWaxFigureStyles.ts`) "어 떴다!!!"를
  처치 전에 보여준다. 그렇게 표시된 개체를 처치하면 `captureWaxFigure()`에
  `forceVariant: 'shiny'`를 넘겨 확률 게이트 없이 확정 포획한다
  (`tryCaptureWaxFigureOnKill()`이 이제 적 이름 대신 `Card`를 받는다).
- **아직 없는 것**: 30F 새싹 병아리 클리어 과정의 확정 튜토리얼 드랍(양초 키틴벌레),
  봉인 순간 연출(별 반짝임 → 비행 → 밀랍상 변신), 소유 효과 발동 체인 배너, 종 콘텐츠
  확장(현재 양초 거미 1종) — v2.11.1에서 미룬 항목 그대로 남아 있다.

### v2.11.1 — 2026-08-11 (실험적)

**밀랍상 탭 UI 추가 + 임시보관함/정리 모델로 전환 + 확률·게이트 재조정.**

- **런 중 무제한 임시보관함**: 봉인이 더는 영구 밀랍상함 한도를 직접 체크하지 않는다.
  처치마다 메모리 전용 임시보관함(저장 안 함)에 한도 없이 쌓이고, 플레이어가 밀랍상
  탭에서 "담기"를 눌러야 영구 밀랍상함(한도 있음)으로 옮겨진다. 정리하지 않고 런이
  끝나면(새 런 시작 시 `resetWaxFigureRunHold()`) 임시보관함은 통째로 사라진다 — 한도
  초과로 방금 잡은 이로치가 그 자리에서 증발하는 상황을 막기 위한 설계.
- **개방 게이트 제거**: 잿빛 굴레와 마일스톤을 공유하던 게이트를 없앴다. 새싹 병아리부터
  처음부터 열려 있다.
- **확률 재조정**: 포획 확률 3%→1%(`WAX_FIGURE_CAPTURE_CHANCE`), 이로치 확률 2%→0.01%
  (`WAX_FIGURE_SHINY_CHANCE`)로 낮췄다. 대신 **이로치는 포획 확률 게이트보다 먼저
  굴리고, 걸리면 확정 포획**된다 — 워낙 희박한 확률이라 그 순간을 일반 포획 굴림이
  가로막지 않게 했다.
- **밀랍상 탭 UI 신설**(`WaxFigureView.ts` + `GameBoardWaxFigureStyles.ts`): 도감/경험
  아이콘 옆에 세 번째 아이콘(봉랍 돔 실루엣, `waxFigureIcon()` 신규)을 추가했다. 열면
  "이번 모험"(임시보관함, 담기/버리기/전부 정리) + "밀랍상함"(영구 보관, 종·색·성급별
  타일 + 효과 확률 %, 3개 이상이면 합성 버튼) 두 섹션이 뜬다. 경험 패널과 같은 유리질
  모달 톤을 쓰되 이로치는 옥빛(초록) 발광으로 정상 색(금빛)과 확실히 갈랐다.
- **아직 없는 것**: 30F 새싹 병아리 클리어 과정의 확정 튜토리얼 드랍(양초 키틴벌레),
  봉인 순간 연출(적 시체 → 별 → 비행 → 밀랍상 변환 → 아이콘으로 흡수), 밀랍상 효과가
  실제로 발동했을 때의 체인 배너 알림(그 전에 효과 자체를 실제 트랩/포자 판정에
  배선해야 한다 — 지금은 엔진에만 존재).
- 신규 유닛 테스트 2개 추가(총 13개) — 런 중 한도 무시 적재, 리셋 시 임시보관함 소멸.

### v2.11.0 — 2026-08-11 (실험적)

**밀랍상(蠟像) 수집 시스템 엔진 1차 착수 — 저장/판정 로직 + 디버그 커맨드만, 탭 UI는 아직 없다.**

- 포켓로그(PokéRogue)식 "사냥이 곧 동기부여"를 이 게임 톤(제물/굳음 테마)에 맞춰 옮겼다.
  적을 처치하면 낮은 확률로 **봉인**되어 밀랍상함(계정 전체, 런을 넘는 진열장)에 들어간다.
  잿빛 굴레와 **같은 마일스톤**(쉬움 100층 클리어, `gray-shackle-unlocked` 플래그 공유)으로
  열린다.
- 핵심 설계 결정 셋:
  1. **직접 스탯 절대 금지** — 성급이 올라도 체력/공격력을 안 준다. 오직 작은 확률형
     페시브만 준다(예: "거미줄 함정 무시 1%"). 확률은 방어구 감쇠 공식과 같은 수렴형
     곡선(`waxFigureEffectChance()`)이라 아무리 모아도 상한을 못 넘는다.
  2. **변종(이로치)은 정상 색과 다른 효과 축**을 준다 — 스탯 우위가 아니라 "존재 자체"가
     매력인 축(포켓로그의 이로치와 같은 이유).
  3. **합성은 플레이어가 직접**(같은 종+색 3개 → 다음 성급) — 자동 합성 없음. 무엇을
     남기고 합칠지 정리하는 과정 자체가 수집 루프다.
- `src/core/WaxFigureCollection.ts`: 순수 저장(`unmelting.waxfigures.v1`,
  `unmelting.waxfigures.capacityBonus` — `/리셋`이 접두사로 함께 지운다)/판정 로직만
  담당하고 게이팅은 호출부(index.ts) 책임으로 분리했다 — 테스트하기 쉽고 RL/러너
  계층에 의존하지 않는다.
- 처치 경로 두 곳(직접 타격 `index.ts` 3946줄대, 손패·조합 `awardHandKillDrops`)에
  `tryCaptureWaxFigureOnKill()` 한 함수로 배선했다 — 손패 전리품의 stingy/1장 상한(무한
  루프 방지용)과는 무관한 별도 자원이라 그 제약을 안 물려받는다.
- `/밀랍상 <종 이름>` 디버그 커맨드는 확률 굴림만 생략하고 등록/용량 검사·저장은 실제
  `captureWaxFigure()`를 그대로 탄다 — 탭에 값을 직접 꽂지 않는다. `/테스트`도 같은
  `gray-shackle-unlocked` 플래그를 마킹해 잿빛 굴레·밀랍상이 함께 열린다.
- **아직 없는 것**: 밀랍상함 탭 UI(도감/경험 옆 아이콘, 합성 인터랙션), 무역의 봉인
  확률/보관함 확장 구매, 종 목록 확장(엔진 검증용 양초 거미 1종뿐 — 종 풀을 채우는 게
  이 시스템의 실질 병목). 정산/캐시아웃 같은 강제 메커니즘은 의도적으로 **안 만들었다**
  — "죽으면 판이 끝난다"는 기존 규칙만으로 재방문 곡선을 만든다는 게 설계 예측이었다.
- 신규 유닛 테스트 11개(`WaxFigureCollection.test.ts`) — 봉인/미등록 종/용량 상한/합성
  성공·실패/변종 독립 집계/확률 수렴 곡선/손상 저장값/`/리셋` 접두사를 검증한다.

### v2.10.0 — 2026-08-11

**잿빛 굴레(엔드리스) 실제 해금 게이트 추가 — 쉬움 난이도 100층 클리어.**

- 200층 진엔딩 조건은 뺐다. 200층 콘텐츠가 없는 지금 걸어 두면 영원히 못 여는 칸이 되고,
  게임을 필요 이상으로 길게 만든다는 판단이다. 100층 엔딩을 축으로 캐릭터 능력치를 더
  키우는 방향으로 난이도 밸런스를 잡을 예정 — 관련 스토리 떡밥(101~200층/다음 난이도)은
  `Unmelting_Story.md`에 `*미구현 — 향후 콘텐츠 방향 메모*`로만 가볍게 남겨 둔다.
- 실제 해금 판정: `run_clear_100_turns`로 런이 끝났고 이번 런 난이도가 `easy`일 때만
  `EnaAutonomousLearner.recordFirstSeen('gray-shackle-unlocked')`로 영구 마킹한다
  (`SettlementScreen.showGameOver()`, 통산 기록 1회 가드와 같은 곳). `HearthScene.cellUnlocked()`가
  이 플래그로 중앙 칸을 연다. `unmelting.` 접두사라 `/리셋`이 함께 지운다.
- 100F 클리어는 난이도와 무관하게 인정되지만(통산 기록엔 그대로 남는다), "잿빛 굴레가
  풀렸다" 문구는 쉬움 클리어에만 붙인다 — 보스 격파 직후 알림(`ShopFlowManager`), 정산
  타이틀(`SettlementScreen`), 서고 일지 제목(`HearthScene.libraryEntryTitle`) 세 곳 전부
  같은 조건(`difficulty === 'easy'`)으로 갈랐다. 한 곳만 고치면 클리어 직후엔 "풀렸다"고
  말해 놓고 서고 일지에서는 안 풀린 것처럼 보이는 모순이 생긴다.
- 둘째 캐릭터(길드의 "캐릭터 해금" 연결)는 보류로 문서를 고쳤다 — 메타 해금으로 늘릴지
  게임 밖에서 고정으로 들여올지 미정이라, 모험 셔터의 캐릭터 선택 UI는 "추후 해금 전까지"가
  아니라 **의도적으로 꺼져 있다**는 걸 `CLAUDE.md`/`Unmelting_Game_Concept.md`에 명시했다
  (코드 동작 자체는 기존 `installFixedCharacter` 그대로 — 선택 캐러셀/모션 없이 에나 고정).

### v2.9.4 — 2026-08-11

**에나 회피 클러치가 "살짝 일찍" 터지던 타이밍을 방패 막기와 같은 파도 beat로 맞췄다.**

- 원인: `TurnManager.runEnemyPhase()`는 모든 적의 공격 결과를 **한 번에 동기 계산**하고,
  그 뒤 `GameBoardRenderer.animateEnemyAttacks()`가 적 하나씩 파도처럼 슬램 연출을 낸다
  (`CLAUDE.md` "적 페이즈는 파도다" 불변식). 방패 막기(`hit.blocked`)는 이 파도의 각 beat
  (`playEnemySlam`의 `onImpact`)에서 피드백을 내는데, 회피는 `rollMinorClutch` 판정과
  함께 대사·클러치 배너·토스트를 **판정 즉시**(파도 애니메이션이 시작하기도 전) 띄우고
  있었다 — 그래서 실제 슬램/충돌 타이밍보다 화면상 먼저 터지는 것처럼 보였다.
- `CompanionDirector.tryCompanionIncomingDodge()`를 판정과 알림으로 쪼갰다:
  `shouldDodgeIncoming()`(피해 적용을 건너뛸지 결정하는 순수 판정, `runEnemyPhase`가 여전히
  동기로 호출)과 `announceDodge()`(대사/배너/토스트, 방패처럼 파도 beat에서만 호출)로 분리.
  `EnemyHit`에 `dodgedDamage`(무효화된 피해량)를 추가해 알림 시점에도 문구를 그대로 낼 수
  있게 했고, `animateEnemyAttacks()`에 `onDodge` 콜백을 받아 `playEnemySlam`의 `onImpact`
  안에서 `announceDodge()`를 부르도록 배선했다(호출부 3곳 모두).
- 방패 막기 로직은 이미 같은 beat에서 정확히 돌고 있어 별도 수정이 필요 없었다 — 이번
  회피 수정은 방패가 쓰던 것과 같은 경로에 맞춘 것이다.

### v2.9.3 — 2026-08-11 (실험적)

**레시피 브래킷 미리보기를 특정 슬롯이 아니라 브래킷 중간에, 배경은 반투명하게 바꿨다.**

- 미리보기 세로 위치를 `previewSlotIndex`(재료 중 한 장)에서 브래킷 구간의 세로
  중앙(`(minY + maxY) / 2`)으로 옮겼다 — "재료 중 하나에 붙는 것"보다 "묶은 결과가
  중간에 뜬다"가 더 자연스럽게 읽힌다. 더는 안 쓰는 `previewSlotIndex`/`newCardSlotIndex`
  배선(`GameBoardRenderer.showRecipeBracketHint` 세 번째 인자,
  `CompanionDirector`의 uid 탐색)도 함께 걷어냈다.
- 패널 배경 알파를 호버용 base(0.96~0.98)보다 크게 낮추고(0.62~0.68) `backdrop-filter:
  blur(6px)`를 얹었다 — 그냥 알파만 낮추면 뒤 손패 그림과 글자가 겹쳐 안 읽히므로, 흐림으로
  대비를 지키면서 판만 옅게 비치게 했다.

### v2.9.2 — 2026-08-11 (실험적)

**레시피 브래킷 미리보기가 브래킷과 동떨어져 뜨던 위치 버그를 고쳤다.**

- `.ena-recipe-bracket-preview`가 base `.hand-recipe-preview`의 `right: calc(100% + 222px)`를
  그대로 물려받고 있었다 — 그 값은 **단일 슬롯 `<li>`** 기준(뒤에 깔린 flip 카드 폭만큼 띄우는
  값)인데, 미리보기를 `.hand-stack` 직속 자식으로 옮기면서 기준이 스택 전체 폭으로 바뀌어
  브래킷에서 한참 떨어진 플레이어 초상화 쪽에 떴다. `right: calc(100% + 26px)`로 브래킷
  (`left: -13px`, 폭 9px) 바로 옆까지 당기고, 미리보기 우측에 26px짜리 금빛 이음선(`::after`)을
  붙여 "브래킷에서 이어지는" 모양으로 만들었다.
- v2.9.1 스크린샷에서 브래킷이 손패 2장만 잇던 건 코드 버그가 아니라 검증용 Playwright
  스크립트가 `showRecipeBracketHint`를 슬롯 2개로 수동 호출한 탓이었다 — 실제
  `CompanionDirector.tryCompanionPrediction()`은 레시피 재료(`Object.entries(recipe.ingredients)`)를
  전부 순회해 슬롯을 모은다. `/악마소환준비`로 4장(제물 양초·의식 양초·양초·불씨)을 쥔 채
  4슬롯을 직접 넘겨 재검증했고, 이번엔 넷 다 이어졌다.

### v2.9.1 — 2026-08-10 (실험적)

**레시피 브래킷 힌트 3종 손질: 픽셀 정렬 · 미리보기 위치 · 발광 강조 + 렌더 생존.**

- **픽셀 정렬**: `offsetTop` 대신 `getBoundingClientRect()`로 슬롯 중심을 재서 낙하/재배치
  애니메이션 도중에도 "지금 실제로 보이는" 좌표를 잡고, 틱(가지) 두께(2px)의 절반만큼
  보정해 브래킷이 카드 중심보다 짧아 보이던 문제를 없앴다.
- **미리보기 위치**: 기존 호버용 `.hand-recipe-preview` CSS(`right: calc(100% + 222px);
  top: 50%`)를 그대로 슬롯 `<li>`에 얹었던 게 "위치가 이상하다"는 원인이었다 — 슬롯 자체의
  정렬/flip 애니메이션에 딸려 다녔다. 브래킷과 같은 좌표계(`.hand-stack` 직속 자식)로
  옮기고 인라인 `top`을 직접 계산해 붙인다.
- **발광 강조 + 자연 소멸**: 등장은 그대로, 표시 유지(`RECIPE_BRACKET_HOLD_MS` 4.2초) 동안
  `box-shadow`/`filter` 발광이 은은히 맥동하고, 그 뒤 반투명하게(`.is-out` 트랜지션) 옅어져
  사라진다. 한 번 튀어나오고 끝나는 1회성 keyframe에서 트랜지션 기반으로 바꿨다.
- **렌더 생존**: "몇 초짜리 힌트인데 렌더 한 번에 사라지면 안 된다"는 지적을 반영해
  `activeEnaHint`/`reattachEnaHintPulses`와 같은 패턴으로 `activeRecipeBracketHint` 상태 +
  `reattachRecipeBracketHint()`를 추가했다. `render()`가 DOM을 통째로 갈아 끼워도 남은
  시간만큼(`endsAt` 기준) 다시 그려 넣고, 실제로 다 되면 스스로 걷힌다 — 지난 버전의
  "손패가 다시 그려지면 사라진다"는 한계는 이번 판에서 해소됐다.
- Playwright로 직접 검증: 브래킷 틱-카드 중심 정렬, 미리보기 앵커, 발광 페이드,
  그리고 힌트가 떠 있는 도중 실제 `render()`를 한 번 더 돌려도 살아남고 hold+fade
  시간이 지나면 정확히 사라지는지까지 확인했다.

### v2.9.0 — 2026-08-10 (실험적)

**에나의 레시피 완성각 지원에 ㄷ(⊏)자 손패 브래킷 + 완성각 미리보기를 추가했다.**

- HandCardAdvisor가 이미 갖고 있던 "이 카드가 마지막 재료면 완성 효과를 지원 근거로
  가산한다"(`recipeCompletionBonus`, 예: 강적 앞에서 양초 스매쉬용 촛농/양초 지원)는
  로직 자체는 그대로 두고, 그 판단이 실제로 어떤 레시피였는지(`recipeId`)를
  `SupportRanking` → `ThreatReport.recommendationRecipeId`까지 끝까지 들고 오게 배선했다.
- `CompanionDirector.tryCompanionPrediction()`이 지원 카드를 지급한 직후, 해당
  레시피의 재료 슬롯들을 손패에서 다시 찾아 `GameBoardRenderer.showRecipeBracketHint()`를
  부른다: 관여한 슬롯들을 금빛 ㄷ자 브래킷 한 줄로 묶고, "에나의 완성각 / 레시피명 /
  효과" 미리보기를 함께 띄운다.
- 이 미리보기는 기존 `.hand-recipe-preview`(호버 시 뜨는 "발동 조합")와 같은 시각
  언어를 쓰지만 다른 조건이다 — 기존 것은 **체인 시퀀스** 기준(`previewTriggeredRecipes`가
  `chain.sequence`로 판정)이라 재료를 손에 쥐고만 있는 이 시점엔 아직 존재하지 않는다.
  그래서 조건 없이 뜨는 전용 aside(`.ena-recipe-bracket-preview`)를 따로 만들었다.
- 실험적 프로토타입으로 남겨 둔다(정렬·위치·발광·render-survival은 v2.9.1에서 손질).

### v2.8.1 — 2026-08-10

**CLAUDE.md에 "호버 툴팁은 Tooltip.ts 하나다" 규칙을 추가했다.**

- 앞으로 새 툴팁은 `data-tooltip="문구"`만 달아 `src/ui/Tooltip.ts`(v2.8.0에서 추가한
  공용 창구)에 연동한다. `title=`을 새로 쓰거나 화면마다 직접 호버 박스를 만들지
  않는다 — 표시/위치/줄바꿈을 이 모듈 하나가 전담하게 해 다음 사람이 매니저처럼
  한곳에서 관리할 수 있게 한다.

### v2.8.0 — 2026-08-10

**공용 툴팁 박스(`src/ui/Tooltip.ts`)를 추가하고, 브라우저 기본 title= 표기 일부를 옮겼다.**

- 검은 배경 + 얇은 금빛 발광 테두리, 16px 폰트의 통일된 툴팁 박스. `data-tooltip="문구"`
  하나만 달면 뜬다 — document 위임 리스너 하나가 전체를 처리하는 단일 창구다.
- 줄바꿈 규칙: 마침표(.) 경계에서만 줄을 나눈다. 그 외엔 `width: max-content`로
  내용 폭만큼 늘어나 중간에 어색하게 꺾이지 않는다. 여러 항목을 보여줄 때는
  각 항목 끝에 마침표를 찍어 넘기면 자동으로 한 줄씩 쌓인다.
- 백작 가위바위보 이벤트의 손패 확률 칩·아이템 설명·불안 표시 등 기존 `title=`
  표기를 이 박스로 옮겼다.
- 거점 "예상 시작 불빛"에 마우스를 올리면 출처별 내역이 뜬다(예: "서고 +77." /
  "만찬 +100."). 출처는 `LobbyLightPreviewSource[]` 배열이라 항목이 늘어나도
  `setLobbyLightPreview(amount, sources)`에 항목만 추가하면 된다.

### v2.7.2 — 2026-08-10

**여정의 유산 표기를 다듬고, 거점 "예상 시작 불빛" 미리보기 누락을 고쳤다.**

- 문구를 "획득량 불빛"→"불빛 획득량", "시작 불빛"으로 어순을 바로잡았다.
- 각 효과(불빛 획득량/시작 불빛/최대 체력 등)를 `.hearth-library-legacy-chip`으로
  감싸 푸른 발광으로 강조하고, 접두사·" · " 구분자는 차분한 색으로 남겨 서로
  또렷이 갈리게 했다. 칩 안의 인라인 SVG 아이콘은 `text-shadow`가 먹지 않아
  글자만 빛나고 아이콘은 안 빛나는 어색함이 있었는데, `filter: drop-shadow`를
  아이콘에 별도로 얹어 함께 빛나게 했다.
- 거점 좌측 "예상 시작 불빛" 미리보기(`setLobbyLightPreview`)가 만찬 버프만 반영하고
  여정의 유산 시작 불빛은 빼먹어 항상 0으로 보이던 버그를 고쳤다. 로비 진입 시
  `lobbyLightPreviewBase()`(여정의 유산 시작 불빛)를 기본값으로 깔고, 만찬을
  고르면 그 위에 만찬 버프를 더한다.

### v2.7.1 — 2026-08-10

**서고 "책 같은 느낌"을 걷어내고 여정의 유산 표기에 실제 아이콘을 붙였다.**

- 반투명 유리판 배경(backdrop-filter)과 통산 판 수에 따라 쌓이던 책등 그림자
  (`applyLibraryStackTier`)를 제거했다 — 폭 확장(880px)과 난이도 칩은 그대로 두고
  표면 장식만 이전 UI로 되돌렸다.
- 여정 목차 줄 왼쪽의 결과 표시용 동그라미 점(`.hearth-library-toc-mark`)을 뺐다 —
  난이도 칩과 결과색 테두리(팝업 쪽)만으로도 충분히 구분된다.
- "여정의 유산" 줄의 불빛 수치 두 종류(획득량 %, 시작 지급량)를 `resourceIconTextHtml`과
  같은 자원 아이콘 표기(`sparkleIcon` + `.resource-term-icon`, HUD·카드 설명문과 동일
  창구)로 바꿨다 — "불빛"이라는 글자 대신 실제 게임에서 쓰는 불빛 아이콘이 수치
  바로 앞에 붙는다.

### v2.7.0 — 2026-08-10

**"여정의 유산" — 통산 기록에서 파생하는 아주 작은 영구 보너스 시스템을 추가했다.**

- `src/core/PlayerLegacy.ts`(신규, 순수함수 `computePlayerLegacyBonus`) — 통산 기록
  (`LifetimeRecord`)만 읽고 별도 저장소를 두지 않는다(단일 출처 유지). 로그라이크는
  수십 판이 기본이라는 전제로, 진행도를 총 런 수 앵커(60판) 기준 √ 압축해 몇 판만에
  확 오르지 않고 많이 해도 무한히 오르지 않게(포화) 했다.
- **성향 블렌딩**: 에나 성향 성장과 같은 발상 — 통산 활동 비중(전투/수집/생존)에서
  가장 우세한 축이 어느 보너스를 더 키울지를 정한다. 안전 축(불빛 획득 +최대 2%,
  시작 불빛 +최대 100, 최대 체력 +최대 5)은 항상 조금씩 오르고, 손패 계산에 직접
  끼는 축(빛 게이지 상한·손패 한도·공격력, 각 +1)은 진행도가 사실상 포화하고 성향이
  압도적으로 쏠렸을 때만, 그것도 최대 1만 오른다. 정합 테스트는 `PlayerLegacy.test.ts`.
- 집계 축을 위해 `GameState.runFlowersHarvested`(직접 타격 경로)를 새로 추적하고,
  `LifetimeRecord`에 `totalFlowers`·개별 런 `flowers`/`difficulty` 필드를 추가했다
  (모두 선택 필드라 구버전 저장본도 그대로 파싱된다).
- 보너스는 런 시작(직업 선택 뒤, 새싹 병아리 제외)에 적용된다. 시작 불빛은 현재
  배율로 먼저 지급한 뒤 유산의 불빛% 가산을 곱해, 방금 받은 시작 불빛이 자기 자신에
  다시 곱해지는 이중 가산을 피했다.

**서고 화면을 더 넓게, 난이도 표기 + 책이 쌓이는 느낌으로 다시 그렸다.**

- 모험일지 패널 폭을 560px → 880px로 넓혀 화면을 더 활용하고, 종합 일지/목차를
  감싸는 반투명 유리판(backdrop-filter blur)을 얹어 텍스트가 배경 위에 그냥 떠
  있지 않고 책 페이지 한 장처럼 표면을 갖게 했다.
- 각 여정 줄에 난이도 칩(새싹 병아리/쉬움/보통/테스트)을 추가했고, 상세 팝업 표제
  줄에도 같은 라벨을 붙였다.
- "여정의 유산" 한 줄(성향 + 현재 보너스)을 종합 일지 아래에 추가해 다음 런에 뭐가
  얼마나 붙는지 미리 보여준다.
- 통산 판 수 구간(0/1~4/5~14/15~39/40+)마다 유리판 뒤로 책등이 한 겹씩 쌓이는
  layered box-shadow를 추가했다 — 새 DOM 요소 없이 그림자만 겹쳐 가볍고, 서고를
  오래 채울수록 쌓인 책처럼 보인다.

### v2.6.1 — 2026-08-10

**서고 모험일지를 얇은 종합 개요 + 책 목차형 여정 목록으로 다시 그렸다.**

- 통계 7개를 각자 행으로 늘어놓던 두꺼운 원장(`.hearth-library-ledger`)을 정산 화면
  우측 하단과 같은 두 줄 요약(`.hearth-library-summary`)으로 줄였다 — "종합 일지"는
  최상단에 얇게, 나머지 공간은 개별 여정에 내준다.
- 상자형 카드였던 여정 한 줄(`.hearth-library-entry`)을 책 목차처럼 얇은 홑줄
  (`.hearth-library-toc-row`)로 바꿨다 — 결과는 작은 점 표식(금빛/붉은빛), 제목과
  층·날짜는 점선 리더로 잇는다. 상자 테두리·배경 없이 줄만 차곡차곡 쌓여 책장에
  더 가깝게 읽힌다.
- 실제 마우스 클릭(Playwright 합성 클릭이 아닌)으로 여정 목차 → 상세 팝업 열림을
  재확인했다 — 클릭이 안 먹는다는 보고가 있었으나 이번 코드에서는 재현되지 않았다
  (구버전 캐시였을 가능성이 높다).

### v2.6.0 — 2026-08-10

**서고 "여정 상세" 카드를 다시 지었다 — 잘리던 아코디언 대신 유리판 팝업 + 활약 손패/위험한 적/에나 성좌.**

- 서고 패널 안에서 펼치던 아코디언(`.hearth-library-entry-detail`)은 패널 높이에 막혀
  통계 뒷부분이 잘렸다(전 버전 리포트). document.body 전용 host에 뜨는 팝업
  (`.hearth-library-detail-backdrop`)으로 바꿔 구조적으로 해결했다 — 테두리 없는 검은
  반투명 유리판(`backdrop-filter: blur`)에 표제·부제를 타이틀처럼 모아 앉혔다.
- 새 데이터를 이번 런에서 뽑아 통산 기록에 함께 싣는다: **활약한 손패**(가장 많이 쓴
  손패, `GameState.runCardUsageCount` → `HandSystem.useSingle`에서 집계) · **위험했던
  적**(누적 피해가 가장 큰 적, `runEnemyDamageByName` → `TurnManager.runEnemyPhase`에서
  집계) · **에나의 성장**(정산 화면과 같은 성좌 위젯을 저장된 축 원시값만으로 다시 그리는
  `ExperienceView.renderHistoricalConstellation`) · 정산의 에나 한마디(제목 아래 훅으로).
- `LifetimeRunResult`/`LifetimeRunEntry`(`LifetimeRecord.ts`)에 위 필드를 전부 선택
  값으로 추가했다 — 구버전 저장본(필드 없음)도 그대로 파싱되고, 팝업은 없는 항목만
  조용히 생략한다(빈 박스 없음).

### v2.5.3 — 2026-08-10

**무역 탭 팩 칸을 조금 더 작게, 글씨는 더 크게 다시 그렸다.**

- `.hearth-trade-pack`(모든 무역 탭 팩/카드가 공유하는 단일 클래스) 크기를 줄이고
  (`142~210px` → `118~176px` 폭), 제목·설명 폰트는 키워(`18~25px` → `20~29px`,
  `12~15px` → `13~17px`) 정보 밀도보다 판독성을 우선했다.
- 좌우로 스크롤되며 잘리는 뷰포트(`.hearth-trade-pack-area`/`-grid`)의 좌우 여백을
  넓혀(`8~12px` → `20~44px`) 가장자리 칸이 화면 끝에 바짝 붙어 잘려 보이지 않게 했다.

### v2.5.2 — 2026-08-09

**기초 해금팩 온오프를 무역 탭 상시 토글에서 런 시작 화면으로 옮겼다.**

- 무역 2번 탭에 항상 떠 있던 "보유 항목 온오프" 타일을 제거했다. 상점을 열 때마다
  거기서 손패/유물을 켜고 끄는 것이 자리를 차지하고, 정작 그 결정이 영향을 주는
  시점(이번 런 시작부터 해금 드래프트)과 멀리 떨어져 있었다.
  대신 직업 선택 뒤·"시작부터 해금" 드래프트 앞에 새 화면(`openUnlockLoadoutScreen`,
  `src/index.ts`)을 넣어 보유 항목이 있을 때만 지나가며 고르게 했다 — 여기서 끈
  항목은 바로 이어지는 드래프트 후보에서도 즉시 빠진다.
- 저장 값(`isCardUnlockPackEnabled`/`isRelicPoolEnabled`)과 판정 함수는 그대로다
  (`MetaContentUnlocks.ts` 단일 출처 유지). UI 위치만 옮겼다.

### v2.5.1 — 2026-08-09

**기초 해금팩 온오프 표기를 검은 덮개 + X로 바꾸고, 손패/유물 풀을 넓혔다.**

- 꺼진 항목은 흐리게만 하지 않고 아트 위를 검게 덮은 뒤 X 마크(`Icons.ts`
  `closeIcon()`)로 금지를 알린다. 채도만 빼면 '꺼짐'과 '흐림'이 헷갈린다.
- 기초 해금팩 손패 풀을 6종 → 10종(찻잔·물뿌리개·정원 가위·손거울 추가), 유물 풀을
  4종 → 6종(기사도·훌륭한 대화수단 추가)으로 넓혔다.

### v2.5.0 — 2026-08-09

**무역 2번 탭에 "기초 해금팩"을 추가했다 — 메타 잠금 손패/유물을 2$에 영구 해금한다.**

- 곧바로 필요 없지만 좋은 손패 6종(양초의 서·모닥불·신사모·칼날의 서·검집·주전자)과
  유물 4종(에나벨라의 반지·펜던트·소중한 머리·황금 열쇠)을 큐레이션했다. 유물은
  `metaLocked: true`(Relics.ts)로 기본 유물 풀(상점/제단/이벤트/보스 보상)에서 아예
  빠져 있다가 해금해야 합류한다(`isRelicMetaAvailable` 단일 창구, 3개 호출부 공용).
- 손패는 유물과 뜻이 다르다 — 해금해도 곧장 손에 들어오지 않는다. 미해금 상태에선
  기존 해금팩(런 한정 임시 해금) 후보에서도 아예 빠지고(`getRunEligibleCardIds`),
  해금해야 비로소 다른 runLocked 카드처럼 매 런 해금팩을 열어야 만날 수 있게 된다.
- 해금한 항목은 온오프 토글로 노출을 잠깐 멈출 수 있다(기본 해금 항목은 토글이 없다,
  이 팩으로 산 항목만) — 유물은 풀 등장을, 손패는 해금팩 등장을 끈다. 소유 자체는
  유지되므로 메타해금으로 덱빌딩과 안 맞는 걸 열어봤다가 다시 잠가 풀을 유지할 수 있다.
- "시작부터 해금" 드래프트를 추가했다 — 해금한 손패 중 온오프가 켜진 것들에서 런 시작
  시 1장을 골라 해금팩 없이 턴 1부터 바로 쓴다(`STARTING_UNLOCK_DRAFT_CAP`, 현재 1).
  새싹 병아리에는 이 이점을 주지 않는다.
- `/테스트`는 기초 해금팩 풀 전체를 함께 해금한다(`unlockAllBasicUnlockPack`).
- 단일 출처는 `src/core/MetaContentUnlocks.ts`.

### v2.4.2 — 2026-08-09

**로비에 "예상 시작 불빛" 패널을 화폐 패널처럼 띄웠다.**

- 좌측 불빛 칸이 거점(로비)에서 `body.hearth-lobby .left-panel .score-panel-total`
  규칙으로 화면 밖으로 숨겨져 있던 것을 확인했다 — 화폐만 남기고 불빛은 늘 안 보였다.
  이제 불빛 칸도 화폐처럼 로비에 남기되, 실제 불빛(매 런 0으로 초기화돼 의미가 없다)
  대신 다음 런의 시작 불빛 버프(만찬 등)를 "예상 시작 불빛"으로 보여준다.
  `GameBoardRenderer.setLobbyLightPreview()` — 로비 진입 시 0, 만찬 완성 시 그 값,
  실제 런 시작 시 null(원래 불빛 표시로 복귀)을 넘긴다.

### v2.4.1 — 2026-08-09

**무역 탭 카드를 드래그로 밀 수 있게 하고, 탭 라벨을 정리했다.**

- 무역 팩 뷰포트에 grab-scroll을 추가했다 — 직업 선택 캐러셀과 같은 손맛(포인터 드래그)을
  일반 카드 그리드에 맞게 scrollLeft 기반으로 옮겼다(3D 코버플로우는 과했다). 드래그로
  카드를 민 직후의 click은 구매 오작동으로 잡히지 않게 별도로 막았다.
  카드가 뷰포트보다 적을 때는 가운데로 모이고, 전체 영역도 살짝 아래로 내렸다.
- 무역 탭 좌측 라벨(1~6번)을 살짝 아래로 내리고, 1번은 "기본기능"(실제 개방 카드가 붙어
  있는 탭), 2~6번은 "미개발"로 이름을 바꿨다.

### v2.4.0 — 2026-08-09

**서고를 처음부터 열고, 개별 런 일지(여정)를 새로 추가했다. 잿빛 굴레 개방 조건도 손봤다.**

- 서고(LIBRARY_INDEX)가 코드상 계속 잠겨 있던 것을 열었다 — 통산 기록을 보여주는 칸이라
  잠글 이유가 없다(기존 게이팅 함수가 항상 false를 돌려주고 있었다).
- `LifetimeRecord`에 최근 30건 개별 런 일지(`history`)를 추가했다 — 기존엔 통산 집계값만
  있고 "언제 어떻게 끝났는지"는 저장되지 않았다. 각 런의 결과(클리어/사망 사유)·도달
  층·처치/함정/보물/불빛과 저장 시각을 기록한다.
- 서고 화면에 "최근 여정" 목록을 추가했다 — 런 하나가 책등처럼 가로로 긴 한 줄(제목·
  도달 층·날짜)로 나오고, 누르면 정산 화면과 같은 스탯 요약이 아래로 펼쳐진다(여러 줄
  동시에 열어 둘 수 있다). 제목은 정산 화면의 승리/패배 헤드라인을 그대로 재사용한다.
- 잿빛 굴레(거점 중앙 칸) 설명문을 "200층 진엔딩 클리어 시 개방" → "쉬움 난이도
  클리어 시 개방"으로 바꿨다 — 200층 콘텐츠가 없는 현재 단계에 맞춰 실제로 도달
  가능한 목표로 정정했다.

### v2.3.2 — 2026-08-09

**만찬 완료 후에도 9칸 그리드의 "Free" 배지가 사라지지 않던 버그를 고쳤다.**

- 9칸 그리드는 만찬 셔터 뒤에 그대로 남아 있는 DOM이라, 식사를 마쳐 `dinnerConsumed`가
  true가 돼도 그리드를 다시 그리지 않으면 뒤로가기 즉시 예전 Free 배지가 그대로 보였다.
  `finishDinner()` 끝에서 `refreshCellLocks()`를 호출해 그 자리에서 바로 지운다.
- 가벼운 한끼($5) 활성화 로직 자체는 재확인 결과 이미 정상 동작한다(잔액 5로 정확히
  버튼이 열린다) — 별도 수정 없음.

### v2.3.1 — 2026-08-08

**가벼운 한끼($5) 스폰 확률 보정 수치를 낮췄다.**

- 소시지/꿀/계란(적·함정·보물 확률)의 커먼/레어/에픽 값을 5/8/12 → 3/5/10으로
  내렸다. 에픽(레전더리에 준하는 최고 등급)만 8 근처로도 충분히 체감되는 수준이라
  판단해 커먼·레어를 더 낮췄다.

### v2.3.0 — 2026-08-08

**만찬(last-supper) 스탯 미적용 버그를 고치고, 유료 1단계 "가벼운 한끼"($5)를 추가했다.**

- **버그**: 만찬 재료 중 `상점 할인`(비스킷/짭짤한 소스/콩)과 `시작 불빛`(콩고기/기름진
  소스/양파)을 고르면 카드 설명에는 찍히는데 실제로는 전혀 적용되지 않았다
  (`applyRelicPurchaseEffect('last-supper')`가 6종 중 4종만 처리). 이번에 두 스탯을 마저
  연결했다 — 상점 할인은 `enhancements.shopDiscountPct`에, 시작 불빛은 `gainFixedLight`로.
- **숨은 지뢰까지 같이 고쳤다**: 직업 선택이 `shopDiscountPct`를 대입(`=`)으로 덮어써서,
  만찬이 먼저 쌓아 둔 할인이 상점 할인이 있는 직업(귀족)을 고르면 조용히 사라지는
  경로가 있었다. 직업 쪽도 덧셈(`+=`)으로 맞춰 두 출처가 항상 누적되게 했다.
- **가벼운 한끼($5)**: 만찬 레일의 두 번째 팩을 실제로 구매 가능하게 열었다. 메타 지갑
  ($)에서 결제하고, 무료 만찬보다 한 단계 위 재료 풀(007~012, 기존 6종 스탯의 상위
  버전)에서 3장을 고른다. 손패/불씨 한도는 무료와 달리 커먼 등급도 열려 있다.
  전용 3종(소시지=적 확률↓·꿀=보물 확률↑·계란=함정 확률↓, 013)도 새로 추가했다 —
  각각 메인/소스/재료 한 자리에서만 나온다.
  잔액이 모자라면 결제 없이 흔들림만 준다(무역 개방 카드와 같은 거절 방식).
- `CardSpawner`에 유물 스폰 보정 축을 하나(`trap`) 더 열어 `{{spawn}}` 표기·실제 스폰
  양쪽에 반영되게 했다 — 기존엔 함정 전체를 유물이 조정할 수단이 없었다(포자 전용
  보정만 있었다).
- 이미지 파일은 아직 없다(`spriteForDinner`가 없으면 CSS 그라디언트로 조용히
  대체한다) — `sprites/dinner/{main,sauce,topping}/{main,sauce,topping}_007~013.webp`에
  넣으면 코드 변경 없이 바로 반영된다.
- 만족스러운 식사($10)·호화로운 만찬($30)은 아직 잠금 카드로 남겨 뒀다(추후 단계).

### v2.2.7 — 2026-08-08

**난이도 선택·도감의 "온보딩" 용어를 플레이어 시선의 문구로 정리했다.**

- 새싹 병아리 난이도 태그를 "온보딩 · 30층" → "모험의 시작 · 30층"으로, 설명을
  "적·함정·보물의 기본기를 익힌다"(가르치는 어투) → "바위·덤불·잡동사니와 함께
  첫걸음을 뗀다"(모험 서사 어투)로 바꿨다.
- 도감의 덤불·잡동사니 요약 노트에 남아 있던 "온보딩 축약형" 문구를 "모험 초반의
  축약형"으로 바꿨다. 내부 식별자·코드 주석의 `onboarding`/`온보딩`은 그대로 둔다 —
  바뀐 것은 플레이어에게 보이는 문구뿐이다.

### v2.2.6 — 2026-08-08

**양초 덱을 방패/밀랍 축으로 재설계하기 위한 첫 걸음 — 제물 양초·의식 양초·부두 인형을 손질했다.**

- 제물 양초: 자해 2 → 1, 피해는 (1.5공+3) → (1.0공+2)로 가볍게 바꾸고, **처치 시 방패 +3**을
  새로 얻는다(트리플은 자해 3 · (3.0공+6)피해 · 처치 시 방패 +6). 태그를 `flame`에서
  `shield`로 바꿔 양초 계열의 골격이 방패에서 나온다는 것을 잇는다 — 방패·밀랍을 쓰는
  양초 덱의 기초 카드가 되도록 설계했다. 처치 판정은 모닥불의 회복 경로와 같은 방식
  (`sacrificeCandleShieldOnKill`)을 그대로 재사용한다.
- 의식 양초: 자해 2 → 1. 트리플도 자해를 늘리지 않고(단일과 동일 자해 1) 드로우만
  2장 → 3장으로 늘렸다.
- 부두 인형 트리플: "필드 전체 처리"가 아니라 "모든 보물/함정 처리"로 범위를 명확히
  하고, 자해를 2 → 4로 올렸다 — 효과 자체는 유지하되 트리플이 단일과 같은 비용으로
  거저 강력해지지 않게 했다. 강력한 한 방은 앞으로 레시피 쪽에서 설계하고, 트리플
  기본 효과는 적당한 선에서 유지한다는 원칙을 따른다.

### v2.2.5 — 2026-08-08

**바늘(needle)에서 처치 회복을 떼고 자해+피해로만 남겼다.**

- 초반 커먼 카드치고 자해·피해·회복 세 축을 한 장에 다 쥔 게 과했다. 처치 시 체력
  회복(단일+3/트리플+5)을 없애고, 자해를 대가로 적을 잡는다는 문장에 맞춰 피해를
  0.5공+1 → 1.0공+1로 올렸다. 트리플은 배율을 더 올리지 않고 같은 위력탄 3발 그대로다
  (자해 1/2는 불변). `damageProfile`·설명문·Shift 수식 렌더러를 함께 맞췄다.

### v2.2.4 — 2026-08-08

**새싹 병아리(30F 온보딩) 유물 풀에 칼날 파편 빌드의 기초 씨앗 2종을 열었다.**

- 숫돌(적 처치 시 칼날 파편 획득) · 망치(칼날 손패 사용 시 25% 확률로 칼날 파편 획득)를
  온보딩 유물 8종 목록에 추가했다. 칼날 파편 손패 자체는 이미 잠금 없이 기본 드로우
  풀에 있으므로, 처치/사용만으로 더 얻는다는 단순한 문장으로 빌드 축의 존재를 보여 준다.
- 증폭(연마)·심화 계열(레어 이상 파편 유물)은 "아직 안 배운 개념"이라 제외했다 — 온보딩
  유물 선정 기준(눈에 보이는 기본 개념만)을 그대로 따른다.

### v2.2.3 — 2026-08-08

**적 처치·즉사·폭탄 연출을 다듬고, 처치 뒤 다음 행동을 막던 숨은 지연을 없앴다.**

- 회피 시 뜨던 "회피!" 플로팅 텍스트를 뺐다 — 에나의 "날렵한 몸놀림" 클러치 대사가 이미
  회피를 알리므로 중복이었다.
- 일반 적이 플레이어를 때릴 때 body에 띄우는 돌진 복제 아래 원본 카드가 반투명(0.38)으로
  남아 "카드가 두 장"으로 보이던 문제를 고쳤다 — 복제가 돌진하는 동안 원본은 완전히 숨긴다.
- 손패·직접 타격으로 죽는 적의 체력 롤링이 실제 죽는 순간(모델에서 이미 제거된 뒤)에는
  멈춰 있던 버그를 고쳤다 — 카드를 못 찾으면 죽어서 사라진 것이므로 0으로 굴러 내려간다.
- **처치 연출이 매번 같아 밋밋하게 느껴지던 문제**의 실제 원인을 찾았다: 피해 수치가
  화면에 오래 머물도록 늘린 `DAMAGE_FLOAT_MS`(1.5초)를 처치·다음 행동을 막는 같은 beat에
  물려 둬서, 카드가 이미 다 사라진 뒤에도 최대 1초 넘게 빈 자리가 멈춰 있었다. 피해
  수치는 카드와 독립된 body 요소라 계속 떠 있을 수 있으므로, 이 beat는 버스트·흔들림·
  롤링까지만 기다리고 수치 자체는 화면에서 알아서 마저 떠 있게 분리했다.
- 처치 연출을 무작위 세 갈래로 갈랐다(회색으로 질려 스러짐 · 위로 스르륵 흩어짐 · 뽀용
  줄어듦) — 매번 같은 연출이던 것을 다양화했다. 폭탄은 별도로 확대 후 잔상처럼 번지며
  사라지는 전용 연출을 받는다("펑" 터지는 것이므로).
- 즉사(참격 트리플·양초 스매쉬 등 체력을 깎지 않고 바로 제거하는 경로)는 일반 피해
  롤링이 없어 사망 신호가 아예 없었다 — 화려한 버스트 + 검붉은 "즉사" 전용 문구를
  추가했다. 손상 목록에 없는 제거를 즉사로 판별한다.
- **원인 불명 롤백**: 세션 사용량 한도로 이 세션이 비어 있는 동안 다른 에이전트가 main에
  손패 예약/타임라인 리팩터 커밋 28개(PR #214~#224)를 쌓았으나, 의도와 검증 경위를 알 수
  없어 사용자 요청으로 이 세션의 마지막 안정 지점(v2.2.2, 상점 애니메이션 제거 커밋만
  유지)으로 `main`과 작업 브랜치를 함께 되돌렸다.

### v2.2.2 — 2026-08-08

**상점·제단의 유물과 카드팩에서 반복 유영 애니메이션을 제거했다.**

- 카드가 미세하게 이동·회전하는 무한 애니메이션이 프레임마다 끊겨 보이던 문제를 없앴다.
  최초 등장, hover 확대, 구매·선택·퇴장처럼 상태가 바뀌는 순간의 피드백은 그대로 유지해
  정적인 진열 상태와 의도적인 조작 반응을 분리했다.

### v2.2.1 — 2026-08-08

**v2.2.0 전투 연출을 눌러 산만함을 걷고, 체력 롤링 버그를 고쳤다.**

- **★ 체력 롤링이 한 칸 높은 수에서 시작하던 버그 수정.** 체력 3인 적을 때리면
  `4 → 3 → 2`로 보였다. 롤링 시작값을 **카드에 적힌 글자**에서 읽고 거기에 피해를
  더했는데, 그 시점의 DOM은 아직 다시 그려지지 않아 **맞기 전 값**이었다(3 + 1 = 4).
  이제 모델의 실제 체력(`findCardById`)에서 되짚는다. 실측 확인: `3 → 2`.
- **적이 확 커지던 문제 수정.** `animatePlayerAttack`은 *맞는 적 카드*에 걸리는데,
  v2.2.0에서 그걸 3D 돌진(scale 1.14 · translateZ 170px)으로 바꿔 레일에서 그 칸만
  불쑥 커졌다. 크기가 아니라 **박자**로 때리도록 바꿨다 — 뒤로 길게 물러났다가(후웅)
  짧고 세게 꽂힌다(팍). 확대는 1.035까지만 쓴다.
- **적 슬램도 같은 박자로.** 뒤로 물러나는 구간을 늘리고(0.5까지) 타격을 뒤로 미뤘다
  (0.62 → 0.68). 꽂을 때의 확대는 1.18 → 1.07로 줄이고 무게는 이동·그림자로 낸다.
- **피격 반응에서 붉은 잔광을 걷었다.** 카드 분위기는 그대로 두고 **깎이는 숫자만**
  또렷하게 만든다 — 체력 수치 확대도 1.85 → 1.28로 낮췄다.
- **처치 연출에서 불타오르는 느낌을 걷었다.** 잉걸 스윕과 밝기 플레어를 없애고,
  색이 빠지며 조용히 스러지게 했다(회색으로 질리듯). 처치는 자주 일어나는 일이라
  연출이 요란하면 판 전체가 시끄러워진다.
- **공격력 예고에서 발광을 걷었다.** 1.75배 + 이중 글로우를 1.3배 + 옅은 밝기로 낮췄다.
- **폭탄에 맞을 때 플레이어 카드가 흔들린다.** `animateDamageImpactOnElement`가 공용
  창구라, 적 페이즈를 안 타는 피해(폭탄 등)도 이제 같은 반동을 탄다.

### v2.2.0 — 2026-08-08

**전투 타격감 개편 — 파도 공격 · 깊게 꽂는 슬램 · 재가 되는 사망 · 읽히는 피해 수치.**

- **적 페이즈가 파도가 됐다.** 예전에는 모든 적이 동시에 돌진하고 피해를 합산해 한 번에
  처리했다. 그래서 "3피해 회피"가 떠도 **누구 공격을 피한 건지** 알 수 없었다. 이제 적이
  하나씩 차례로 때리고, 피해·방패·회피가 **그 적이 때리는 순간**에 뜬다. 체력도 한 대에
  한 번씩 나눠 굴린다(`hpBefore`를 넘겨 표시값을 따로 굴린다 — 모델은 이미 최종값이다).
  - 호출부의 합산 피해 표기는 걷어냈다(로그만 남긴다). 겹쳐 띄우면 같은 피해가 두 번
    들어온 것으로 보인다. 선공·레바테인·보스 종복 경로까지 전부 같은 규칙이다.
  - 회피는 촛불빛 `회피!`로 그 자리에 뜬다 — 붉은 피해와 색으로 갈려 "안 들어왔다"가 읽힌다.
- **때리기 전에 예고한다.** 적이 덤비기 직전 공격력 수치가 1.75배로 부풀며 번쩍이고
  (`is-enemy-telegraph`), 카드가 살짝 웅크린다. 무엇이 얼마로 오는지 맞기 전에 읽힌다.
- **슬램 어휘를 통일했다.** 일반 적도 보스처럼 들어올렸다 → 뒤로 당겼다(원근으로 물러남)
  → 앞으로 깊게 꽂는다. 플레이어 타격도 같은 어휘로 바꿨다(예전에는 위로 한 번 뛰었다
  내려오는 pop이라 '부딪혔다'까지만 읽혔다).
- **맞은 적이 아파한다.** 붉은 잔광이 안쪽으로 번지고(`is-enemy-hit::before`), 카드에 적힌
  체력 수치가 1.85배로 부풀며 **한 칸씩 굴러 내려간다**(4→3→2→1→0). 많이 깎여도 늘어지지
  않게 총 길이를 정해 두고 그 안에서 나눠 센다.
- **처치는 재가 되어 흩어진다.** 안으로 오므라들며 뽀용 사라지던 것을 바꿨다 — 마지막으로
  환하게 달아올랐다가, 밑동부터 타 들어가는 잉걸 스윕과 함께 위로 흩날리며 채도를 잃는다.
  양초로 만들어진 것들이라 꺼지는 게 아니라 타서 남는 쪽이 맞다.
- **피해 수치가 더 크고 오래 남는다.** 크기 30→38px(상한 58→74), 발광 세 겹 위에 **뒤로
  지는 그림자** 두 겹을 더해 어떤 일러스트 위에서도 읽힌다. 표시 시간은 980ms → 1500ms이고,
  솟아오른 뒤 **머무는 구간**을 둬 여러 대 맞은 턴에도 하나씩 세어진다.
- 실제 브라우저에서 확인했다: 슬램 복제가 동시에 1개(=순차), 공격력 예고·체력 롤링·
  피해 수치가 모두 발화한다.

### v2.1.1 — 2026-08-07

**첫 판에 잡동사니를 보고 거미줄 설명을 하던 진짜 원인 — 카드 종류 판별 결함.**

앞 버전에서 "강조가 대사보다 오래 산다"를 고쳤지만 그건 곁가지였다. 첫 판에는 거미줄이
**아예 스폰되지 않는데도**(바위·덤불·잡동사니만) 거미줄 대사가 나왔다는 제보로 다시 파서
근본 원인을 찾았다.

- `Card` 생성자는 `trapKind`를 `'web'`으로 **기본값** 설정한다(`options.trapKind ?? 'web'`).
  그런데 소개 종류 판별기가 종류(`type`)를 안 가리고 `trapKind`부터 물어서, 함정이 아닌
  카드가 전부 `trapKind === 'web'`에 걸렸다:
  - 잡동사니·보물상자·꽃·일반 적 → 전부 '거미줄'로 분류
  - `junk` 분기는 그 위에서 가로채여 **도달 자체가 불가능**했다
  - 게다가 첫 소개에서 'web'이 소개 완료로 기록돼, 그 뒤 **어떤 종류도 소개되지 못했다**
    ("설명이 제대로 안 이어진 느낌"의 정체)
- 판별기를 `type`으로 먼저 거르도록 고치고, 순수 함수라 테스트할 수 있게
  `src/systems/BoardIntroKind.ts`로 분리했다. 회귀 테스트 4종을 붙였고, 옛 코드로
  되돌리면 3종이 실패하는 것을 확인했다.
- **에나 관측 쪽은 이상 없음을 전수 확인했다.** `trapKind === 'web'`을 읽는 모든 지점
  (`EnaRuntimeObserver`·`CompanionForesight`·`HandSystem`·학습 시뮬·스프라이트)은 이미
  `type === CardType.TRAP`으로 먼저 거르고 있었다 — 결함은 소개 분류기 한 곳뿐이었다.

> 이전 세이브에는 잘못 소비된 first-seen 기록(`encounter:web` 등)이 남아 있을 수 있다.
> `/리셋`으로 지우면 첫 조우 소개를 처음부터 다시 볼 수 있다.

### v2.1.0 — 2026-08-07

**에나 강조가 대사와 어긋나던 것 수정 · 타깃 점선 제거 · 새싹 상점 절반값.**

- **"잡동사니가 빛나는데 거미줄 설명" 수정.** 강조 맥동은 4.3초(240ms + 1350ms×3) 이어지는데
  대사 최소 노출은 1.5~3초라, 강조를 들고 오지 않는 다음 대사(시작 인사·회상·트리플 소개
  등)가 뜨면 **이전 칸이 계속 빛난 채로 엉뚱한 설명이 붙었다**. 대사와 강조는 짝지어져
  있었고(큐도 정상), 문제는 강조가 자기 대사보다 오래 살아남는 것이었다.
  이제 대사가 바뀌는 순간 강조를 먼저 거두고, 새 대사가 가리킬 것이 있으면 그때 다시 켠다.
- **손패 타겟팅의 금빛 점선 테두리를 걷었다.** 유효 타깃은 발광(맥동 글로우 + 밝기·채도)
  으로만 말한다. 딱딱한 선은 '선택된 UI'로 읽혀 상태 표시와 성격이 어긋나고(CLAUDE.md
  UI 규칙), 카드 종류마다 이미 테두리 색이 달라 칸마다 선이 겹치거나 묻혔다.
  맥동은 원래 `box-shadow` 키프레임이라 선만 지워도 그대로 남는다.
- **새싹 병아리 상점 물가를 절반으로 내렸다**(`SPROUT_SHOP_PRICE_SCALE`). 유물·카드팩
  모두 걸리며, 상점을 여러 번 열어 보라고 만든 자리라 한 번 살 것을 두 번 사게 한다.
  - ★ `enhancements.shopDiscountPct`(직업·만찬 할인)와 **별도 축**으로 뒀다. 직업 선택이
    그 필드를 대입(`=`)으로 덮으므로 거기 얹으면 난이도 보정이 조용히 사라진다 —
    스폰 보정에서 이미 겪은 함정이라 같은 방식으로 갈랐다. 두 축은 곱해져 함께 걸린다.
  - 유물 가격 하한(120)도 같은 배수로 내린다. 안 그러면 새싹에서 싼 유물이 전부 하한에
    붙어 "절반값"이 무의미해진다.
  - 학습 시뮬도 같은 공유 함수를 쓴다(팩·유물) — 한쪽에만 두면 에나가 없는 물가를 배운다.
  - $로 내는 리롤(팩 피커 재뽑기)은 정수 단위라 그대로 뒀다.

### v2.0.8 — 2026-08-07

**새싹 병아리 난이도를 한 단계 더 순하게 — 적·함정 가중치 −5씩.**

- `SPROUT_SPAWN_ADJUST`를 `enemy -7 / trap -7`에서 `enemy -12 / trap -12`로 내렸다
  (보물·꽃 +5는 그대로). 이 상수가 단일 출처라 런타임(`CardSpawner`)과 학습 시뮬이
  함께 따라온다.
- bright 기준 실제 분포 변화:

  | | 적 | 함정 | 보물 | 꽃 |
  |---|---:|---:|---:|---:|
  | 정규 | 44.0% | 25.0% | 22.0% | 9.0% |
  | 새싹(이전) | 38.5% | 18.8% | 28.1% | 14.6% |
  | 새싹(현재) | 37.2% | 15.1% | 31.4% | 16.3% |

  가중치는 총합으로 정규화되므로, 적·함정을 내린 만큼 보물·꽃 비율이 함께 올라간다 —
  보물을 직접 올리지 않아도 상자가 더 자주 나온다.
- `trap` 보정은 `webTrap`에만 붙는다(폭탄·포자는 그대로). bright 17 → 5로 남아
  음수로 내려가지 않는다.

### v2.0.7 — 2026-08-07

**보스 칸 함정 수치가 실제로 물릴 값을 적는다(표기 거짓말 수정).**

전체공격이 함정을 칸마다 물리는 게 위험하지 않냐는 물음에서 출발해 수치를 실측했다.
기본값은 광역 1회 총 함정딜이 "보스에게 한 대 더 맞는 값"(0.33~1.29배) 수준이라
보물을 전부 쓸어 담는 대가로 의도한 선이었다. 대신 **표기 쪽에 진짜 결함**이 있었다.

- 칸에 적히던 함정 수치가 모델 기본값뿐이라, 런 보너스(역경 시련·유물)가 붙으면
  `1`이라 적힌 칸이 3을 물렸다. 필드 함정 카드는 이미 `character.trapDamageBonus`를
  렌더 시점에 더해 최종 수치를 적고 있었는데 보스 칸만 빠져 있었다. 같은 규칙으로 맞췄다
  (모델은 캐릭터를 모르는 순수 상태로 두고 렌더가 더한다).
- 실측 확인: 역경(+2)을 건 뒤 악마를 소환하니 함정 칸이 `1` → `3`으로 바뀐다.
- **밸런스는 그대로 둔다.** 보너스가 칸마다 더해지는 것은 의도다 — `1피해 함정 ×2`에
  역경 +1이면 `2피해 함정 ×2`가 된다. 광역이 칸을 전부 훑으므로 그만큼 곱해 맞는 것도
  포함해서 의도된 맞바꿈이다. 수치가 칸에 그대로 적혀 있으니 계산하고 들어갈 수 있다.

### v2.0.6 — 2026-08-07

**전체공격의 부가물 수확을 확인하고, 유물 타격이 남기던 정산 구멍을 막았다.**

- **확인 결과 광역은 함정·보물을 전부 건드린다** — 성한 칸을 한 번씩 훑으므로 보물을
  싹 쓸어 담는 대신 함정도 칸마다 물린다(30F 기준 함정 2칸 = 피해 2배). 그 맞바꿈이
  "지우고 때릴까, 그냥 쓸어버릴까"를 선택으로 만든다. 깨진 칸은 광역에서도 빠진다.
  이 세 가지를 회귀 테스트로 묶었다 — 광역 진입점이 바뀌어도 규칙이 조용히 무너지지 않게.
- **유물 타격이 부가물을 떨어뜨려도 정산이 한 박자 밀리던 것을 고쳤다.** 직접 타격 beat는
  `resolveCellFixtures()` → `applyPlayerAttackRelics()` 순서라, 공격 시 발동 유물(무작위·
  지정·광역 타격)이 격자를 때리면 그 부가물이 다음 행동까지 정산되지 않았다. 함정 피해와
  보물이 엉뚱한 beat에 도착해 무엇 때문에 아팠는지가 어긋난다. 유물 판정 뒤에 한 번 더
  훑는다 — 떨어진 것이 없으면 즉시 빠져나오므로 공짜다.
  - `retaliateGracefulResponse`는 `takeDamage`를 직접 불러 격자를 지나지 않으므로 무관하다.
- 유물의 무작위 타격이 칸 부가물에 실제로 닿는지도 테스트로 못 박았다(손패만의 규칙이
  아니라는 것 — 유물 경로가 격자를 안 지나면 이 테스트가 0으로 실패한다).

### v2.0.5 — 2026-08-07

**보스 칸 부가물 손질 — 아이콘 표기 · 빗자루 미반영 수정 · 사라지는 시점 정렬.**

- **표기를 아이콘 + 수치로 바꿨다**(`(덫)3` · `(카드)1장`). 뱃지 판을 걷어냈다 — 칸이
  작아 판이 먼저 읽히고 정작 수치가 묻혔다. 아이콘은 `Icons.ts`의 것을 그대로 쓴다.
- **빗자루·성수로 지운 함정이 화면에서 안 사라지던 것을 고쳤다.** 모델은 정상적으로
  지우고 있었지만 `resolveCellFixtures()`가 '지웠다' 경우에만 `render()`를 부르지 않아,
  격자 스냅샷이 화면에 반영되지 않았다(보물·피해 분기에만 render가 있었다). 이제 모든
  경우에 격자를 맞추고 다시 그린다. 회귀 테스트로 빗자루·성수 경로를 함께 묶었다.
- **처리한 칸이 사라지는 시점을 칸 새로고침에 맞췄다.** 예전에는 처리하는 즉시 표기가
  사라져 방금 무엇을 밟았는지·주웠는지가 화면에서 지워졌다. 이제 판정에서는 즉시 빠지되
  (`fixtureSpent`) 표기는 흐려진 채 남았다가 연출이 끝난 뒤 `purgeSpentFixtures()` 한
  창구로 걷힌다. **규칙은 그대로이고 사라지는 시점만 옮긴 것이다** — 처리하지 않은
  부가물은 그대로 남는다.
- **보물 칸의 손패가 얻은 칸 자리에서 나온다.** 보스 타일 중앙에서 나오던 것을 칸 rect
  기준으로 바꿨다(`bossGimmickCellRect`) — 아홉 칸 중 어디서 나왔는지가 남는다. 연출은
  손패 획득 공용 창구(카드 토큰) 그대로다.
- 실제 브라우저에서 악마 보스를 소환해 확인했다: 보물 칸을 때리면 표기가 약 1초 남았다가
  사라지고, 건드리지 않은 함정 2칸은 그대로 남는다.

### v2.0.4 — 2026-08-07

**배경음이 실제로 들린다 — 실제 브라우저로 재현해 원인 두 개를 잡았다.**

v2.0.3까지는 추측으로 고치고 있었다. Chromium을 띄워 Howler 내부 상태를 직접 들여다보니
서로 다른 결함 두 개가 겹쳐 있었고, 둘 다 **에러도 안 나고 화면도 멀쩡한** 종류였다.

- **① 사용자 입력 전에 만든 Howl은 잠긴 오디오 노드를 받는다.** Howler의 html5 오디오
  풀은 자신의 언락 핸들러가 도는 첫 사용자 입력에 채워진다. 그 전에 만들면 빈 풀에서
  "potentially locked audio object"를 받고, 나중에 `play()`를 불러도 조용히 아무 일도
  하지 않는다(측정값: 클릭 5초 뒤에도 `paused: true, currentTime: 0`).
  - v1.1.0에서 넣은 `preload()`가 모듈 로드 시점에 Howl을 만들고 있었다 — 제거했다.
  - 예전 `armAutoplay()`가 **window 캡처**에 걸려 있던 것도 같은 함정이었다. window
    캡처는 Howler가 언락을 건 document 캡처보다 **먼저** 돈다. 버블 단계로 옮기고,
    듣는 이벤트도 Howler와 같은 click/touchend/keydown으로 맞췄다.
- **② 페이드가 playLock에 갇혀 음량이 0에 머문다.** html5 스트림의 `play()`는 비동기라,
  그 직후에 건 `fade()`는 Howler 내부 큐에 들어가 영영 실행되지 않는다. 재생 위치는
  흘러가는데 소리만 안 난다(측정값: `currentTime` 4.95, `volume` 0). 페이드를
  `once('play')` 뒤로 미뤘다.
  - 고친 뒤 측정: 0 → 0.19 → 0.55로 올라 유지된다.
- 회귀 테스트 3종을 `BgmManager.test.ts`에 넣었다(입력 전 Howl 생성 금지 · 버블 단계
  언락 리스너 · 페이드는 재생 시작 후). 각각 고친 줄을 되돌리면 실제로 실패한다.

### v2.0.3 — 2026-08-07

**부팅 게이트 클릭에 배경음이 실제로 켜진다(v1.1.0 회귀 수정).**

v1.1.0에서 시작 지점을 'Click to Start' 클릭으로 옮기면서 재시도 그물을 함께 걷어냈다.
그런데 `playSegment()`는 `play()` 전에 `await this.whenLoaded()`를 지나고 있었다 —
브라우저는 **클릭 핸들러와 같은 동기 실행 안에서 불린 `play()`만** 사용자 입력으로
인정하므로, await를 한 번 지나는 순간 자동재생 정책에 막힌다. 예전에는 리스너가
성공할 때까지 남아 다음 클릭에서 다시 시도했기 때문에 이 결함이 "노래가 애매한 데서
나온다"로만 보였고, 그물을 걷자 그대로 무음이 됐다.

- **이미 로드된 스트림은 `await` 없이 그 자리에서 재생한다.** async 함수는 첫 `await`
  까지 동기로 흐르므로, 기다리지 않으면 `play()`가 클릭과 같은 실행 안에서 불린다.
  게이트가 로딩 중 첫 곡을 미리 받아 두는 것이 이 경로를 위한 준비였다.
- **재시도 그물을 되살렸다.** `beginAudio()`가 `armAutoplay()`를 먼저 깔고 시작한다 —
  성공하면 리스너가 스스로 떨어지고, 기기 사정으로 막히면 다음 입력에서 다시 시도한다.
  그물이 없으면 한 번 막혔을 때 그 판 내내 무음이 된다.
- 회귀 테스트(`BgmManager.test.ts`)가 "미리 받아 둔 곡은 `start()`와 같은 동기 실행에서
  재생된다"를 못 박는다. 고친 줄을 되돌리면 실제로 실패하는 것을 확인했다.

### v2.0.2 — 2026-08-07

**v2.0.1의 배포 분리(test 브랜치 + Vercel)를 되돌렸다.**

되돌린 이유: 작업 방식과 안 맞았다. 임시 브랜치에서 작업해 `main`에 합치는 흐름이라
개발 전용 상설 브랜치(`test`)가 하는 일이 없었고, 브랜치가 하나 늘어난 만큼 "지금
어디서 작업 중인가"를 관리할 거리만 생겼다. 배포처는 Pages 하나로 되돌린다.

- `vercel.json` 삭제, 워크플로를 v2.0.0 상태로 복원(기능 브랜치도 빌드까지 돈다 —
  타입·테스트가 못 잡는 번들 깨짐을 `main` 합류 전에 잡는 자리다).
- `vite.config.ts`의 `base`도 `/Unmelting/` 고정으로 되돌렸다. 다만 **다른 호스트에
  올릴 때 여기부터 갈라야 한다**는 사실은 `CLAUDE.md`에 남겼다 — 루트로 서빙하는
  호스트에 그대로 올리면 에셋이 전부 404가 되고 화면이 하얗게만 뜨는데, 원인이
  화면에 안 보여서 한 번 겪으면 시간을 크게 잃는다.
- 원격 `test` 브랜치는 지웠다(`main`과 같은 커밋만 있어 잃은 것은 없다).

### v2.0.1 — 2026-08-07

**배포(main → Pages)와 개발(test → Vercel)을 갈랐다.**

- `main`에 올린 것만 Actions가 Pages로 배포한다(기존 동작 유지). 개발은 `test`
  브랜치에서 이어 가고, Vercel이 그 브랜치를 받아 빠른 플레이 URL을 준다.
- **에셋 기준 경로를 환경으로 갈랐다.** `base`가 `/Unmelting/`으로 하드코딩돼 있어
  Vercel(루트 서빙)에 그대로 올리면 에셋이 전부 404가 되고 화면이 하얗게만 뜬다.
  `VERCEL` 환경변수를 보고 고르며 `VITE_BASE_PATH`로 덮을 수 있다. 두 경로 모두
  실제로 빌드해 `src=` 출력이 갈리는 것을 확인했다.
- `vercel.json`: 빌드는 `vite build`만 돈다. `npm run build`를 태우면 verify(435 테스트,
  16초짜리 에나 트레이너 포함)가 매 프리뷰마다 돌아 "빨리 켜 본다"는 목적이 사라진다 —
  정합성은 Actions가 본다. Vercel이 `main`을 빌드하지 않도록도 막았다(배포는 Actions 소유).
- Actions는 Pages 번들을 **배포하는 브랜치에서만** 굽는다. 기능 브랜치까지 굽던 것을
  걷어 브랜치 CI가 검사만 하고 끝나게 했다.

### v2.0.0 — 2026-08-06

**보스 칸에 두 번째 축이 생겼다 — 함정/보물 부가물.**

주 버전인 이유: 에나 관측 계약이 `ENA_FEATURE_COUNT` 371 → **373**으로 바뀐다. 저장된
구버전 정책은 `EnaPolicyStore`가 거부하고 동봉 정책으로 폴백한다(재학습 완료본 동봉).

- **부가물 축(`fixture`)을 배율 축(`kind`)과 독립으로 얹었다.** 약점/경화가 피해 배율을
  정한다면, 함정/보물은 **때린 사람에게** 무언가를 한다 — 함정은 피해를 물리고 보물은
  손패를 준다. 같은 칸에 함께 놓이므로 "약점인데 함정 4"가 성립한다: 때리고 싶은 칸이
  곧 아픈 칸이 되는 갈등이 이 기능의 전부다.
  - 배율 축에 감경·증폭 같은 종류가 늘어도 이 축은 그대로다(`BOSS_GIMMICK_FIXTURE_META`
    한 줄 + `.is-fixture-*` CSS 한 규칙).
  - 함정 피해는 보스 공격력의 40%(`bossGimmickTrapDamage`)다. 고정 수치로 두면 30F에서
    아프고 100F에서는 없는 것과 같아진다.
  - 배치는 보스별 프로필의 `fixtures` 표에서 온다(30F 함정 2·보물 1 … 100F 마녀는
    손패를 태우는 보스라 보물 2칸이 유일한 보급선이 된다).
- **지우고 때릴 수 있다.** 칸 함정은 **필드 함정과 같은 것으로 취급**해, 거미줄·폭탄·
  포자를 지우는 손패라면 전부 닿는다(키틴·성수·빗자루·부두 인형·필드 함정 전체 제거).
  열쇠 계열은 보물 부위를 연다. 걷어낸 함정은 아프지 않다 — 그게 지우는 값어치다.
  - 판정은 `bossFixtureMatchesFilter` 순수함수 하나를 HandSystem(실판정)과
    GameBoardRenderer(하이라이트)가 공유한다.
  - 키틴을 들면 보스 타일 전체가 아니라 **함정 칸만** 빛난다. 보스 카드 자체는 키틴의
    대상이 아니지만 칸에 얹힌 함정은 칸 하나짜리 대상이므로, 보스 폭(3칸)이나
    카드 종류(BOSS)로 재지 않고 칸으로 판정한다.
- **함정 처리 신호를 한 곳으로 모았다** — `GameState.onTrapResolved`. 필드 함정 카드와
  보스 칸 함정이 같은 훅을 지나므로 함정 수집 같은 유물이 양쪽에서 똑같이 발동한다.
  밟아서 터뜨렸든 손패로 지웠든 '처리'는 처리다. 함정의 대가(무시 확률)와 역경 시련
  (피해 보너스)도 필드 함정과 같은 순서로 걸린다.
- **재생성은 보스가 때리는 beat에만 돈다**(`replenishFixtures`). 타격마다 채우면 지우는
  의미가 사라지고 보물이 무한 보급이 된다. 굳은 보스는 이 beat를 건너뛰므로 재생성도
  건너뛴다. 배율 리롤은 부가물 자리를 옮기지 않는다 — 같이 굴러다니면 "이 함정을 지우고
  때린다"는 계획이 매 타격마다 무효가 되고 지우는 손패가 도박이 된다.
- **효과 정산은 `resolveBossCellFixtures()` 한 창구**다. 모델(`BossGimmickManager`)은
  "칸에서 무엇이 떨어졌다"까지만 기록하고, 피해·손패 지급은 직접 타격과 손패가 같은
  함수를 쓴다. 피해가 0으로 막힌 beat(리미트 페이지)에도 밟은 함정은 발동한다 —
  안 그러면 함정 칸이 안전지대가 된다.
- **학습 시뮬도 같은 세계를 산다.** 조준 타격은 "그 칸이 부가물 칸일 확률"로 굴리고,
  광역은 남은 부가물을 통째로 건드린다(보물을 쓸어 담는 대신 함정도 전부 밟는다).
  재생성 주기도 실게임과 같다. 남은 함정/보물 칸 수를 관측에 넣었다 — 빼면 에나는
  함정 칸을 없는 셈 치고 "약점을 때려"라고만 말하고, 지울 손패를 아껴 두라는 판단을
  배우지 못한다.
  - `npm run ena:pretrain`으로 373차원 정책과 감사 리포트를 재생성했다.
  - `SIM_FITTED`는 **유지**했다. held-out 300시드에서 스냅샷 19.60 vs 기본값 18.68로
    바뀐 세계에서도 앞선다(절대값이 예전 21.32/19.20보다 낮은 것이 함정 피해가 실제로
    들어간 증거다). 전면 재피팅은 기존 블로커(`predictBaseChance`가 탐색 하한으로
    밀린다)가 그대로라 후보를 뽑아도 채택할 수 없어 돌리지 않았다.
- 보스 체력·목표 행동 수는 건드리지 않았다(`npm run balance:boss` 분포 동일). 이 기능이
  올리는 것은 보스의 맷집이 아니라 **플레이어가 치르는 체력 비용**이다.

### v1.1.0 — 2026-08-06

**메타 화폐($)가 런을 넘어 저축되고, 무역이 그 돈으로 물건을 판다.**

- **메타 지갑(`src/core/MetaWallet.ts`)을 새로 만들었다.** 이전에는 런이 끝나면 남은
  화폐가 그냥 사라졌다 — `coins`는 런 시작마다 0으로 초기화되는 변수뿐이었고 저장
  경로가 아예 없었다. 정산이 통산 기록을 합산하는 **같은 런당 1회 가드 안에서**
  남은 화폐를 지갑에 입금한다(정산이 두 번 열려도 두 번 입금되지 않는다).
  - 저장 키는 `unmelting.meta.currency.v1` — `/리셋`이 접두사로 지운다.
  - 정산 화면에 `저축한 화폐` 줄을 추가했다. 화폐를 못 버는 런(새싹 병아리)에서는
    줄 자체를 만들지 않는다 — 0을 보여 주면 뭔가 잘못된 것처럼 읽힌다.
  - 거점은 런 소지금이 아니라 지갑 잔액을 화폐 패널에 띄운다(같은 패널을 공유하므로
    로비 진입마다 지갑 값으로 덮는다). 런이 시작되면 다시 0에서 출발한다.
- **무역 개방 카드가 상품이 됐다.** 임시 단가는 품목당 3$(`TRADE_ITEM_PRICE`)이고,
  잔액이 되면 클릭 한 번으로 산다. 잔액이 모자란 카드는 채도를 빼고 가격표를 남긴 채
  눌러 두며, 클릭하면 흔들려 거절만 알린다.
  - 개방/해제 토글을 구매로 바꿨다 — 되팔기는 없다. 개방 상태를 오가려면 `/테스트`·
    `/개방` 디버그 경로를 쓴다.
- **단일 조준 손패가 보스 칸으로 곧장 날아간다.** 예전에는 블라스트가 보스 타일에서
  한 번 터진 뒤 다시 칸으로 튀어, 물방울이 튕기는 것처럼 두 사건으로 갈렸다. 겨냥한
  칸이 하나뿐인 beat에서는 타일 블라스트와 출발 버스트를 접고 칸으로 한 발만 보낸다.
  광역은 그대로다 — 거기서는 타일 한 발이 "이 손패가 보스 전체를 덮었다"를 말해 준다.
- **배경음이 부팅 게이트의 `Click to Start` 클릭에서 시작된다.** 이전에는 전역
  pointerdown 리스너가 잡아 로딩 중 아무 클릭에나 음악이 켜졌고, 노래가 시작되는
  지점이 화면 전환과 어긋났다. 이제 게이트 클릭 하나가 오디오 언락·전체화면·화면
  전환을 함께 흡수한다. `?test=1` 부팅은 게이트가 없으므로 예전 방식(첫 입력 대기)을
  폴백으로 남겼다.
- **새싹 병아리 해금팩에서 `동전`이 사라졌다.** 온보딩은 화폐 획득 요소를 전부 잠그는데,
  `동전`은 unique 등급이라 커먼 전용 풀에서 '잠긴 카드'로 분류돼 해금팩 후보로 새어
  나왔다. 먹어도 쓸 데가 없는 값이라 해금팩 후보에서 온보딩 금지 카드를 제외했다.

### v1.0.51 — 2026-08-06

**합체 적의 초반 폭 보너스를 낮추고 보스 구간별 성장표를 추가했다.**

- 일반 2칸/3칸 적의 합체 보너스를 각각 +2/+2, +3/+3에서 +1/+1, +2/+2로 낮춰
  초반에 합체 한 번만으로 전투 난도가 과도하게 뛰지 않게 했다.
- 30/60/90층 이후에는 그 보너스를 구간마다 2배로 올려, 각각 2칸은 +2/+2,
  +4/+4, +8/+8이고 3칸은 +4/+4, +8/+8, +16/+16이 되게 했다.
- 런타임과 에나 학습 시뮬레이션이 같은 보너스 함수를 사용하게 해 실제 플레이와
  학습 환경의 수치가 어긋나지 않게 했다.

### v1.0.50 — 2026-08-06

**자해 카드가 체력을 먼저 대가로 지불하고 공격하도록 박자를 당겼다.**

바늘을 포함한 자해 손패는 카드 비행과 적 타격 연출이 끝난 뒤에야 자해를 처리해,
비동기 피격 연출만 겹쳐도 실제 화면에서는 여전히 "공격 후 반격"처럼 읽혔다.

- 자해 모델 계산·HUD 갱신·피격 연출 시작을 카드 비행 직후로 옮겼다. 이제 카드를 내는
  순간 체력이 줄고, 그 다음 적 피해 수치와 처치 연출이 이어진다.
- 자해 사망·생존 유물·제물 유물 판정도 적 타격 피드백보다 먼저 끝낸다. 대가를 감당하지
  못하면 이미 계산된 공격 결과를 화면에 보상처럼 보여 주지 않고 행동을 종료한다.
- 처치 회복은 기존처럼 적 처치 뒤에만 계산해, `자해 결제 → 적 타격/처치 → 하트 회복`의
  인과 순서를 유지한다.

### v1.0.49 — 2026-08-06

**백작 가위바위보: 덱은 9장 그대로, 던지는 판만 3판.**

직전(v1.0.48)에 덱을 `1/1/1`로 줄였더니 2판이면 남은 패가 확정돼 예측이 너무 쉬워졌다.
조성 공개(9장)는 그대로 두고 **판 수만** 잘라, 덱을 다 보지 못한 채 끝나게 했다 —
카운팅이 확정이 아니라 확률 읽기로 남는다.

- `CountRpsConfig.maxRounds`(선택) 신설. 생략하면 종전대로 덱 소진까지 던진다.
- 진행/종료 판정을 `playFinished()` 한 창구로 모았다(덱 소진 **또는** 판 수 소진).
  아이템 사용 가능·던지기 가능·완승 유물·종료 타이밍이 전부 이 값을 본다.
- 학습 시뮬도 같은 상한을 쓴다(`maxRounds ?? deckSize`).

**바늘 같은 자해 카드의 박자를 고쳤다.**

카드 발동 → 적 피해 → 자해 → 회복이 **전부 순차**라 한 장 쓰는 데 턴이 늘어지고,
자해가 "적을 때린 뒤 따로 한 대 맞는" 별개 사건으로 읽혔다.

- 자해 연출(`animatePlayerDamageImpact`)을 기다리지 않는다. 카드가 나가는 그 박자에
  체력 감소가 겹쳐 보인다 — 모델은 이미 깎였고 사망 판정도 모델을 보므로 연출만
  흘려보내면 된다. 새 이펙트를 만들지 않고 기존 피격 연출을 그대로 쓴다.
- 처치 회복(바늘·모닥불)의 하트가 화면 중앙이 아니라 **잡은 적 자리**에서 떨어진다.
  중앙에서 나오면 "허공에서 체력이 찼다"로 읽힌다.
- 이를 위해 `ResourceTrailSource`에 `{ kind: 'rect' }`를 추가했다. 회복 시점엔 그 적이
  이미 보드에서 지워진 뒤라 카드 id로는 좌표를 찾을 수 없어, 처치 전리품과 같은 방식으로
  **사라지기 전에 rect를 잡아 둔다**.

### v1.0.48 — 2026-08-06

**렌더가 여러 번 도는 구간에서 연출이 끊겨 화면이 버벅이던 문제.**

한 행동에서 render가 몇 번씩 도는 구간(빗자루로 거미줄 여러 장 정리 → 레일 하강 →
리필, 보스 칸 배율 리롤)에서 카드 낙하·격자 연출이 중간에 뚝 끊겼다. 원인은 셋 다
같다 — 연출 클래스를 **1회성**으로만 붙여 두었기 때문이다. 낙하 도중 다른 이유로
render가 한 번 더 돌면 그 클래스가 사라져 애니메이션이 통째로 잘린다.

CLAUDE.md가 이미 정한 규칙(`렌더가 지우면 안 되는 임시 클래스는 만료 시각을 상태로
두고 render가 다시 심는다`)을 세 곳에 마저 적용했다.

- `is-entering`(카드 낙하) — `cardEnterUntil` 만료표. 이전에는 "이번 렌더에 처음 보이는
  카드"에만 붙어, 두 번째 렌더에서 카드가 뚝 제자리로 스냅했다.
- 보스 격자 등장 · 배율 리롤 — `consumeBossGimmick*`가 boolean 1회 소비였다.
  리롤 연출은 칸별 시차 포함 ~0.7초인데 클래스는 렌더 한 번만 살아 있었다.
- 세 경우 모두 클래스가 유지되면 `restoreBoardAnimationPhases`가 신원(`data-card-id` ·
  `data-boss-gimmick-cell`)으로 재생 위치를 인계하므로, 끊기지 않고 **이어진다**.

**칼날 파편에 공격력 축을 붙였다.**

`0공+1` 고정이라 후반에 완전히 죽는 카드였다. `(0.5공+1)` / 트리플 `(1.5공+3)`으로
바꿨다. 같은 값을 쓰는 칼날의 서(단일·트리플 투척)도 함께 맞췄다.

**공격력 연동 유물 두 개에 Shift 수식 전환이 빠져 있었다.**

`벽걸이 횃불`(`(0.5공+1)`)·`뜨거운 돌`(`+(0.25공+1)`)은 효과문에 수식이 **글자 그대로**
적혀 있어 지금 몇이 들어가는지 알 수 없었다. 품격있는 대처·물양동이와 같은
`atkDmgHtml` 경로로 옮겨 합산 수치 + Shift 수식 전환을 쓴다. 효과문에 공격력 수식이
남아 있는 유물은 이 둘뿐이었다(전수 확인).

**백작 가위바위보 판수 9 → 3.**

덱이 `3/3/3`(9판)이라 한 이벤트에서 불빛을 너무 많이 뽑아 경제가 무너졌다.
`1/1/1`로 줄였다 — 짧아진 대신 카운팅이 또렷해진다(2판이면 남은 한 장이 확정된다).
학습 시뮬은 덱 합계에서 판수를 뽑으므로 자동으로 따라온다.

### v1.0.47 — 2026-08-06

**리미트를 여는 조건을 만족시킨 타격만 손해를 보고 있었다.**

`cell-break` 리미트(30F 백작·새싹 고양이의 절반 HP 페이지)가 걸린 동안에는 하한이
피해를 전부 깎아 냈다. 그래서 **부위를 깨서 리미트를 연 바로 그 타격**도 피해가 0으로
버려졌다 — "부쉈는데 아무 일도 없었다"가 되고, 조건을 만족시킨 행동만 손해를 봤다.

- `bossPageFloor()`가 `BossGimmickManager.brokeCellThisAction`을 보고, 이번 행동이 부위를
  깼으면 하한을 푼다. 배율 피해와 부위 파괴 보너스가 그대로 들어간 뒤 페이지가 열린다.
- 다만 하한을 0이 아니라 **1**로 푼다. 0이면 큰 한 방이 2페이지를 통째로 건너뛰고
  격파해 버린다 — 뚫되 끝내지는 않는다.
- `resolveHalfPage()`도 같은 값을 본다. 부위를 깬 행동은 기준선(`halfPageBrokenMark`)을
  세우지 않고 그 자리에서 페이지를 연다 — 첫 도달 타격이 곧 부위를 깬 경우에도
  한 턴 더 기다리지 않는다.
- '이번 행동이 부위를 깼는가'의 단일 출처는 `brokeCellThisAction`이다(`beginAction`이
  기준선을 세운다). 손패·유물·직접 타격이 한 행동 안에서 이어 때려도 경로마다 따로
  세지 않아, 손패 경로(`clampExternalDamageToPageFloor`)도 배선 없이 같이 따라온다.
- 뚫은 순간은 크게 알린다: 로그 + `리미트 돌파!` 강조 플로트. 막혔다는 문구
  (`칸을 파괴해야합니다.`)는 피해가 실제로 0으로 막힌 beat에만 남으므로, 뚫은 타격에는
  자연히 뜨지 않는다.
- 학습 시뮬도 같은 규칙으로 맞췄다(`damageBoss`/`checkBossProgress`). 경계가 삼킨 몫이
  칸 하나를 채우는 타격은 하한에 막히지 않고 HP로 들어가며, 최소 1에서 멈춘다.

**보스 HP를 피해 수치와 같은 박자로 나눠 굴린다.**

부위 파괴 beat는 배율 피해 수치 → 파괴 연출 → 부위 파괴 보너스 수치로 이미 나뉘어
있었지만, HP는 그 전에 한 번에 깎여 있었다. 어느 수치 때문에 얼마나 줄었는지가 안 읽혔다.

- `playBossGimmickStrikes(hits, source, { hpBefore })` — 연출이 수치를 띄울 때마다
  그만큼 `playHudCounterFeedback('boss-hp', ...)`로 굴려 내린다. 모델은 이미 최종값이라
  표시만 따라가는 구조다(광역 손패가 여러 칸을 때려도 칸마다 순서대로 굴린다).
- 직접 타격은 `beforeBossHp`를 그대로 넘기고, 손패 경로는 렌더가 저장해 둔
  `bossHpCurrent + 관측된 손실`로 되짚는다.

### v1.0.46 — 2026-08-06

**에나가 때리지도 않은 적에게 "못 막아줘서 미안"이라고 사과하던 문제.**

강적 미숙 대사(miss-shield)가 두 갈래로 오발동했다.

- **굳은 적에게 사과했다.** `strongEnemyIncoming`이 `!!strongEnemy`라 굳음 여부를 아예
  보지 않았다. 굳은 적은 `TurnManager.runEnemyPhase`가 통째로 건너뛰어 그동안 아무도
  맞지 않는데도 "막아야 하는데 못 막아"가 나갔다. 공격이 닿기까지 남은 턴을
  `strongEnemyAttackInTurns()` 한 곳에서 뽑아(전방 비굳음 0 · 전방 굳음 = 남은 굳음 턴 ·
  대기 행 1), **다음 턴 안에 실제로 때릴 수 있는 강적만** 위협으로 센다. 방어 대비 점수가
  이미 쓰던 값이라 새 규칙이 아니라 같은 기준을 플래그에도 적용한 것이다.
- **아직 맞지도 않았는데 사과했다.** 게이트가 '최근 2턴 내 피격 **또는 처치**'였다.
  처치를 근거로 삼으면 적 하나 잡은 직후, 아직 때린 적 없는 대기 행 강적(괴물꽃 등)에
  대고 사과가 나간다. 이 대사는 **막아주지 못해서** 미안하다는 말이라 방패가 필요한
  상황 = 피해가 실제로 들어오는 상황에서만 성립하므로 **피격만** 남겼다.

**양초 백작의 탐욕 살포에서 둥근 금화를 걷어냈다.**

`spawnGreedCoin`이 `border-radius: 50%` 금화를 화면에 흩뿌렸다. 이 게임의 이펙트 어휘는
사각 블라스트 하나뿐이라 그 동전만 혼자 둥글게 떠 있었다. 손패 획득 공용 창구
(`animateResourceTrailFromCard` → `'hand'` 카드 토큰)로 바꿨다 — 직전에 양초 고양이를
같은 창구로 옮겼으므로(v1.0.45) 이제 보스 손패 지급이 전부 한 경로다.
쓰는 곳이 없어진 `animateBossScatterToHandSlots`·`spawnGreedCoin`·`.boss-greed-coin`
CSS·상수 2개를 함께 지웠다(퍼사드 위임 포함).

### v1.0.45 — 2026-08-05

**보스전에서 사각 블라스트 어휘를 벗어난 둥근 이펙트 두 곳을 정리했다.**

**부위 파괴(칸 깨짐)** — 조각이 튀기 직전 칸을 부풀리던 예비 동작이
`radial-gradient` 발광 판(`.boss-cell-shatter-pane`)이라 혼자 둥글게 읽혔다.
이 판(과 body 오버레이 `.boss-cell-shatter`, `@keyframes boss-cell-burst-pane`)을
지우고, 같은 자리에 좁은 스프레드(spread 20)의 사각 블라스트를 짧게 터뜨리는
것으로 바꿨다 — 이어지는 파편 블라스트 두 겹과 같은 어휘다.

**새싹 30F 양초 고양이의 손패 지급** — `giftHandCards()`가 30F 양초 백작의
탐욕의 동전 살포용 연출(`animateBossScatterToHandSlots` → 둥근 금화
`spawnGreedCoin`)을 그대로 재사용하고 있었다. 백작에게는 "탐욕의 동전"이라는
실제 동전 카드가 있어 금화 모양이 맞지만, 고양이가 주는 건 일반 손패라 카드
모양이어야 한다. 이미 있는 손패 획득 창구(`animateResourceTrailFromCard(...,
'hand', ...)` → `animateHandCardTokens`, 세로로 긴 사각 카드 토큰)로 바꿨다 —
보스 피해 보상 지급(`BossEvent.ts:1126`)이 이미 같은 패턴을 쓴다.
백작의 동전 살포(`animateBossScatterToHandSlots`)는 그대로 남는다 — 그건 실제로
동전이 맞다.

### v1.0.44 — 2026-08-05

**덤불 피해를 잡동사니(보물)와 같은 패턴으로 바꿨다 — 카드에 값을 굽지 않고 밟는 순간 굴린다.**

이전(v1.0.37)엔 개체마다 `Card.bushDamageRoll`을 스폰 시 한 번 굴려 들고 다녔다.
표기·실제 피해가 갈리지 않게 하려는 선택이었지만, 사실상 "결과가 이미 정해진 채
숨겨져 있다"는 점에서 잡동사니(보물 손패 개수를 여는 순간 굴리는 방식)와 결이 달랐다.

- `Card.bushDamageRoll` 필드를 없앴다. `getTrapDamagePenalty()`가 덤불 1·2칸에서
  매 호출마다 `Math.random()`으로 새로 굴린다(3칸 5는 그대로 고정).
- 카드 얼굴 표기가 이 값을 직접 부르면(매 렌더) 화면에서 수치가 깜빡인다. 그래서
  `GameBoardRenderer`의 함정 스탯 배지에 덤불 전용 분기를 추가해, 잡동사니의
  `JUNK_RANGES`와 같은 패턴으로 **범위 라벨**(`0~1` / `2~3` / `5`)을 정적으로 보여준다 —
  실제 굴림은 `ActionSystem.evadeTrap`이 부르는 순간(밟는 순간) 단 한 번만 일어난다.
- 합체 시 설명문(`Deals N damage brush`)도 확정 수치 대신 범위 텍스트로 바꿨다 —
  한 번 굴려 굳히면 그 뒤로 실제 피해와 갈린다.
- 학습 시뮬(`EnaTrainingSimulation`)의 `trapDamage()`도 같은 패턴으로 맞췄다. 재현
  가능한 셀프플레이를 위해 `Math.random` 대신 `EnaRandom`을 넘기도록 시그니처를
  바꿨고, 호출부 8곳(실제 밟기·신사모 지연·함정 제거·위협 관측·정책 판단)을 전부
  갱신했다. `encodeCard`(신경망 관측 인코딩)에도 같은 함수가 쓰이므로, 덤불의 정확한
  피해를 미리 알 수 없다는 정보 상태가 에나 관측에도 그대로 반영된다 — 카드에 값을
  구워 두던 예전 방식은 오히려 실제 게임보다 더 많은 정보를 에나에게 주고 있었다.

### v1.0.43 — 2026-08-05

**보물/방패/회복 축에 실제로 반응하는 유물 5개에 태그가 안 달려 있었다.**

`곡괭이`·`개봉식`·`황금 열쇠`는 전부 보물 스폰/생존/상위 전환을 조작하는데
`treasure` 태그(라벨 '보물')가 0개였다. `귀족의 품격`은 보물 획득 시 방패를 주는데
`shield` 태그가 없었고(당시 방패 축 유물은 가시 방패 1개뿐이었다), `헌혈팩`은 회복량을
피해로 되돌리는 관계상 `넘치는 촛농`·`붉은 포션`과 이미 3중 연계라고 주석에 적혀
있었는데 `heal` 태그가 없었다. 앞선 변칙·악마 인형(v1.0.42)과 같은 종류의 등록 누락.

- 유물 태그는 화면에 노출되지 않는다(손패만 `CardFaceRenderer.handCardTagLabels`로
  표기) — `CompanionForesight.ownedRelicTags` → `HandCardAdvisor` 지원 점수,
  `EnaKnowledgeAdapter` 유물 시너지 겹침 판단에만 쓰이는 에나 내부 참고용이다.
- `pickaxe`·`opening-ceremony`·`golden-key` → `treasure`. `wax-crow` → `shield`.
  `blood-pack` → `heal`.

### v1.0.42 — 2026-08-05

**변칙·악마 인형에 sacrifice 태그가 빠져 있었다.**

CLAUDE.md는 두 유물을 각각 넓은 폭(`체력 N 잃을 때마다`)·좁은 폭(`자해 N마다`) 제물
축 유물로 명시하는데, 정작 `synergyTags`가 비어 있었다. 문서·주석상의 의도와 실제
데이터가 갈린 상태였다.

- 이건 표기 문제가 아니라 실제 판단에 구멍이었다: `HandCardAdvisor.ts:580`이
  `ownedRelicTags`(보유 유물 태그)와 손패 태그의 겹침으로 에나의 지원 점수를
  올린다(`+1.2/겹침`). 두 유물을 들고 있어도 에나는 사실상 인식하지 못했다 —
  플레이어가 제물 축에 투자했는데 바늘·부두 인형·단두대를 더 추천받지 못했다.
- `anomaly`(변칙)·`demon-doll`(악마 인형)에 `synergyTags: ['sacrifice']`를 채웠다.
  Tags.test.ts의 유물 태그 등록 검사만 통과하면 되는 안전한 수정이다(유물은 손패와
  달리 태그당 최소 5장 계약이 없다).

### v1.0.41 — 2026-08-05

**박쥐~전갈(9~14번)을 키틴/거미·생쥐/개구리·벌/사마귀와 같은 짝 형식으로 다시 짰다.**

도마뱀/너구리는 체력만 다르고 축이 같은 방향이라("그냥 더 큰 놈") 짝의 정체가 없이
따로 노는 느낌이었고, 박쥐/고슴도치·풍뎅이/전갈은 이전 세션에서 축을 반전했지만
당시 제안한 폭(예: 5/5 대 10/2)이 너무 커서 다른 짝들과 언어가 달랐다.

- 세 짝 모두 **합계는 같고 축만 반전**하는 근소한 차이로 다시 잡았다(델타 1, 기존
  키틴/거미·벌/사마귀와 같은 형식):
  - 박쥐(급습형) 7/4 · 고슴도치(방벽형) 9/2 — 합11
  - 도마뱀(급습형) 8/5 · 너구리(방벽형) 10/3 — 합13
  - 풍뎅이(방벽형, 설명문 'Armored'와 일치) 12/4 · 전갈(급습형, 설명문 'Stinging'과 일치) 10/6 — 합16
- 짝마다 합계를 11→13→16으로 한 단계씩 올려, 뒤로 갈수록 두 짝 모두 전체적으로
  세지는 것을 유지했다("강함이 높아질수록 더 까다롭고 강하게").
- 방벽형(고슴도치·너구리·풍뎅이)은 공격력이 그 층 플레이어 공격력을 넘지 못해
  플레이 후반까지도 계속 2타가 필요하다 — 다른 적들은 대부분 1타로 트리비얼해지는
  구간에서 이 셋만 "여전히 귀찮은 벽"으로 남는다.
- 밴드가 겹치는 구간(예: 60~69턴에 박쥐·고슴도치·풍뎅이·전갈이 동시 등장)을
  전수 확인해 처치 타수·비용이 겹치는 쌍이 하나도 남지 않았다.

### v1.0.40 — 2026-08-05

**층이 깊어질수록 그 층 풀 안에서 강한 적 쪽으로 스폰이 쏠린다.**

`getActiveEnemyDefinitions()`가 반환하는 풀(2~6종)에서 지금까지는 균등 추첨이었다 —
30층을 넘겨 벌·사마귀가 섞여도 여전히 생쥐·개구리가 똑같은 확률로 나왔다. 후반부로
갈수록 이전 층 잔당이 상대적으로 약해지는 문제는 초반/중반/후반 어디서나 반복되는데,
그때마다 스탯을 올려 잡는 대신 **풀 안의 비중 자체를 조종**하기로 했다.

- `pickWeightedEnemyDefinition(pool, turn, rng)` — 풀의 `enemyPower` 최솟값~최댓값을
  0~1로 정규화한 뒤, `(0.3 + rank) ^ exponent`로 가중치를 매긴다. `exponent`는
  `enemyPowerBiasExponent(turn)`이 턴 1의 0(균등)에서 시작해 턴당 0.025씩 올라
  2에서 상한이 걸린다(턴 81 근방부터 고정).
- 약한 쪽도 0.3 바닥값 덕분에 가중치가 0으로 죽지 않는다 — 몬테카를로 검증 결과 턴
  99에서도 그 풀의 최약체가 2~3% 확률로는 여전히 나온다(완전히 사라지지 않는다).
- 강한 쪽 비중은 턴 25에서 ~20%대, 턴 55~65에서 ~30%대까지 자연스럽게 올라간다 —
  "20층 넘으면 키틴벌레가 덜 나온다"는 체감이 스탯 변경 없이 스폰 비중만으로 생긴다.
- 적 처치 불빛은 `enemyPower` 랭크에 정비례하므로(`ENEMY_LIGHT_PER_RANK`), 강한 적
  비중이 늘면 불빛 수급도 같은 층 안에서 자연히 오른다 — 별도 배선 불필요.
- 런타임(`CardSpawner.generateEnemy`)과 학습 시뮬(`EnaTrainingSimulation.spawnEnemy`)이
  이 함수 하나를 공유한다 — 한쪽만 편향을 걸면 에나가 실제와 다른 스폰 분포를 학습한다.

### v1.0.39 — 2026-08-05

**양초 벌·사마귀가 같은 적이었다 — 축을 갈랐다.**

둘 다 5/3이라 이름과 그림만 다르고 플레이 차이가 0이었다. 다음 티어를 열었는데
체감이 오지 않는 구간이었다.

- **벌 5/3 → 5/4.** 이 구간 최고 딜러. 공격 4는 위 두 티어(박쥐·고슴도치 3)보다
  높은데, 그게 곧 정체라 의도된 예외로 둔다.
- **사마귀 5/3 → 6/3.** 체력 계단의 한 칸(두더지 5 → 사마귀 6 → 박쥐 7 → 고슴도치 8).

체력을 낮춰 유리대포로 만드는 안(4/4)은 접었다. 적은 반격하지 않고 자기 턴에만
때리므로(`TurnManager.runEnemyPhase`) **처치 비용 = (필요 타수 − 1) × 공격력**이고,
4체력은 공격력 4 이상에서 항상 1타라 반격을 한 번도 못 한다 — 유리대포가 탱커보다
상대하기 안전해져 의도가 뒤집힌다. 체력을 5로 두면 공격력 4 구간에서 2타가 되어
받는 피해 4(사마귀 3)로 순서가 바로 선다.

### v1.0.38 — 2026-08-05

**칼날 파편을 기본 드로우 풀로 — 칼날 축이 유물 없이도 시작된다.**

파편은 `dropSource: 'relic'`이라 숫돌을 먼저 뽑아야만 손에 들어왔다. 제물 축의 씨앗인
바늘이 기본 풀에 있는 것과 어긋나 있었고, 그 탓에 검집·칼날의 서·연마가 뽑힐 이유가
런 초반에 존재하지 않았다.

- `dropWeight: 0 → 6` · `dropSource: 'relic' → 'any'`. 바늘(9)보다 낮게 둬 축은
  시작되되 풀을 크게 묽히지 않는 자리에 놓았다.
- `TagReactions.test.ts`의 파편 계약에서 `dropSource === 'relic'` 검사를 뺐다. 생성기
  유물과 **태그를 공유한다**는 것이 계약이고, 드로우 풀 노출은 카드마다 다른 선택이다.
- 부수 효과(의도한 것): 파편은 화력이 거의 없어(0공+1) 보스전 드로우 풀의 기대 피해가
  내려가고, 역산 체력이 그만큼 따라 내려간다 — 백작 160→150 · 기사단장 450→440 ·
  조각사 610→600 · 마녀 1190→1180. 전투 길이(진짜 불변식)는 그대로다.
- 온보딩 고양이 `targetActions` 11 → 10. "온보딩이 30F보다 짧다"는 불변식이 체력
  반올림(둘 다 10 단위)에 기대 겨우 서 있어서, 30F만 한 칸 내려가자 순서가 뒤집혔다.
  이제 실제 행동 수로도 확실히 짧다(약 10.0 대 10.7). 체력 120 → 110.

**덤불 0 피해가 시련·유물 상승을 삼키지 않는지 확인.**

- 함정 피해 보너스(시련 '역경'·유물)는 `character.trapDamageBonus`로 호출부에서
  **더해지므로** 밑값이 0이어도 그대로 얹힌다(0+2=2). 회귀 테스트로 고정했다.
- 피해가 0일 때 로그가 `덤불을(를) 밟았다 (-0)`으로 찍히던 것을 `(피해 없음)`으로 바꿨다.
  `-0`은 고장으로 읽힌다.
- 적 스탯 시련(`applyTrialEnemyStatBonus`)·적 HP 유물은 바위에도 같은 식으로 붙지만,
  실제로는 만나지 않는다 — 온보딩 필드는 1~10턴에만 스폰되고 만료가 3턴이라 30F
  시련·유물이 붙는 시점까지 남지 않는다.

### v1.0.37 — 2026-08-05

**초반 레벨 디자인 — 바위와 덤불이 "합쳐지면 아프다"를 먼저 가르친다.**

바위가 반격 0이라 온보딩 판에서는 맞아 보지 않고 10층을 지날 수 있었다. 최소한의
피격을 먼저 겪게 하고, 그 위에 초반 4종의 스탯 모양을 다시 깔았다.

- **바위 1/0 → 1/1.** 합체는 기존처럼 선형이라 2칸 2/2 · 3칸 3/3이 된다(일반 적 합체
  보너스 +2/+3은 여전히 미적용). 감당 가능한 폭으로 "합칠수록 아프다"를 보여 준다.
- **덤불 피해 1/2/3 → 1칸 0~1 · 2칸 2~3 · 3칸 5.** 1칸은 그냥 지나갈 수도 있는 소프트
  함정으로 두고, 2칸부터 값을 뛰게 해 합체의 대가를 체감시킨다.
  범위는 개체마다 한 번만 굴린 `Card.bushDamageRoll`(0/1)로 만든다 — 호출마다 굴리면
  카드에 표기된 수치와 실제 피해가 갈린다(표기·`ActionSystem`·에나 관측이 같은 함수를 읽는다).
- **초반 4종 재배치**: 키틴벌레 1/1 → **1/2**(유리대포) · 거미 1/1 → **2/1**(맷집) ·
  생쥐 2/1 → **3/2** · 개구리 1/2 → **2/3**. 새(3/3)는 그대로다. 체력과 공격력을 서로
  다른 축으로 올려 "무엇이 아픈 적인지"를 스탯 모양만으로 읽히게 했다.
- 학습 시뮬도 같은 값을 쓴다(`spawnFieldCard` 바위 1/1 · `trapDamage` 덤불 식 ·
  `EnaSimCard.bushDamageRoll`). 적 스탯은 `ENEMY_DEFINITIONS`를 직접 import 하므로 자동 반영된다.

### v1.0.36 — 2026-08-04

**패배 정산 팁이 마지막 낱말만 다음 줄로 떨어지던 문제.**

`Melting` 타이틀 아래 조언 줄(`.death-tip`)이 카드 폭(772px)의 절반인 `max-width: 30em`
(390px)에 묶여 있었다. 기본 사망 문구 "체력이 0이 되면 끝. … 강적은 합체 전에 끊자."의
한 줄 필요 폭이 **정확히 390px**라 아슬아슬하게 넘쳐, "끊자."만 두 번째 줄로 떨어졌다.

- `max-width: min(46em, 100%)` — 카드 안에서 넉넉히 잡아 한 줄로 앉힌다(카드보다 넓어지지
  않게 `100%`로 묶는다).
- `word-break: keep-all` — 좁은 화면에서 접힐 때 한국어 낱말 중간이 끊기지 않는다.
- `text-wrap: balance` — 접힐 수밖에 없을 때도 두 줄을 고르게 나눠 고아 줄을 막는다.

실측(같은 마크업 주입, 1920/1440/1280/900/700px): 두 사망 팁 모두 **전 폭에서 1줄**
(이전 기본 팁 2줄). 카드 폭 772px, 팁 필요 폭 384~390px.

### v1.0.35 — 2026-08-04

**칼날 파편 트리플에서 '강타' 표기 제거 · 과묵 변형(short)의 어색한 문장 교정.**

- `강타`는 이 게임에 없는 용어라 뜻을 더하지 않았다. 카드 표기와 체인 로그 라벨에서 뺐다
  (`필드 랜덤 적 1장 3피해`, 로그 `칼날 파편`).
- 전 대사 1,754줄을 말수 3단계 × 말투 3단계로 전수 렌더해 훑었다. 말수 도입으로 실제로
  어색해진 것은 **`short` 변형이 만드는 "명사구 + 마침표"** 뿐이었다
  ("앞길. 빗자루를 받아줘." / "미리 하는 청소. 이걸 받아줘."). 줄일 때는 **쉼표로 잇도록**
  고치고("거미줄, 빗자루로 쓸어두자."), 그 규칙을 `LineTemplate.short` 주석에 못 박았다.
  런타임에서 덮이는 `{이유}` 슬롯은 명사구가 들어오므로 특히 이 함정에 잘 걸린다.
- 함께 확인하고 **문제 없음**으로 남긴 것: terse에서 종결이 마침표로 고정되며 어감이 죽는
  촉구형 줄(0건) · terse가 plain보다 길어지는 줄(0건) · 잔여 토큰/이중 공백/종결/56자 초과(0건).
  `...`(마침표 3개)는 파일 헤더가 `…`와 함께 허용한 표기라 그대로 둔다.

### v1.0.34 — 2026-08-04

**에나가 과묵해지면 말도 짧아진다 — 말수(Verbosity) 축 신설.**

지금까지 스킵/열람 학습은 **빈도**만 움직였다(`minTurnGap`). 같은 수치(`chattiness`)에서
**길이 축**을 하나 더 뽑아, 과묵해진 에나는 덜 말하면서 말할 때도 핵심만 짚게 했다.
두 축을 따로 두지 않은 이유는 "가끔 말하는데 길다"가 더 성가시기 때문이다.

- `Verbosity = 'terse' | 'plain' | 'talkative'` — `chattiness` 0.75 미만/1.3 초과가 경계.
- **문장을 자르지 않는다.** ① 꾸밈 토큰(강조·재촉)을 비우고 ② 같은 풀에서 더 짧은 줄을 고르는
  **길이 편향 선택**으로 줄인다. 절삭은 뜻을 부수지만 선택은 안 부순다 — 게다가 풀이 다양할수록
  잘 듣는 방식이라 대사를 늘리는 일이 그대로 이 기능의 품질이 된다.
- `LineTemplate.short` — 자주 나오는 줄에 **핵심만 남긴 변형**을 데이터로 선언할 수 있다(선택).
  예측 지원 5줄에 먼저 붙였다. 선언이 없으면 길이 편향이 대신하므로 전 대사 의무가 아니다.
  `short`도 `enumerateLineRenders`를 지나 같은 품질 검사(잔여 토큰·종결·56자)를 받는다.
- 위급(`urgent`)은 말수의 영향을 받지 않는다 — 과묵해졌다고 위험 경고까지 밋밋해지면 안 된다.
- `LineTemplate.slots`를 optional로 바꿔 `{ template, short }`만으로도 줄을 쓸 수 있게 했다.
- 말수는 `EnaLearningSnapshot`에 싣지 않는다 — `chattiness`에서 그대로 파생되는 값이라
  스냅샷에 두면 같은 사실이 두 곳에 적힌다. 경험 탭은 이미 수다 수치를 보여 준다.

실측(런타임 경로, 상황 반응 200여 회):
기본 20.9자 → **스킵 누적 후 16.0자(−23%, 최대 38→29)** → 열람 누적 후 25.9자.
예: "거미줄이 모이고 있어. 빗자루로 쓸어두자." → 과묵: "거미줄. 미리 준비해."

### v1.0.33 — 2026-08-04

**제물 축 sink 좁히기(v1.0.31)를 되돌린다 — 두 폭은 의도된 설계였다.**

주사기·피의 대가를 자해 전용으로 좁히고 문턱을 40 → 20으로 내렸던 것을 원래대로
돌린다(`체력 5 / 40 잃을 때마다`). 제물 축 유물은 **문구가 곧 계약**이고, 두 폭이
의도적으로 공존한다:

- `자해 N마다`(혈서·응고·수혈·악마 인형) = **좁은 축**. 자해만 센다.
- `체력 N 잃을 때마다`(주사기·피의 대가·변칙) = **넓은 축**. 자해 + 받는 피해를 모두 센다.

넓은 유물과 좁은 유물이 서로를 보완하는 구조라, 한쪽으로 통일하면 축이 좁아지는 게
아니라 절반이 죽는다. v1.0.31에서 새로 넣었던 `pendingSelfHarm`은 쓰이지 않게 되어
제거하고 `pendingHpLoss`를 되살렸다. (v1.0.31의 방패 흡수 연출은 그대로 남는다.)

### v1.0.32 — 2026-08-04

**자원 아이콘 치환을 제자리 방식으로 · 세 화면 설명문 통일 · 공격 어휘는 글자로.**

앞머리로 빼던 방식(`resourceLeadTextHtml`)은 구분자(`·`)가 있으면 통째로 건너뛰어,
양초("방패 +1")는 아이콘이 붙고 족쇄("방패 +4 · 즉시 1턴 흐름")는 안 붙는 들쭉날쭉이
생겼다. 규칙을 하나로 바꿨다 — **낱말 바로 뒤가 수치면 그 낱말을 제자리에서 아이콘으로
바꾼다**(`resourceIconTextHtml`). 어순을 건드리지 않아 어떤 문장에도 안전하고, `·`가
섞인 여러 효과 문장도 전부 걸린다.

- **공격력·피해는 치환하지 않는다.** 손패 피해 수식이 이미 검 아이콘을 쓰고 있어
  같은 아이콘이 "공격력 스탯"과 "가하는 피해" 두 뜻을 오갔다. "피해"라는 글자가 더 직관적이다.
- **도감·손패 미리보기·상점 팩 카드가 같은 글을 쓴다.** 도감만 HTML 태그를 전부 지워
  아이콘·수식 전환이 사라졌던 것을 고쳐, 세 화면이 `enhancedHandCardDescription` +
  `resourceIconTextHtml` 같은 두 함수를 지난다. 실측: 해금팩 "전방 선택 적 1장 1~4피해"
  = 도감 동일.

### v1.0.31 — 2026-08-04

**방패로 막은 피격이 화면에서 사라지던 문제 · 자해와 피격 키워드 분리 · 표기 창구 정리.**

- **방패 흡수 피격**: 방패가 다 막으면 수치만 조용히 줄어 아무 일도 없던 것처럼 보였다.
  같은 자리에 같은 양식으로 **회색 `-N`** 을 띄우고(피가 아니라 금속이 막은 것이므로
  은백), 대상은 **버텨 낸 흔들림**으로 반응한다(`playShieldBlockFeedback` — 짧고 단단한
  좌우 진동 + 은빛 잔광). 플레이어·보스 양쪽 같은 창구를 쓴다. 방패 흡수량은 반환값
  (실 HP 피해)에 안 남으므로 `Character.lastBlockedByShield` / `EnemyHit.blocked`로 넘긴다.
- **자해 ≠ 피해를 받을 때**: 제물 축 유물(주사기·피의 대가)이 적/함정에게 맞은 피해로도
  쌓여, 제물 카드를 한 장도 안 써도 축이 굴러갔다. 자해 전용 누적(`pendingSelfHarm`)을
  새로 두고 `takeDirectDamage`에서만 쌓는다. 효과 문구도 "체력 N 잃을 때마다" →
  "자해 N 누적마다"로 통일했다. **변칙은 그대로 '피해를 받을 때'로 남긴다** — 문구가
  자해 키워드가 아니고, 옮기면 피격 보상 축이 통째로 사라진다.
  피의 대가 문턱은 **40 → 20**: 받는 피해가 빠진 만큼 도달량이 절반 이하로 줄어, 문턱을
  안 내리면 런당 1~2회 발동하는 죽은 유니크가 된다.
  누구도 읽지 않게 된 `pendingHpLoss`는 제거했다.
- **Shift 토글 팬텀 여백**: `desc-dyn`이 두 겹을 `inline-grid`로 **겹쳐** 둬서 칸 너비가
  항상 넓은 쪽(nowrap 수식)으로 잡혔다. Shift를 누르지 않아도 그만큼 여백이 남아 뒤
  텍스트가 밀렸다(유리 파편 등 전용 override가 없던 레시피 자리에서 드러남). 기본을
  **표시 토글**로 바꾸고 자리별 중복 override 5곳을 지워 규칙 하나로 모았다. 실측 폭
  29px = `__s` 29px(이전엔 수식 폭까지 차지).
- **해금팩 카드 문구가 실제 설명문과 달랐다**: `resourceTextHtml`이 문장 **어디서든**
  자원 낱말을 찾아 문장 맨 앞에 아이콘을 세우고 그 낱말을 지웠다. 손거울
  "전방 선택 적 1장: 그 적의 공격력만큼 피해" → "(검) 전방 선택 적 1장: 그 적의 만큼 피해".
  가드가 있는 `resourceLeadTextHtml`(자원 어휘로 **시작**하고 구분자·HTML이 없을 때만)로
  통일하고 무가드 버전은 제거했다 — 표기는 데이터의 description을 그대로 따라간다.
- **손패 시너지 태그가 화면에 없었다**: `handCardTagLabels`가 카테고리+직업만 내보내
  불씨·양초·제물 같은 축이 카드에 안 보였다. 이 함수가 도감·미리보기·상점 팩 카드의
  **단일 창구**이므로 여기서 시너지 태그를 더했다(라벨은 `Tags.ts` 그대로). 순서는
  분류 → 시너지 → 직업. 실측: 불씨 `[공격·불씨]`, 의식 양초 `[도구·양초·제물·수집]`.
- **자원 토큰 속도 차등**: 전부 같은 속도로 꽂혀 한 턴에 여러 자원이 들어오면 어디를
  볼지 뭉갰다. 흔한 것부터 빨리 치운다 — 불빛·화폐 460ms · 콤보 게이지 600ms ·
  빛 게이지 700ms · 나머지(체력·방패·공격력·유물) 820ms. 발사 간격도 비행 속도를 따라간다.

### v1.0.30 — 2026-08-04

**별빛 반짝 글리프를 백금색으로 · 손패 `청소` → `빗자루` 개명.**

별빛 토큰의 글리프까지 금빛으로 물들여 놓으니 일반 불빛 반짝과 구분이 되지 않았다.
**글리프 자체는 백금(차가운 은백)**으로 두고, 황금빛·푸른빛은 그 둘레 발광에만 남긴다 —
가르는 것은 형태도 아니고 글리프 색도 아닌 **후광의 색**이다. 글리프에 바로 붙는 첫
발광 겹도 은백으로 바꿨다(여기부터 금빛이면 글리프가 금색으로 물들어 보인다).
이 배색은 **별빛 토큰 한정**이다 — 다른 자원 토큰은 각자 HUD 색을 그대로 쓴다
(불빛=호박, 체력=붉은빛).

손패 `청소`를 `빗자루`로 개명했다. 카드 일러스트가 이미 빗자루라 이름만 어긋나 있었다.
이름의 단일 출처는 `HandCards.ts`이므로 도감·손패·미리보기·상점은 자동으로 따라오고,
**이름으로 부르던 곳을 함께 고쳤다**: 에나 대사(손패 평·사용 대사·예측 지원 대사·
첫 조우 소개), 엔진 주석(`HandSystem`·`index`), **RL 시뮬 주석**(`EnaTrainingSimulation`),
기획서. 역할 축을 뜻하는 '청소'(빗자루·성수·키틴이 함께 속한 cleanup 축)는 그대로 둔다 —
그건 카드 이름이 아니라 분류다.

### v1.0.29 — 2026-08-04

**자원 획득을 '허공에서 흘러드는 파편'에서 '출처에서 떨어지는 아이콘'으로.**

회복을 얻어도 붉은 파편이 화면 어딘가에서 체력 게이지로 흘러들 뿐이라, 무엇을 어디서
얻었는지가 남지 않았다. 손패 획득 토큰과 같은 세 박자를 모든 자원에 준다 —
출처(카드·잡은 적·꽃)에서 **아이콘이 곡사로 튀어나와 떨어지고** → 잠깐 머물고 →
HUD로 빨려 들며 블라스트한다(`animateResourceIconTokens`).

- 토큰 글리프는 전용 도형을 새로 그리지 않고 **HUD가 이미 쓰는 `Icons.ts` 아이콘**을
  그대로 띄운다: 체력=하트 · 방패=방패 · 빛 게이지=불꽃 · 콤보=게이지 · 공격=검 ·
  유물=책 · 불빛=반짝. 떨어진 것과 채워지는 곳이 같은 그림이라야 눈이 이어 붙인다.
- 판(배경)은 없다 — 글리프 자체가 발광한다. 사각 조각 위에 아이콘을 얹으면 '조각'이
  먼저 읽혀 무엇을 얻었는지가 도로 묻힌다.
- 수가 많아도 토큰은 3개까지만 낸다. 토큰은 '무엇이 왔는지'를 말하는 역할이고 수량은
  HUD 카운터가 말한다 — 개수만큼 띄우면 화면이 덮인다.
- **소모(불빛/화폐 → 버튼)는 이 경로로 보내지 않는다.** 나가는 값에 획득 아이콘이
  뜨면 무엇이 늘고 무엇이 줄었는지가 뒤집힌다. 상점 지불·보스 소용돌이 같은 비획득
  트레일은 기존 사각 파편(`animateResourceTrail`)을 그대로 쓴다.

**별빛 토큰을 반짝(✦) 글리프로 교체.** 45° 세운 사각 조각은 결국 '기울어진 알약'으로
읽혔다. 불빛에 쓰는 네 꼭짓점 반짝 글리프를 그대로 쓰되, 황금빛 심지에 **푸른빛을 섞은**
발광으로 일반 불빛과 구분한다 — 가르는 것은 형태가 아니라 빛의 색이다. 푸른 빛무리는
반경을 따로 키웠다(기본 반경으로는 금빛 심지에 먹혀 보이지 않는다).

### v1.0.28 — 2026-08-04

**별빛 수집을 파편 트레일에서 '떠올랐다 꽂히는 반짝이'로.**

90~100층에서 별빛은 턴을 여는 유일한 열쇠인데, 획득 연출은 다른 자원과 똑같은
파편 트레일이라 그 무게가 화면에 없었다. 손패 획득 토큰과 같은 세 박자를 쓰되
방향을 뒤집었다 — 손패는 곡사로 튀어나와 떨어지지만 별빛은 **떠오른다**.

- 카드가 빛으로 풀려 위로 솟고(0~350ms) → 두둥실 표류하며 한 번 반짝이고
  (350~720ms) → 턴 카운터 **위로 건너간 뒤 수직으로 내리꽂힌다**(~1155ms).
  옆에서 흘러들면 그냥 자원이 되므로 정점을 목표 위에 두고 낙하 구간을 만든다.
- 꽂힌 **뒤에** 턴이 오른다(실측: 착지 1239ms · 턴 +1 1295ms). 여러 장을 한 번에
  먹어도 장마다 이 순서가 반복돼 몇 턴이 올랐는지 세어진다.
- 토큰은 전용 도형이 아니라 **같은 사각 조각을 45°로 세운 것**이다(이펙트 어휘 규칙).
  회전은 흔들림 수준으로만 얹는다 — 손패 토큰처럼 두 바퀴 돌리면 굴러가는 보석이
  되고 다이아 실루엣이 사라진다.
- 발광을 흰빛으로 쌓았더니 조각이 통째로 날아가 **정체 모를 흰 공**이 됐다. 심지만
  은백으로 두고 몸통·발광을 남보라로 내려 빛 속에서도 윤곽이 남게 되돌렸다.

### v1.0.27 — 2026-08-04

**기록 패널을 투명 레이어로 걷어 내고, 강조 맥동을 대상별로 어긋냈다.**

로그 패널이 테두리·배경·그림자를 가진 상자 안에 `여정의 기록` 제목과 기록 개수까지
달고 있었다. 로그 줄 자체가 이미 좌측 광원으로 자기 범위를 말하는데 그것을 감싸는
판이 또 있어 톤이 두 번 겹쳤고, 제목/카운터는 읽을 일이 없는 자리를 48px 먹었다.
헤더를 통째로 들어내고 `.score-log-list`를 투명하게 비웠다 — 스크롤 트랙도 함께
비웠다(세로 막대가 남으면 걷어 낸 테두리가 그 자리에 다시 생긴다).

강조 맥동(v1.0.26)은 대상이 여럿일 때 전부 같은 순간 켜졌다. 첫 조우 소개처럼 여러
칸을 한꺼번에 가리키면 어디를 보라는 건지 흩어진다. `ENA_HINT_STAGGER_MS`(180ms)로
하나씩 짚어 가듯 어긋내 켠다. 연출 종료 시각은 대상 수에 따라 달라지므로
`activeEnaHint.endsAt`로 들고 다닌다 — 고정 상수면 마지막 대상이 잘린다.

### v1.0.26 — 2026-08-04

**에나가 가리키는 강조를 세 번 맥동으로.**

한 번만 번쩍이고 끝나 대사를 읽는 사이에 놓쳤다. 이제 대사가 뜬 **뒤**(0.24초)에 시작해
**세 번** 맥동한다(1.35초 × 3 = 4.05초). 맥동 사이에는 투명한 쉼을 둔다 — 쉼 없이 이으면
한 번의 긴 번쩍임으로 뭉쳐 몇 번인지 세어지지 않는다.

- **렌더를 넘어 이어진다**(`reattachEnaHintPulses`). 강조는 카드 안에 심긴 레이어라
  `innerHTML` 교체에 통째로 날아가는데, 4초를 쓰는 연출이라 그 사이 렌더가 한 번은 돈다.
  이미 흐른 시간만큼 시작 지연을 당겨(음수 delay) 같은 위상에서 다시 심는다.
- **플레이어가 먼저 움직이면 끊는다**(`clearEnaHintPulses`, 칸/손패 클릭). 이미 고른 뒤에도
  맥동하면 "아직 여길 봐"로 읽혀 방금 한 선택과 어긋난다. 다음 설명이 올 때도 이전 강조를
  거둔다 — 겹쳐 두면 무엇을 가리키는지 흐려진다.
- 실측: 생존 4320ms(기대 4290ms) · 맥동 3회 · 도중 클릭 시 2302ms에 중단.

### v1.0.22 — 2026-08-03

**첫 실행 인게임에서 로그창이 통째로 안 보이던 문제.**

`.score-panel`이 `grid-template-rows: auto auto 1fr`(불빛 / 화폐 / 로그)이었다. 새싹
병아리처럼 **화폐 패널이 숨겨지는 런**(`meta-currency-locked` → `display: none`)에서는
남은 두 칸이 auto·auto 행으로 밀려 **1fr 행이 비었고**, 로그 목록은 자식이 절대배치라
auto 높이가 곧 0이 됐다. 실측 `276×0`.

열 배치를 flex로 바꾸고 로그 슬롯에 `flex: 1`을 줬다 — 형제가 몇이든 남는 높이가 로그로
간다. 실측 `276×0` → `276×638`(첫 실행), 테스트 런은 그대로.

**로그 레이어 재설계 — 판이 아니라 빛으로.**

테두리와 채운 배경, 왼쪽 3px 막대를 걷어냈다. 대신 좌측 색 라벨을 **번지는 광원**으로
풀고(blur된 그라디언트 + 가는 심지 한 줄), 행 전체는 좌 → 우로 사그라드는 호박빛
반투명 판으로 깔았다. 오른쪽 끝은 완전히 투명해 배경 촛불이 그대로 비친다.

- 종류별 색은 `--log-hue` 변수 하나로 갈린다. 새 종류는 그 한 줄만 더하면 된다.
- 빈 상태의 점선 상자도 걷었다 — 로그 줄보다 눈에 띄어 '없음'이 강조돼 보였다.

### v1.0.21 — 2026-08-03

**첫 판 시작과 동시에 긴 설명이 쏟아지던 문제.**

바위·덤불·잡동사니가 한꺼번에 깔리면 앞머리 + 종류별 절 3개 + 마무리를 **한 줄로 묶어**
말했다. 60자가 넘는 문장이 시작하자마자 나와 읽기 전에 지쳤다. 이제 한 beat에
**한 종류만** 소개하고, 나머지는 표시하지 않고 남겨 둔다 — 다음 턴 스캔이나
**그 칸을 실제로 눌렀을 때**(`introduceFieldKindOnce`) 그 자리에서 짚어 준다.
효과를 받는 순간과 설명이 한 박자에 붙어 훨씬 알기 쉽다.

- 무엇부터 짚을지는 위협 순서를 따른다(폭탄·포자·거미줄 → 덤불·바위·잡동사니 → 문·별빛·씨앗).
- 묶어 말하던 `FIELD_INTRO_COMBINED_LEAD`/`FIELD_INTRO_BRIEF`는 지웠다. 지금 요구와
  정반대의 장치라 남겨 두면 다음 작업자가 되살린다.
- 실측(첫 실행): 거미줄 5.2초 → 바위 16.5초 → 덤불 22.5초, 최장 46자.
  이전에는 한 줄에 셋을 다 담아 60자를 넘겼다.

**대사 검수.**

- `재 봤어`/`재 보니까` → `숫자를 세어 봤어`/`따져 보니까`. '재다(셈)'인데 이 게임에서는
  촛농·재 모티프가 먼저 읽혀 뜬금없는 문장이 됐다.
- `옮기 전에 끊자` → `번지기 전에 끊자`. '옮기다(이동)'로 먼저 읽혀 뜻이 뒤집혔다.
- 전 풀 13,927개 렌더를 기계 검사했다(미치환 토큰·이중 공백·문장부호·56자 상한·
  고풍 어미·슬랭·긴 작대기) — 위 두 건 외 잔여 0건.

### v1.0.20 — 2026-08-03

- **손패가 쌓이면 이름이 밀리던 정체 — 8장 경계에서 18px 튀고 있었다.**
  7장까지는 이름이 아래 정렬(`align-items: end` + 세로 여백 8px), 8장부터
  `is-crowded`가 붙어 가운데 정렬로 갈렸다. 그래서 한 장을 더 얻는 순간 **모든 이름이
  한꺼번에 위로 튀었다**(카드 높이 대비 0.705 → 0.5). 장수와 무관하게 늘 가운데로
  통일했다 — 카드가 줄어도 이름이 제자리에 남아 어느 높이에서나 안전하다.

- **트리플 손패를 등급과 겹치지 않게 다시 만들었다.**
  - **뺀 것**: 우상단 반짝 아이콘(레시피 힌트와 자리 다툼), 금색 테두리 + 바깥 발광
    (등급 테두리·글로우도 box-shadow라 **등급이 안 읽혔다**), 이름의 알약 배경
    (선을 그으면 '선택된 UI'로 읽히고 등급 테두리와 선이 겹친다).
  - **넣은 것**: 카드 **그림 위에만** 색을 입히는 오버레이(little_token 기법).
    `color` 한 겹만 쓰면 색조가 통째로 갈려 샛노래지므로, `overlay`를 섞어
    원래 명암을 살린 **황금빛**으로 만들고 `soft-light` 한 겹으로 밝기를 얹었다.
  - **호일**(`.triple-foil`) — little_token `.card-foil`에서 가져왔다. 반짝이는 점이
    흐르고(`::before`) 좁은 광택 띠가 비스듬히 지나간다(`::after`). `screen` 합성이라
    어두운 부분은 거의 건드리지 않는다.
    타일 간격을 47~91px로 촘촘히 깔았더니 손패처럼 작은 카드에서는 호일이 아니라
    **얼룩**으로 보여, 118~193px로 넓혀 카드당 서너 점만 남겼다.
  - 트리플 표시는 이제 **그림의 금빛 + 이름의 ★와 글자 발광**이 맡는다.
    등급 테두리는 온전히 등급만 말한다.

### v1.0.19 — 2026-08-02

**손패 슬롯이 토큰보다 먼저 앉던 순서 어긋남.**

새 손패 슬롯의 등장 지연이 `330ms`로 못 박혀 있었다 — **예전 자원 트레일**의 착탄 시각이다.
카드 토큰은 874ms에 도착하므로, 토큰이 아직 날아오는 중에 카드가 먼저 자리에 앉았다.
지연을 `HAND_TOKEN_LAND_MS`(= 비행 × 착탄 프레임)로 묶고, 슬롯 등장 간격도 토큰 발사
간격(`handTokenStaggerMs`)과 같은 값을 쓰게 했다 — 둘이 다르면 세 번째 장부터 어긋난다.
실측: 토큰 발사 73ms → 착탄 947ms → 슬롯 등장 1000ms(이전에는 착탄 전에 떴다).

**긴 대사일수록 빠르게 친다.**

글자당 70ms 고정이라 56자 대사가 3.9초를 끌었다. 전체를 목표 시간(1.5초)에 맞추되
글자당 26~70ms로 가둔다 — 짧은 말은 그대로고(21자 이하 무변화) 긴 말만 빨라진다.
56자 3920 → 1512ms · 40자 2800 → 1520ms.

**좌측 패널의 이중 표기와 작은 아이콘.**

- 손패 헤더가 `(카드 아이콘) 손패 (0/10)`으로 같은 말을 두 번 했다. 낱말을 지우고
  `(카드 아이콘) 0/10`으로 줄이면서, 낱말이 맡던 몫만큼 아이콘을 20px로 키웠다.
- 콤보 게이지 이름표 아이콘은 12px 글자를 따라 1.05em이라 눈금이 뭉개졌다. 글자를
  따라가지 말고 17px로 못 박았다 — 이 아이콘이 게이지의 이름표다.


### v1.0.18 — 2026-08-02

**손패 토큰이 엉뚱한 데서 떨어지던 문제 + 낙하 감각 손질.**

손패로 대기열 적을 잡으면 전리품이 그 칸이 아니라 **화면 중앙**에서 나왔다.
`awardHandKillDrops`가 출처를 `{kind:'center'}`(펼쳐진 카드 자리)로 두고 있었기 때문이다.
잡은 칸의 rect를 **`render()`보다 먼저** 잡아 그 자리에서 나가게 고쳤다 — 렌더가 돌면
그 칸이 DOM에서 사라져 위치를 못 찾는다(못 찾으면 예전대로 중앙에서 낸다).

- 착지점도 출처 **아래 54px**에서 **16px**로 당겼다. 크게 내리면 잡은 칸이 아니라
  한 줄 아래에서 나온 것처럼 보였다.
- 전체 길이 1250ms → **950ms**, 바닥에 머무는 구간도 짧게(0.62 → 0.5). 오래 남아 있어
  '아직 안 들어갔다'로 읽혔다.
- **줄 세우지 않는다**: 착지 각도(±5~21°)와 높이(±13px)를 장마다 어긋내고, 착지 프레임을
  더 납작하게 눌러(1.3×0.66) 튕기게 했다. 세로로 나란히 서면 진열대로 보이고, 기울어야
  '툭 떨어뜨린 것'으로 읽힌다. 난수가 아니라 고정 표라 같은 장수는 늘 같은 모양이다.

### v1.0.17 — 2026-08-02

**손패가 차면 콤보 게이지를 덮던 문제.**

손패 스택은 `justify-content: flex-end`라 카드가 아래로 쌓이고, 넘치면 **위로** 삐져나간다.
그런데 `.hand-slot`은 `flex-shrink: 0`이라 줄지 않아, 열 높이보다 카드 총합이 크면 그대로
위쪽 콤보 게이지를 덮었다. 1024×600에서 실측 **72px 침범**(게이지 높이가 60px이니 통째로
가려진다).

이미 대응이 있긴 했다 — 다만 `(orientation: landscape) and (max-height: 520px)` 조건이라
**1024×600 같은 '긴 가로'는 걸리지 않았다**. 화면 크기로 가르는 방식 자체가 원인이라,
조건을 넓히는 대신 **넘칠 때만 줄어들게** 바꿨다: `flex-shrink: 1` + `flex-basis: 78px`.
여유가 있으면 flex는 아무것도 줄이지 않으므로 기존 크기가 그대로 유지된다.

- `min-height`를 0으로 푸는 대신 기본 높이를 `flex-basis`로 옮겼다. min-height만 풀면
  여유가 있는 화면에서도 카드가 내용 높이까지 주저앉는다(1100×900에서 73→49px).
- 줄어든 카드는 이름을 아래가 아니라 가운데에 두고 세로 여백을 걷는다 — 8px×2 여백이
  얇아진 높이를 통째로 먹어 글자가 위아래로 잘렸다.
- 실측(10장): 1024×600 침범 72→0px(카드 61→35px) · 1100×900 침범 0px 유지(73→64px) ·
  915×412 변화 없음(22px).

### v1.0.16 — 2026-08-02

**아이콘이 말하는 낱말을 글자에서 지웠다.**

`(하트) 체력 +5`는 같은 말을 두 번 한다. 아이콘이 대신하는 낱말을 지워 `(하트) +5`로
줄였다. 지우는 조건은 둘 다 필요하다: **문장 맨 앞**에 있고 **바로 뒤가 수치**일 것.
앞이 아니면 아이콘은 앞머리에 붙는데 지워진 낱말은 문장 중간이라 뜻이 어긋나고
(`체력 5 손실마다 빛 게이지 +1`), 뒤가 수치가 아니면 문장이 무너진다
(`공격력만큼 체력 회복` → `만큼 체력 회복`).

- `$`는 앞머리 아이콘을 아예 붙이지 않는다 — 글자 자체가 화폐 표식이고 수치 **뒤**에
  붙는 단위라, 앞에 하나 더 세우면 `$1$`가 됐다.
- 무료 카드 `랜덤 손패 +2`는 데이터에서 `손패 +2`로 줄였다(무작위 지급은 손패의 기본).

**손패로 잡은 전리품에 보이지 않는 제약을 걸었다.**

카드 표기(`0~N장`)는 그대로 두고 굴림만 눌렀다 — 손패·조합 처치는 **두 번 굴려 낮은
쪽**을 쓴다(`rollDefeatDrops(true)`). 0~1이면 50:50이 25:75가 된다. 범위를 줄이면
표기를 믿고 잡았다가 손해 보는 느낌이 남으므로 범위 자체는 건드리지 않았다.

- 실측 기댓값(직접 → 손패): 1칸 0.50 → 0.25 · 2칸 1.00 → 0.56 · 3칸 1.51 → 0.87.
- 행동당 1장 상한은 그대로 유지한다. 눌린 확률만으로는 3칸 여럿을 한 번에 잡는
  광역기가 여전히 순이득이 된다(3칸 × 0.87 = 2.6장 > 1장).
- 학습 시뮬도 같은 두 겹을 쓴다.

**레일에서 손패가 떨어지는 연출.**

보물·꽃·적에서 손패가 나오면 이제 **황금 카드 토큰**이 장수만큼 튀어나온다
(26×38 — 손패 아이콘과 같은 비율). 전용 도형이 아니라 같은 사각 조각의 **비율 변주**다 —
정사각에 가까우면 그냥 파편으로 읽히고, 세로로 길어야 '카드'가 된다. 호박빛 발광을
세 겹 둘러 어두운 레일 위에서도 금붙이로 읽히게 했다.

낙하 중에는 **두 바퀴 회전**한다(뒤로 갈수록 빨라지는 t² 곡선). 회전은 짝수 바퀴로
끝나야 착지 프레임에서 되감기지 않고, 방향은 카드마다 번갈아 줘 여러 장이 한 덩어리처럼
같이 도는 것을 막는다.

박자는 셋이다: 곡사로 튀어나와 착지(푱) → **살짝 표류** → 손패로 빨려 들며 블라스트.
표류 구간을 빼고 멈춰 세웠더니 화면이 굳어, 몇 장이 나왔는지 세는 구간이 정지 화면이
됐다. 착지점은 부채꼴로 벌려 겹치지 않게 한다.
목적지가 `hand`인 트레일은 출처가 어디든(레일 칸·화면 중앙·체인 배너) 이 어휘를 쓴다 —
파편 세 줄기로 흘러드는 자원 트레일은 손패 획득에서 더 이상 쓰지 않는다.

### v1.0.15 — 2026-08-02

**적도 손패를 준다 — 그 규칙을 카드에 적어 두고, 착취 고리는 막았다.**

지금까지 적 처치 전리품은 폭과 무관하게 무조건 1장이었고 카드 어디에도 적혀 있지
않았다. 이제 `0 ~ 폭` 균등 굴림이고(1칸 0~1 · 2칸 0~2 · 3칸 0~3) 그 범위를 카드
가운데(체력과 공격력 사이 빈자리)에 작게 적는다. 굴림의 단일 출처는
`Card.rollDefeatDrops()`이고 표기가 곧 그 분포다.

- 넓은 적을 그대로 두는 위험을 손패로 갚는 축이 생겼다. 기댓값은 1칸 0.5(하향) ·
  2칸 1.0(동일) · 3칸 1.5(상향)로, 합체를 방치할 이유가 처음으로 생겼다.
- **손패·조합으로 잡아도 전리품이 나온다** — 다만 **행동 1회당 1장**이 상한이다.
  손패는 턴을 넘기지 않으므로 잡은 수만큼 돌려받으면 턴을 올리지 않은 채 손패와
  불빛을 무한히 불릴 수 있다(광역기 한 장 → 3장 회수 → 다시 광역기). 상한 1이면
  쓴 것보다 더 받을 수 없어 고리가 성립하지 않는다: 광역기로 1칸 적 셋을 잡아도
  기대 회수는 0.875장으로 여전히 손해다. 직접 타격은 턴을 소모하므로 0~N 그대로다.
- 학습 시뮬에도 같은 상한을 뒀다(`handKillDropBudget`). 시뮬의 처치 드롭은 폭과
  무관한 고정 30%였는데, 그대로 두면 에나가 실제와 다른 경제를 배운다.
- 미믹은 자기 수치를 그대로 따라가 1/2/3칸이 0~2 / 0~5 / 0~10이 된다. **평균 전리품이
  절반이 된다**(2→1 · 5→2.5 · 10→5). 보물인 줄 알고 열었다 물리는 카드라 변동폭이
  성격에 맞다고 보고 수치는 두었다 — 되돌리려면 `MIMIC_BY_SPAN.drops`만 올리면 된다.
- 0장 굴림과 '손패가 가득 차 잃음'은 다른 문구로 남긴다. 같은 말로 뭉치면 왜 못 받았는지
  알 수 없다.

**소진된 카드팩과 2장짜리 팩 피커.**

- 후보가 3장보다 적으면 열 수를 장수에 맞춘다(`--pick-count`). 3열 고정이라 2장일 때
  빈 칸 하나가 남아 한쪽으로 쏠려 보였다. 안내 문구도 실제 장수를 따른다.
- 더 줄 것이 남지 않은 조합팩/해금팩은 가격을 지우고 판을 어둡게 깐 뒤 흰 `소진`을
  크게 박는다. 클릭도 받지 않는다 — 사도 빈 피커가 열려 불빛만 잃었다.
- 표기와 실제 후보는 `isPackExhausted()` 한 함수를 본다. 갈리면 살 수 있는데 소진으로
  보이거나, 사고 나서 빈 피커가 열린다.

### v1.0.14 — 2026-08-02

**발사체를 곡사포로 바꿨다 — 조준 볼트(v1.0.9)를 되돌린다.**

곧게 가속해 꽂히는 조준 볼트는 300ms 만에 끝나 눈이 좇을 틈이 없었고, 진행 방향으로
눕혀 늘인 조각은 사각 블라스트 어휘에서 혼자 다른 도형이었다. 자원 트레일과 같은
**정사각 조각**으로 되돌리되, 자원처럼 얕게 흘러들지 않도록 궤적을 곡사로 바꿨다.
되돌린 이유를 남긴다: v1.0.9에서 포물선을 버린 근거는 "'흘러들었다'로 읽혀 어느 칸을
때렸는지 흐려진다"였다. 그 근거는 **정점이 낮을 때만** 성립한다 — 정점을 목표보다
`LOB_PLUNGE_PX`(120) 위로 강제해 마지막 구간이 수직 낙하가 되면 착탄점이 오히려 또렷해진다.

- 비행 300ms → **620ms**, 등속(linear). 가속하면 좇을 수 있는 구간이 사라진다.
- 궤적은 2차 베지에이고 제어점을 목표 쪽(0.62)으로 치우쳐 정점이 목표 바로 위에 온다.
  정점 높이는 거리 비례가 아니라 **목표 기준 역산**이다 — 비례식만 쓰면 가까운 목표에서
  내리꽂는 구간이 사라졌다.
- 조각은 12표본 키프레임으로 곡선을 편다(성기면 꺾인 선분으로 보인다). 올라갈 때 커지고
  떨어질 때 작아져 높이를 크기로 말하고, 착탄 프레임에서 납작하게 눌린다.
- **레시피(조합)도 대상마다 한 발씩 쏜다**(출처 = 체인 배너). 도화선·성화처럼 전방
  전체를 치는 조합은 지금까지 발사체가 아예 없어 "무엇이 몇 칸을 때렸는지"가 없었다.
  손패 지급 `render()`보다 **먼저** 쏜다 — 렌더가 돌면 제거된 카드가 DOM에서 사라져
  그 칸으로 갈 발사체가 조용히 없어진다.
- 대상이 많으면 발사 간격만 좁히고(70ms → 최소 26ms) 발수는 줄이지 않는다. 필드 전체
  9칸도 같은 간격으로 쏘면 한 방의 광역기가 늘어진 연사로 읽혔다.

**모바일에서 Shift 자세히보기가 켜진 채로 굳던 버그.**

손패 카드의 `touchend` 핸들러가 `stopPropagation()`을 부르는데 롱프레스 해제 리스너가
버블 단계에 있었다. 카드 위에서 480ms 넘게 눌렀다 떼면 해제가 **영영 오지 않아**
`is-shift-detail`이 남았고, 그때부터 기본 표기가 수식으로 보였다. PC는 keyup으로 풀려
증상이 없었다. 해제 리스너를 캡처 단계로 옮기고 `blur`/`visibilitychange` 안전망을 뒀다.

**기본 레시피 5종을 해금 대상으로 돌렸다.**

조합팩은 '재료가 이미 해금된 잠긴 레시피'만 제시하는데, 잠긴 레시피가 전부 잠긴 손패를
요구해서 기본 손패풀 런에서는 제시할 것이 `불꽃 주입` 하나뿐이었다. 판을 통째로 뒤집는
쪽을 잠가 해금 풀을 5종으로 만들었다: `한 걸음씩`(필드 전체 제거) · `도화선`(기본 재료 중
최고 배율 · 흔한 성냥 2장) · `밀매`(대기칸 보물 전량 · 상한 없음) · `불꽃 사슬`(방어와
필드 공격을 동시에). 기초 조합(따뜻함·점화·열쇠공 등 8종)은 그대로 열어 둔다.

### v1.0.13 — 2026-08-02

**꽃 아이콘이 여전히 따로 놀았다 — 원인은 글리프가 아니라 flex 정렬이었다.**

v1.0.12에서 아이콘 글리프를 박스 한가운데로 맞췄는데도 꽃·보물 표기만 아이콘이 떠 보였다.
`.card-stats.group-note`에 `align-items`가 없어 기본값 `stretch`로 놓였고, 글자 줄상자는
1.35em인데 아이콘은 1em이라 **아이콘이 줄상자 위쪽에 붙었다** — 글리프를 아무리 맞춰도
컨테이너가 밀어 올리면 소용이 없다. `align-items: center` + `line-height: 1`로 잡았다.

- **꽃·보물 표기 확대**(12px → `clamp(15px, 2.05vh, 20px)`): 함정 피해(16~21px)와 반 단계
  차이로 붙였다. 셋 다 "이 칸에서 얼마가 오가는가"를 말하는 수치인데 꽃·보물만 작아
  그 칸의 값어치를 낮게 어림잡게 됐다. 대기 행(dist-1/2)은 한 단계 줄인다.
- 아이콘·`$` 표식을 `1.05em`으로 바꿔 글자를 따라가게 했다(고정 13px이라 글자만 커지면
  다시 따로 놀았다). v1.0.12의 꽃 전용 규칙은 이 공통 규칙으로 흡수해 지웠다.
- 보스 보상만 표기가 수치가 아니라 문장('체력 / 불씨 회복')이라 키우면 줄이 넘친다 —
  `group-note--sentence`로 원래 크기를 지킨다.

### v1.0.12 — 2026-08-02

**아이콘을 넣고 나니 아이콘끼리 줄이 안 맞았다 — 글리프 정렬을 한 곳으로 모았다.**

아이콘마다 24 박스 안에 그려진 높이·위치가 제각각이었다. 하트는 아래로 처지고(중심
13.25) 콤보 게이지는 위로 떴다(중심 9.7) — 같은 크기로 놓아도 글줄 위에서 최대 0.15em,
14px 기준 2px가 어긋나 "미묘하게 안 맞는" 느낌이 남았다. CSS `vertical-align`을 아이콘별로
손보면 쓰이는 자리마다 다시 어긋나므로, 실제 잉크 경계를 24 박스 한가운데로 옮기는
보정(`fitBox`)을 `Icons.ts` 한 곳에 두고 하트·촛불·주머니·불꽃·콤보 게이지에 적용했다.
측정 결과 12종 전부 −0.074em(계통 오프셋)에 모였고 잔차는 0.01em 이하다.

- **빛 게이지 불꽃 교체**: 두 겹으로 휘감던 부적 실루엣은 작은 크기에서 뭉쳐 불꽃으로
  읽히지 않았다. 아래가 통통하고 위로 좁아지며 끝이 갈라지는 한 덩어리 실루엣으로 바꿨다.
- **콤보 게이지 눈금 6칸 → 4칸**: 12px에서 눈금이 서로 뭉개져 얼룩으로 읽혔다. 굵고 적게
  가고, 반짝임은 눈금 위로 완전히 빼 겹치지 않게 했다.
- **보물 표기에서 '손패' 글자 제거**(`손패 1~2장` → `1~2장`): 앞의 카드 아이콘이 이미
  손패를 말한다. 좁은 카드에서 수치가 클수록 유리하다.
- **꽃 수확 수치 확대**(12px → `clamp(15px, 2vh, 19px)`): 함정 피해와 같은 결로 키웠다.
  둘 다 "이 칸에서 얼마가 오가는가"를 말하는 수치인데 꽃만 작아 어림잡기 어려웠다.
  아이콘과 `$` 표식도 함께 em으로 따라오게 했다(고정 13px이라 글자만 커지면 따로 놀았다).
- **씨앗의 수확 아이콘 제거**: 아직 아무것도 주지 않는 칸에 ✦를 달아 두면 이미 자원이
  나오는 칸으로 읽힌다. '대기' 글자만 남겼다.

### v1.0.11 — 2026-08-02

**아이콘이 "무엇을 주는가"를 말하지 못하고 있었다 — ✦ 하나로 뭉뚱그려진 자리를 걷어냈다.**

가장 심한 곳은 꽃이었다. 꽃 5종은 주는 자원이 전부 다른데(불빛·체력·화폐·방패·콤보
게이지) 카드에는 모두 `✦ +3` 하나로만 표시돼, 이름을 외우지 않으면 무엇을 키우는지
알 수 없었다. 함정 피해는 적 공격력과 같은 검(⚔)을 써서 "이 함정이 공격한다"로 읽혔다.

- **신규 아이콘 3종**(`Icons.ts`): `trapIcon`(마주 문 이빨) · `handCardIcon`(카드 1장) ·
  `comboGaugeIcon`(차오르는 눈금 + 반짝). 전부 단색 `currentColor` 플랫 path 규칙을 따른다.
  카드 안쪽 반짝임은 배경색으로 덮지 않고 `fill-rule: evenodd`로 **뚫는다**(`punch`) —
  표면 색이 다른 도감/상점에서 사각 얼룩이 남지 않게.
- **꽃 수확물 표기**의 단일 출처는 `CardFaceRenderer.flowerHarvestMeta()`다. 카드 face와
  도감 꽃 탭이 함께 읽으므로 새 꽃을 추가해도 한 줄만 늘리면 둘이 같이 따라온다.
  화폐만 아이콘이 아니라 `$` 글자를 쓴다(HUD 지갑과 같은 어휘).
- **✦ 오용 정리**: 별빛 `턴 +1` → 촛불(턴) · 보물 칸 `손패 N장` → 카드 ·
  손패 헤더와 게이지 '손패' 모드의 주머니(가방) → 카드.
- **함정 피해**: 검 → 이빨. 카드 스탯·도감 함정 탭·시련 '역경'이 같은 아이콘을 쓴다.
  수치는 살짝 줄이고(18~24px → 16~21px) 색을 적 공격력(#ff9b4a)보다 붉은 쪽(#ff4a33)으로
  밀었다 — 밟으면 내가 피를 보는 수치라 경고 톤이 맞다. 발광은 검붉게 깔아 촛불 톤은 지킨다.
- **글자로만 써 있던 보상 문구**(무료 카드 6종·제단 자원팩 8종·시련)에 앞머리 아이콘을
  붙였다(`resourceTextHtml`). 문구는 그대로 두고 훑기만 빨라진다. 어휘 판정은 좁은 것부터
  본다(`콤보 게이지` → `게이지`). 손패 설명처럼 **효과가 섞인 문장**은 앞머리 아이콘이
  나머지를 지우므로, 자원 어휘로 시작하고 구분자가 없을 때만 붙인다(`resourceLeadTextHtml`).
- **도감 용어 탭 최신화**: 10항목 → 16항목. 각 항목에 화면에서 실제로 쓰는 아이콘을 달아
  도감이 사전 노릇을 하게 했다. 콤보 게이지 설명을 실제 동작(1장 1칸·트리플 3칸·만충 시
  영구 성장·초과분 이월)으로 고치고, 함정 피해·보물 칸·별빛·꽃·불빛·화폐·손패를 새로 넣었다.
  수치는 적지 않고 규칙만 적는다 — 밸런싱 때 조용히 어긋난다.

**첫 실행 인트로에서 유틸 레이어만 슬라이드인을 안 하고 있었다.**

`.left-panel`/`.hand-column`만 숨겨 두고 플레이어 카드 양옆의 유틸 레이어(좌: 도감/경험 ·
우: 유물)는 빼먹어, 카드가 홀로 떠오르는 인트로 한가운데에 버튼이 이미 놓여 있었다.
둘도 같은 beat로 좌/우에서 들어오게 했다.

### v1.0.10 — 2026-08-02

**칸 표기가 그 칸이 무엇인지와 어긋나 있었다.**

- **새싹 만료 뱃지 색을 카드 타입에 맞췄다.** 바위(적)·덤불(함정) 모두 뱃지가 오프화이트/
  잎빛이라 뱃지만 봐서는 무슨 칸인지 알 수 없었다. 이제 바위는 적 타일 붉은빛, 덤불은
  함정 타일 어스름 보랏빛, 잡동사니는 보물 금빛 — 카드 상단 액센트 띠(`.type-*`)와 같은
  계열이다. 타일 액센트 원색(`--color-enemy` #a83a3a / `--color-trap` #5c3f7a)은 어두워
  11px 글자로는 안 읽히므로 같은 계열의 밝은 쪽(적 스탯 #ff6b5a 톤)을 썼다.
- **함정 피해 수치를 적 스탯과 같은 크기로 키웠다**(`card-stats--trap`, 12px → 18~24px).
  둘 다 "이 칸이 나를 얼마나 때리는가"를 말하는 수치인데 함정만 작아 피해를 낮게
  어림잡게 했다. 수치가 하나뿐이라 양 끝으로 벌리지 않고 가운데에 그대로 둔다.
- **열쇠/열쇠공/밀매 표기를 '보물상자' → '보물칸'으로 고쳤다.** 실제 대상 필터가
  `CardType.TREASURE` 전체라 상자뿐 아니라 잡동사니·황금 상자·별빛도 연다 — 설명이
  실제보다 좁게 적혀 있었다. 상자 카드를 실제로 **만들어 내는** 변환 계열(정화·탐욕)은
  '보물상자'가 맞으므로 그대로 뒀다.

### v1.0.9 — 2026-08-02

**전체 새로고침 때마다 화면의 모든 CSS 애니메이션이 0프레임으로 되감기고 있었다.**

`render()`는 보드를 `innerHTML`로 통째 교체한다. 그 순간 진행 중이던 CSS 애니메이션이
**전부 동시에 처음으로 되감긴다** — 촛불 발광·레일 예고선 맥동·보스 뱃지 호흡·약점 칸
맥동이 한꺼번에 튀는 것이 "화면이 번쩍인다"의 정체였다. 실측(브라우저): 렌더 직전
9650ms까지 흐르던 루프들이 렌더 직후 일제히 33ms로 돌아갔다.

- `captureBoardAnimationPhases()` / `restoreBoardAnimationPhases()`를 render 앞뒤에 두어
  재생 위치를 인계한다. **무한 루프는 이름으로**(같은 이름의 루프는 이미 같은 위상이라
  이름만으로 정확히 되돌아간다), **유한 애니메이션은 신원으로**
  (`data-fx-id`/`data-card-id`/`data-boss-gimmick-cell`/`data-hand-uid`).
  이번에 처음 생긴 것은 손대지 않는다 — 그건 실제로 지금 시작한 연출이다.
- 실측 결과 렌더 전후 되감김 **0건**(일반 레일·보스전 모두), 애니메이션 시각이 렌더를
  가로질러 단조 증가(12849 → 13016 → 13250)한다. 처음엔 공용 시계(epoch) 위상으로 맞추는
  안을 썼는데, 요소가 태어난 뒤 첫 렌더에서 한 번 위상이 튀어 이름 기반으로 바꿨다.
- 비용은 render당 `getAnimations({subtree:true})` 2회 = 약 0.07ms(325노드 기준 0.034ms/회).
- **남는 한계(인지하고 둔다)**: CSS **트랜지션**은 요소가 교체되면 되살릴 수 없다.
  게이지 폭처럼 렌더를 넘어 이어져야 하는 값은 이미 표시값 기준으로 다시 그리고 있다
  (`hudCounterVisibleStartValue`/`syncHudCounterLinkedVisuals`). `getAnimations` 미지원
  브라우저에서는 보정 없이 기존 동작으로 내려간다.

**피격 체력 게이지 연출.** 피해 수치는 떠올라 사라지지만 "얼마나 남았는가"는 막대에
남는데, 막대가 아무 말도 하지 않아 수치와 게이지가 별개의 사건으로 흩어져 있었다.

- 피격 시 체력 게이지가 확대(×1.22) → 좌우로 흔들리며 → 시뻘겋게 달아올랐다 → 640ms에
  걸쳐 돌아온다. 수치 롤링도 같은 beat에서 함께 굴러 내려간다.
- 방패가 전부 막아 체력이 그대로면 발동하지 않는다 — 안 깎였는데 게이지가 비명을 지르면
  방패가 막아 준 사실이 지워진다.
- 붉은빛은 전부 `inset`으로 낸다(`.player-card`가 `overflow: hidden`이라 바깥 그림자는
  잘린다). 채우기는 밝기만 올리면 기본 그라디언트가 **노랗게** 타 버려, 그라디언트 자체를
  붉은 쪽으로 갈아 끼웠다.
- 연출 중 render가 돌아도 클래스가 유지되도록 만료 시각(`hpDamagePulseUntil`)을 상태로
  두고 render가 다시 심는다. 진행 위치는 위의 인계 장치가 이어 붙인다.

### v1.0.8 — 2026-08-02

**양초 고양이가 "줬다 뺏는" 보스가 됐고, 칸 정보/파괴 표기를 읽을 수 있게 키웠다.**

- **고양이 주기(30F 새싹).** 페이지로 성격이 뒤집히던 것(1P 주기 / 2P 뺏기)을 바꿔,
  **주기를 모든 페이지의 기본**으로 두고 2페이지에 뺏기를 얹었다. 매 주기 손패 2장
  (`CAT_GIFT_CARDS`)을 굴려 주고, 2페이지는 그 **뒤에** 1장(`CAT_PAGE_TWO_STEAL_CARDS`)을
  도로 가져간다. 순서가 주기→뺏기라야 뺏기는 장이 방금 받은 두 장일 수도, 원래 쥐고 있던
  패일 수도 있어 "줬다 뺏는다"가 한 주기 안에서 읽힌다. 두 박자 사이에 `pauseBeat()`를 둬
  살포 연출과 강탈 연출이 한 덩어리로 뭉개지지 않게 했다. 2페이지 대사도 "이제 안 나눠 줄
  거야"에서 "준 건, 도로 가져갈 거야"로 바꿨다 — 여전히 주기 때문이다.
  수치는 `BossPages.ts` 단일 출처이고 RL 시뮬도 같은 순서(지급 후 추첨)로 굴린다.
- **칸 성격 글자.** 약점 ×2 / 경화 ×0.5는 매 타격마다 보고 고르는 정보인데 12~16px에
  불투명도 0.7로 눌려 있어 일러스트에 묻혔다. 15~26px로 키우고 불투명도를 거의 남기지
  않았다(대비는 3겹 그림자로).
- **`칸 파괴` 표기.** 노출 900ms → 2000ms, 22~42px. 넓게 퍼지는 발광은 획을 흐리므로
  좁게 줄이고 대비는 2px 외곽선(`paint-order: stroke fill`)으로 만들었다. 추가 피해
  수치로 넘어가는 박자는 1150ms — 표기를 끝까지 기다리면 타격 하나가 4초에 가까워진다.

### v1.0.7 — 2026-08-02

**정산 화면이 모바일에서 잘리고, 보스 칸 파괴가 한 덩어리로 뭉개져 있었다.**

- **모바일 정산 화면(저택으로 버튼 잘림).** 육각형 크기를 줄이던 규칙
  (`.settlement-constellation`)이 `.experience-constellation`(min(430px, 80vw))과 같은
  특이도라 주입 순서에 밀려 **적용되지 않고 있었다** — 육각형 하나가 화면 절반을 먹어
  버튼과 통산 기록이 접힌 아래로 밀려났다. 두 클래스를 함께 쓴 선택자로 승부를 확정하고,
  헤드라인·부제·팁·스탯·버튼·간격을 한 단계씩 줄였다(≤700px, 그리고 낮은 화면은 한 번 더).
  하단 여백에 `env(safe-area-inset-bottom)`을 더해 모바일 브라우저 바 아래로도 들어가지
  않게 했다. 390×720 · 360×640 · 414×896에서 사망/클리어 모두 스크롤 없이 들어온다.
- **부위 파괴 순서.** 배율 피해 수치와 파괴 연출·추가 피해가 동시에 나가 "몇 대 맞아
  깨졌는지"가 한 덩어리로 읽혔다. 이제 배율 피해 수치 → (0.42초) 파괴 연출 + 수치 자리의
  `칸 파괴` 표기 → 부위 파괴 추가 피해 수치 순으로 **차례로** 나간다. 파괴는 수치가 아니라
  사건이므로 그 자리에 수치 대신 말을 띄운다(양식은 `damage-float` 그대로).
- **깨진 칸 표기.** 잔해 텍스처(feTurbulence 노이즈 + 재/숯 그라디언트)를 걷어내고 회색
  `파괴` 글자 하나만 남겼다. 배경 타일(220px·60px)이 칸보다 커서 모바일에서는 한 귀퉁이만
  찍혀 **잘린 얼룩**으로 보였고, 그게 무엇인지도 읽히지 않았다.
- **손패 → 칸 블라스트.** 자원 트레일(포물선 파편 세 줄기)을 그대로 쓰던 탓에 "어디선가
  흘러들었다"로만 보였다. 조준 볼트(`animateStrikeBolt`)를 따로 뒀다: 진행 방향으로 눕혀
  늘인 사각 조각이 거의 직선으로 가속해 착탄점에서 앞뒤로 눌리며 멈춘다(슈우웅 → 딱).
  꼬리는 테마 색, 머리만 흰빛이라 약점/경화 어느 칸을 노렸는지가 비행 중에도 남는다.
  전용 도형을 새로 만들지 않았다 — 같은 사각 블라스트를 눕히고 늘였을 뿐이다.
- **새싹 병아리 스폰 보정.** 적·함정 -7 / 보물·꽃 +5. 단일 출처는 `SPROUT_SPAWN_ADJUST`
  (`EmberSystem.ts`)이고 런타임과 RL 시뮬이 같은 상수를 import 한다. 직업 보정과 **다른
  축**으로 누적한다 — 한 축에 합치면 직업 선택이 난이도 보정을 덮어써 온보딩 판이 조용히
  정규 난이도로 돌아간다.

### v1.0.6 — 2026-08-02

**모바일 가로 손패 — 카드는 눌러 놨는데 글씨는 그대로라 비율이 깨져 있었다.**

v1.0.5에서 카드 높이를 19px로 못 박았지만 안쪽은 손대지 않아, 필요 높이가 카드보다
컸다: 버튼 세로 여백 8px×2 + 이름 14px(줄높이 약 17px) + 알약 여백 8px = **약 33px**.
19px 카드에 33px가 들어가니 글자가 위아래로 잘렸다.

- 짧은 화면(`max-height: 520px` 가로)에서 이름을 **최소 12px 원칙의 하한**까지 내리고
  `line-height: 1`을 함께 준다. 글꼴 크기만 줄이면 기본 줄높이가 17px로 남아 다시 잘린다.
- 버튼 세로 여백(8px×2 = 16px)을 걷어내고 이름을 가운데 정렬했다 — 19px 카드에서
  여백이 카드를 통째로 먹고 있었다.
- 카드에 `overflow: hidden`을 걸었다. 사용자 브라우저의 '최소 글꼴 크기' 설정처럼
  **CSS가 막을 수 없는 확대**가 걸려도 열이 다시 화면 밖으로 밀려나지 않고 카드 안에서
  잘린다. 브라우저 자동 확대(폰트 부스팅) 자체는 `index.html`의
  `text-size-adjust: 100%`가 이미 막고 있다.

이제 10장 전부 이름 한 줄이 온전히 읽힌다(양초의 서 / 열쇠 / 불씨 / 의식 양초 …).

### v1.0.5 — 2026-08-02

**모바일 가로에서 손패 열이 화면 밖으로 밀려나 있었다.**

- **가로 모바일 규칙이 실제 폰에서 하나도 걸리지 않고 있었다.** 손패 크기 조정 블록이
  `max-width: 760px` 조건인데, 폰을 눕히면 **폭은 844~915px**이고 짧아지는 건
  높이(약 390px)다. 세로 길이로 가르도록 바꿨다
  (`(orientation: landscape) and (max-height: 520px)`) — 데스크탑은 세로가 600px을
  넘으므로 걸리지 않는다.
- **`min-height`로는 카드가 줄지 않았다.** 카드 내용 높이가 약 58px이라 하한을 아무리
  낮춰도 소용이 없었고, `.hand-slot`이 `flex-shrink: 0`이라 압축되지도 않았다.
  10장이면 580px가 188px짜리 칸에 들어가려다 `justify-content: flex-end` 때문에
  **위로 넘쳐** 콤보 게이지·스폰 예고 띠를 덮고 화면 위로 잘려 나갔다.
  이제 짧은 화면에서는 높이를 손패 장수로 나눠 **`height`로 못 박는다**.
- 기본 `.hand-stack.is-crowded` 규칙이 클래스 4개라 3개짜리 선택자로는 그쪽
  `min-height: 58px` 하한을 못 이겼다(미디어 쿼리는 명시도를 올려 주지 않는다).
  같은 명시도로 맞춰 눌렀다.
- 짧은 화면에서 콤보 게이지가 60px(화면의 15%)를 먹던 것을 34px로 줄이고 헤더 여백도
  좁혀 손패가 설 자리를 만들었다.

  | 기기 | 이전 | 지금 |
  |---|---|---|
  | iPhone 14 가로(390px) | 카드 58px · 첫 장이 화면 위 −241px | 카드 19px · 전부 화면 안 |
  | Pixel 7 가로(412px) | 카드 58px · 첫 장 −219px | 카드 21px · 전부 화면 안 |
  | 작은 화면(360px) | 카드 58px · 첫 장 −251px | 카드 18px · 전부 화면 안 |

  손패 10장을 세로 390px에 담는 대가로 카드는 얇은 띠가 되지만, 이름 한 줄과
  등급 테두리는 그대로 읽힌다. 손패가 적을 때(6장 이하)는 예전과 같은 크기다.

### v1.0.4 — 2026-08-02

- **30F 양초 백작 체력 220 → 160**(목표 행동 13 → 9). 220은 리포트에서 실제 15~17행동이
  나와 첫 보스전치고 늘어졌다. 100까지 내리는 안도 검토했지만 칸 내구도가 12로 떨어져
  **약점 칸이 샹들리에 두 방에 깨진다** — 격자를 처음 배우는 자리에서 부위 파괴를
  배우기도 전에 끝난다. 그 사이가 160이다.

  | HP | 총 행동 | 칸 내구도 | 샹들리에 1회 비중 | 약점칸 1개 깨는 데 |
  |---|---|---|---|---|
  | 100 | 8.9 | 12 | 30% | 샹들리에 2.0회 |
  | **160** | **13.0** | **20** | **19%** | **샹들리에 3.3회** |
  | 220(이전) | 17.2 | 27 | 14% | 샹들리에 4.5회 |

  이후는 직접 플레이하며 조정한다.

- **손패 지급 검사를 절대 횟수에서 비율로 바꿨다.** "보스마다 3~16회 지급"은 전투가
  짧아지면 무조건 깨진다 — 9행동 전투에 4장을 주는 건 촘촘한 게 아니라 손이 남는 것이다.
  규칙의 의도는 "몇 행동마다 한 장인가"가 일정한 것이므로
  `targetActions / ACTIONS_PER_HAND_GIFT` 대비 비율(0.7~1.6배)로 검사한다.
  마녀의 소각 보정(지급 간격 절반)은 `HAND_BURN_RELIEF_FACTOR`/`_FLOORS`로 내보내
  검사가 같은 출처를 보게 했다.

- 에나 성향은 그대로 둔다. 바뀐 세계에서도 현 스냅샷이 기본값을 앞선다
  (검증 300시드×2정책: 기본 21.1 / 현 스냅샷 24.3). 교사 정책의 30F 격파는
  600시드 1건 → 3건으로 오히려 나아졌다.

### v1.0.3 — 2026-08-02

**밸런스 리포트가 손패 한 장을 1.8배 세게 보고하고 있었다.** 사용자가 "샹들리에가
정말 그 정도냐, 나와도 0~한두 장일 텐데"라고 짚어 다시 검산했고, 두 군데가 틀려 있었다.

- **부위 파괴 보너스를 카드 한 장에 곱하고 있었다.** `breakBonusFactor`(×1.82)는
  "전투 내내 칸을 깨며 받는 보너스"를 **누적 피해에 상각한** 배수다. 카드 한 장에
  곱하면 안 된다 — 30F 칸 내구도가 27인데 샹들리에는 칸당 3을 넣으므로 한 장으로는
  아무것도 깨지지 않는다.
  - `handCardImpact()` 신설 — 지금 한 번 썼을 때 **실제로 깎이는 체력**(보너스 없음).
  - `handCardDamage()`는 전투 기여분(상각 포함)으로 남겨 체력 역산이 계속 쓴다.
    **보스 체력은 하나도 바뀌지 않는다** — 총합에 곱하던 것을 총합에 그대로 둔 것뿐이다.
  - `BossSpecs.test.ts`의 "광역 한 장이 절반을 넘지 않는다" 검사도 1회 실효 피해로 옮겼다.

- **'샹들리에 확정' 행이 강제 카드 피해를 표에서 숨기고 있었다.** 체력에서 미리 빼는
  방식이라 표에 남는 건 *나머지* 카드의 피해뿐이었고, 그래서 확정 행의 중앙값이
  무작위보다 **낮게** 찍혔다(강제로 넣었는데 약해 보였다). 이제 손에 쥐여 주고
  그 피해도 함께 센다.

- **등장 확률을 함께 찍는다.** 한 장의 위력만 보면 "이 카드가 전투를 지배한다"로
  읽히지만 샹들리에는 3장 뽑아 **23%**, 4장 뽑아 **30%**로 대개 안 들어온다.
  드로우 풀 표에 카드마다 이 확률을 붙였다.

- 바로잡은 30F 수치(HP 220 · 공격력 4): 샹들리에 **1회 실효 30 = 체력의 14%**
  (기여분으로 세도 25%). 이전 보고의 "절반 이상"·"전투의 2/3"는 위 두 오류가 겹친
  값이었다. 리포트 하단에 1회 실효 피해와 기여분을 섞어 읽지 말라는 주석을 남겼다.
  광역 카드가 성한 칸에만 들어가 후반부에 약해지는 점(모델은 항상 전체 칸)도 함께 적었다.

### v1.0.2 — 2026-08-02

**이펙트 어휘를 사각 블라스트 하나로 되돌렸다.**

v1.0.1에서 부위 파괴에 clip-path 파편 도형을, 손패 출발에 원형 섬광을 새로 만들었다.
둘 다 "확실히 보이게"는 됐지만, 이 게임의 이펙트는 원래 `SquareBurst`(사각 블라스트)
하나로 통일돼 있다 — 전용 도형을 하나씩 늘리면 화면에 정체 모를 것이 쌓이고 톤이 갈라진다.
v1.0.1에서 걷어낸 '불씨 조각'이 바로 그렇게 생긴 것이었다.

- **부위 파괴** — 파편 도형(네 조각 clip-path + 회전 낙하)을 지웠다. 대신 같은 사각
  블라스트를 **두 겹**으로 쓴다: 큰 사각(18~38px, 가까이) = 깨진 판 덩어리 →
  한 박자 뒤 잔 사각(넓게) = 잔해. 크기와 박자만으로 '크게 깨졌다'를 말한다.
  칸이 부푸는 '팡'은 남기되 **빛만** 쓴다(도형 없음).
- **손패 출발 섬광** — 전용 원형 오버레이(`.boss-cell-volley-launch`)를 지우고 같은
  사각 블라스트를 넓고 짧게(spread 152 · 420ms) 쓰는 것으로 바꿨다.
- **탐욕의 값 표시** — 2px 테두리 선을 뺐다. 선을 그으면 '선택된 카드' UI로 읽힌다.
  안쪽으로 그늘이 차오르고 바깥으로 붉은 빛이 번지는 발광으로만 말한다.
- `CLAUDE.md`에 규칙으로 남겼다: 이펙트 어휘는 `SquareBurst` 하나이며, 표현이 부족하면
  전용 도형이 아니라 테마·개수·확산·크기·박자와 **발광**으로 조정한다.

### v1.0.1 — 2026-08-02

**보스전 연출 손질 — 무엇이 일어났는지가 안 읽히던 네 곳.**

- **'불씨 조각'을 걷어냈다.** 부위 파괴마다 발광하는 촛농 방울 12개(`.boss-cell-shard`)가
  아래로 떨어졌는데, 코드 의도는 '흘러내리는 촛농'이었지만 화면에서는 **정체 모를 불씨
  조각이 흩날리는 것**으로 읽혔다. 파괴 순간의 어휘를 아예 바꿨다.

- **부위 파괴 = 팡! 부풀었다가 판이 빠개진다.** 칸 절반을 깨면 보스가 쓰러지는 만큼
  배율 타격과 확실히 다른 어휘여야 한다. 두 박자로 나눴다:
  1. 칸이 한 번 부푼다(0.34s) — 터지기 전 예비 동작
  2. 판이 네 조각으로 갈라져 회전하며 날아간다(0.3s부터) — 되돌릴 수 없다는 표시
  - 조각 경계는 **잘린 단면의 발광**으로 읽힌다(`drop-shadow`는 `clip-path`로 잘린 실제
    모양을 따라가므로 파편마다 윤곽이 산다 — `box-shadow`는 사각형 그대로라 안 된다).
  - 몸통은 반투명이다. 불투명하게 채웠더니 칸이 갈색 사각형이 돼 일러스트가 통째로
    가렸다 — 격자에 구멍이 뚫린 것처럼 보이면 안 된다.

- **약점 리롤 — 스르륵 사라졌다가 발광하며 확대 등장.** 기존에는 그냥 위에서 아래로
  페이드였다. 이제 오므라들며 흐려졌다가, 칸마다 시차를 두고 부풀며 다시 켜진다.
  약점 칸은 그때 한 번 은은하게 달아올라 **글자보다 빛으로 먼저** 읽힌다.

- **손패 블라스트의 출발점을 보이게.** 광역 손패가 9칸을 때릴 때 트레일 9개가
  **동시에** 나가서 "그냥 모든 칸에 딜이 꽂혔다"로만 보였다. 손패가 터진 자리에 출발
  섬광을 한 번 놓고, 칸마다 70ms씩 시차를 둬 한 발씩 날아가는 게 보이게 했다.

- **탐욕의 값을 불길하게, 그리고 느리게.** 셈 110ms·징수 90ms는 눈으로 따라갈 수 없었다.
  셈 260ms·정적 460ms·징수 220ms로 늘리고, 순서를 **세기 → 값 확정 → 청구**로 갈랐다.
  세어진 동전은 핏빛 테두리로 물들고 그늘이 지며 맥동한다(`is-greed-counted`) — 값에
  포함됐다는 표시다. 몇 번째 장인지도 숫자로 올라간다.

- 되돌린 것: 세어진 동전을 `hue-rotate(-18deg)`로 붉히려 했더니 빨강(0°)이 마젠타로
  돌아 **형광 분홍 테두리**가 됐다. 색상환을 돌리지 않고 어둡게 눌러 불길함을 냈다.
  파괴 섬광도 중심을 흰빛으로 뒀더니 `screen` 합성이 R·G를 함께 밀어 올려 **형광 연둣빛**이
  됐다 — 핵을 주홍으로 낮추고 `brightness(2)` 증폭을 뺐다.

### v1.0.0 — 2026-08-02

**에나가 학습하는 세계를 지금의 보스전에 맞췄다.** 지난 다섯 버전 동안 보스 규칙을
크게 바꿨는데(모든 보스 격자 · 리미트 페이지 · 페이지별 능력 · 소환 비대화/광폭화)
학습 시뮬은 그 절반만 따라와 있었다. 관측 계약이 바뀌므로 **주 버전 변경**이다 —
저장된 구버전 정책은 `EnaPolicyStore`가 거부하고 교사 정책으로 폴백한다(재학습 필요).

- **보스 페이지 수치의 단일 출처를 만들었다 — `src/data/BossPages.ts`.**
  절반 페이지 보스 목록·페이지 경계식·조각사 비대화(+5)·마녀 광폭화(공 +3/체 +8)·
  탐욕의 값(1장당 1)·기사단장 2P 증분·기사단장/마녀 손패 수치가 여기 모였고,
  본편(`BossEvent`)과 시뮬이 **같은 상수를 import** 한다. 그동안 두 곳에 따로 적혀
  있어서 한쪽만 바꿔도 아무 검사에 걸리지 않았다.

- **시뮬 보스 행동을 런타임과 다시 맞췄다.**
  - **30F 2페이지 '탐욕의 값'** — 시뮬에는 아예 없었다. 살포도 실게임과 같은 구성으로
    바꿨다(탐욕의 동전 1~2 + 나머지 일반 드롭).
  - **60F 기사단장** — 방패 4/7·피해 2/4로 뭉뚱그린 근사를 실제 규칙으로 바꿨다:
    효과 3종에서 비복원 추출(1P 2장 / 2P 3장 전부), 수치는 2P에 +1. 카드가 전부
    발동된 뒤 돌진이 따로 들어간다.
  - **90F 조각사 · 100F 마녀 3페이지 — 종복을 실체로 넣었다.** 이전에는 보스 자가
    회복량으로 환산했는데, 그러면 "종복부터 치울까 보스를 밀까"라는 실제 선택이
    시뮬에서 사라진다. 이제 종복이 서서 매 턴 때리고, 레인 1 행동으로 치우거나 광역
    손패로 쓸어 담는다. 조각사 2P는 체력만(비대화), 마녀 3P는 체력+공격력(광폭화).
  - **마녀 3페이지 격자 축소(9칸 → 6칸)** 를 시뮬 기대값도 따라간다
    (`bossGimmickExpectation(kind, cells)`). v0.7.0에 "알려진 근사"로 남겨 둔 자리다.
  - **리미트 페이지 비용** — 경계에 닿는 것만으로 열리던 것을, 실게임처럼 부위 하나
    (칸 내구도)만큼 더 때려야 열리게 했다. 경계가 삼킨 피해는 버리지 않고 그쪽으로
    넘긴다 — 실게임에서 그 피해는 칸으로 들어간다.

- **체력을 뽑을 때 쓴 화력을 시뮬에도 실제로 넣었다(`supportStrikeChance`).**
  보스 체력은 `BossFightBudget`(공격 손패 3~4장 + `supportHits` 2)에서 역산하는데,
  시뮬에는 레시피·유물 시너지 화력이 통째로 빠져 있었다. **체력을 뽑은 세계와 에나가
  배우는 세계가 달랐던 것**이다. 행동 1회당 `supportHits / targetActions` 확률로 직접
  타격 1회분을 되돌려 넣는다.

- **에나의 인식 범위를 보스판까지 넓혔다 — `ENA_FEATURE_COUNT` 336 → 344.**
  화면에 그려져 있는데 관측에는 없던 것들을 넣었다: 페이지 게이트가 지금 피해를 막고
  있는가 / 그걸 여는 데 남은 피해 / 격자 칸 수 / 약점 조준 배율 / 다음 페이지 경계까지의
  거리 / 종복 수와 합산 위협 / 손에 쥔 탐욕의 동전이 요구할 값. 없으면 에나는 **이미
  막힌 경계를 계속 때리라고** 조언하고, 앞을 막은 종복을 없는 셈 친다.

- **교사 휴리스틱도 그 정보를 쓰게 했다.** 공격 손패를 손에 든 순서가 아니라 **가장
  크게 들어가는 것부터** 쓰고(격자에서 광역 한 장은 단일 여러 장 몫이다), 보스가
  가까우면 공격 손패를 남기고 회복을 앞당기며(체력 산출이 "손패를 들고 온전한 체력으로
  들어온다"를 전제한다), 종복이 치명적이면 먼저 치우고, 탐욕의 값이 이번 주기를 못
  버티게 할 때만 동전을 턴다(쥐고 있는 편이 대체로 싸다 — 터는 데 행동 1 + 자해 2).
  이 손질로 30F 격파 신호가 되살아났다(600시드 0 → 1건). 약한 교사에게 30F는 여전히
  드문 사건이지만, 학습 그래디언트는 살아 있다.

- **재피팅은 돌렸고 채택하지 않았다.** 이유는 `EnaDisposition.ts`의 `SIM_FITTED` 주석에
  적어 뒀다(같은 실험 반복 방지). 요약: λ∈{6,9,12}×시드 5종의 후보 15개 중 선택 시드에서
  이긴 둘이 **선택에 쓰지 않은** 2차 검증에서 도로 졌고(20.6·20.8 vs 21.3), 모든 후보가
  `predictBaseChance`를 탐색 하한으로 밀어 경험 탭 '예지' 축이 캘리브레이션 대역
  (0.45~0.55)에서 0.36~0.38로 무너졌다. 현 스냅샷은 **바뀐 세계에서도** 기본값을 크게
  앞선다(21.3 vs 19.2) — 세계가 달라졌다고 토대까지 흔들 이유가 없었다.
  다음 재피팅 전에 고칠 것: 시뮬이 예지의 값을 돌려주게 하거나 `helpCost`에서 빼기.

- 검사 6개 추가: 리미트 페이지가 경계만으로 열리지 않는가 · 2P 탐욕의 값이 장수만큼
  요구하는가 · 조각사 종복 비대화가 체력만 올리는가 · 마녀 3P 광폭화와 격자 6칸 축소 ·
  격자·게이트·종복이 관측에 실리는가 · 축소된 격자 기대값의 밀도 환산.

### v0.7.2 — 2026-08-02

- **격자 재배치를 보스별 호출이 아니라 몸집 변화 감지로 돌린다.**
  v0.7.0에서는 마녀 3페이지 전용 코드가 `resize(3, 2, ...)`를 직접 불렀다. 그러면
  "줄어들 때"만 처리되고, 몸이 돌아오거나 다른 보스가 몸집을 바꾸면 격자는 옛 모양으로
  남는다 — 칸 수와 판정이 어긋나도 화면은 멀쩡해서 묻힌다.
  - `syncGimmickGrid()` 앞에 `syncGimmickShapeToBody()`를 두어 `occupiedDistRows`(다행
    보스는 그 값, 아니면 프로필 기본 행 수)와 현재 격자 행 수가 다를 때만 다시 짠다.
  - **줄어들 때와 늘어날 때가 같은 경로다.** 6칸으로 접히면 6칸 기준으로, 9칸으로
    펴지면 9칸 기준으로 약점이 새로 노출된다. 누적 손상은 남기지 않는다 — 몸이 바뀌면
    때려 둔 부위도 함께 바뀐 것으로 읽는다.
  - 내구도는 그 시점의 **남은 체력**에서 다시 뽑아, 바뀐 몸도 그 페이지 안에서 깨진다.
  - `performWitchSummonToBack()`의 전용 `resize()` 호출은 지웠다. 이제 새 변신 보스는
    `occupiedDistRows`만 바꾸면 격자가 따라온다.
  - 몸집이 바뀐 순간은 알림으로 남긴다(`부위 9칸 → 6칸, 약점 재배치`) — 조용히 바뀌면
    방금 때린 칸이 왜 사라졌는지 알 수 없다.
  - 검사: 6칸으로 줄인 뒤 **다시 9칸으로 늘리는** 경우도 새로 굴려지고 내구도가 남은
    체력에서 파생되는지 확인한다.

### v0.7.1 — 2026-08-02

- **조각사 2페이지를 바로잡았다 — 부푸는 것은 본체가 아니라 소환된 양초 조각들이다.**
  v0.7.0에서 조각사 본체를 비대화시키고 종복에게 광폭화를 걸었는데, 의도는 반대였다.
  - 이제 **소환되는 양초 조각이 비대화**된다(체력 +5). 조각사 본체 연출은 되돌렸다.
  - **비대화와 광폭화는 다른 어휘로 갈랐다.** 광폭화는 붉은 잔상 + 공격력 강조(마녀),
    비대화는 붉게 달구지 않고 **몸집**으로만 말한다 — 카드 그림이 커지고 느리게
    맥동하며 체력 칩이 강조된다(`Card.swollen` → `.is-swollen`). 섞으면 무엇이 오른
    강화인지 안 읽힌다.
  - 부푼 상태는 다음 턴에도 남는다(1회성 연출이 아니라 카드 상태다).

- **체력 곡선의 남은 두 곳을 블렌딩했다.**
  - **90F 평지 제거.** 60F 450 → 90F 550은 ×1.22로 거의 평지였다. 원인은 조각사 격자가
    6칸이라 부위 파괴 배수가 낮고(×1.43 vs 9칸 ×1.82), 공격력 가정 7→9의 성장이 그
    차이에 먹혀 **1타 피해가 25.5 → 25.7로 붙어 버린** 것이다. 90F 공격력 가정을 10,
    100F를 12로 올려 풀었다.
  - **악마가 최종 보스보다 긴 싸움이던 문제.** 프리미엄 1.35가 목표 행동 수에 곱해져
    악마 33행동 vs 마녀 28행동이었다 — 중간에 끼어드는 선택 보스가 최종전보다 길면
    안 된다. 기준 행동 수를 18 → 14로 낮추고 프리미엄도 1.35 → 1.15로 줄였다.

  | 보스 | 체력 | 상승폭 | 전투 길이 |
  |---|---|---|---|
  | 새싹 양초 고양이 | 120 | – | 10.7 |
  | 30F 양초 백작 | 220 | ×1.83 | 15.2 |
  | 60F 불씨 기사단장 | 450 | ×2.05 | 18.2 |
  | 90F 밀랍 조각사 | 550 → **610** | ×1.36 | 22.2 |
  | 100F 녹지 않는 마녀 | 1090 → **1190** | ×1.95 | 28.0 |
  | 악마(30/60/90턴) | 360 / 600 / 740 | – | 20 / 24 / 26 |

  검사도 함께 늘렸다: 표시 체력 단조 증가, 층 간 상승폭 1.3배 이상(평지 감지),
  악마 전투가 최종 보스보다 길지 않을 것.

- **악마 턴에도 박자를 넣었다.** 30F에서 다듬은 순차 연출(특징 → 한 박자 → 타격)이
  악마만 빠져 있었다. 검은 양초 → 거짓과 진실 → 타격이 각각 별개의 사건으로 읽힌다.
  이로써 **여섯 보스 전부**가 같은 보스전 어휘를 쓴다: 슬램 타격(들어올림→당김→쾅) ·
  칸 블라스트 · 칸 위 피해 수치 · 피격 반동 · 배율 리롤 · 페이지 열기 필터 ·
  HP바 경계선. 보스별로 따로 붙이는 것이 아니라 같은 경로를 지나므로, 새 보스도
  격자만 켜면 그대로 따라온다.

### v0.7.0 — 2026-08-02

**모든 보스에 약점 칸.** 격자는 30F와 새싹 고양이에만 있었다. 이제 여섯 보스 전부가
칸 단위로 맞고, 그에 맞춰 체력 곡선이 다시 계산됐다.

- **격자 행 수를 보스가 실제로 점유하는 레일 행 수에 맞췄다.** 레일 1행을 CSS로 3행처럼
  늘려 그리는 보스(백작·고양이·기사단장·마녀·악마)는 3×3(9칸), 레일 2행을 진짜로
  차지하는 조각사는 2×3(6칸)이다. 화면의 몸집과 판정 칸이 어긋나면 어디를 때리는지가
  안 읽힌다.

  | 보스 | 격자 | 약점 | 경화 | 성격 |
  |---|---|---|---|---|
  | 30F 양초 백작 | 3×3 | 2 | 2 | 기준형 |
  | 새싹 양초 고양이 | 3×3 | 3 | 1 | 처음 배우는 자리라 관대 |
  | 60F 불씨 기사단장 | 3×3 | 2 | 3 | 방패 보스라 경화가 많다 |
  | 90F 밀랍 조각사 | 2×3 | 2 | 1 | 칸이 적어 한 칸이 무겁다 |
  | 100F 녹지 않는 마녀 | 3×3 | 2 | 3 | 최종 보스 |
  | 악마(이벤트) | 3×3 | 2 | 2 | 등반 보스와 같은 형태 |

- **다행 보스 렌더링을 열었다.** `occupiedDistRows ≥ 2`인 보스는 레일이 행마다 별도
  타일을 그려서 지금까지 격자를 켤 수 없었다(`CLAUDE.md`가 경고로 막아 두던 자리다).
  격자 뷰에 `tileRows`·`startDistance`를 실어 **타일마다 격자 한 행씩** 그리게 했다.
  실측: 조각사가 격자 2개 × 3칸 = 6칸으로 정확히 나뉘어 렌더된다.
  - 1회성 연출 플래그(등장·리롤)는 이제 **렌더 1회 단위**로 소비한다. 타일마다
    소비하면 첫 행만 연출을 먹고 나머지 행이 굳는다.

- **전투 중 격자 축소 — 마녀 3페이지.** 3×3 몸이 2×3으로 접히면 판정 격자도 함께
  접힌다(`resize()`). 칸은 **새로 굴린다** — 몸이 바뀌었으니 부위도 새로 나고, 남은 칸
  수에 맞춰 약점이 다시 배치돼야 "재세팅"이 읽힌다.
  - 내구도는 **남은 체력**에서 다시 파생한다. 최대 체력 기준을 그대로 쓰면 마지막
    페이지에서 깰 수 없는 칸이 된다 — "칸 절반을 깨면 쓰러진다"가 그 페이지 안에서도
    성립해야 한다.
  - 특수 칸 수는 같은 밀도로 환산한다(9칸의 약점 2 → 6칸에서 1). 그대로 얹으면 접힌
    몸이 오히려 물러진다.

- **체력 곡선 재계산 — 그리고 표시 순서가 저절로 풀렸다.** 격자가 붙으면 손패가 칸
  단위로 증폭돼 같은 전투 길이에 훨씬 두꺼운 체력이 필요하다(v0.5.0의 모델 그대로).

  | 보스 | 체력 | 목표 행동 |
  |---|---|---|
  | 새싹 양초 고양이 | 120 (유지) | 11 |
  | 30F 양초 백작 | 220 (유지) | 13 |
  | 60F 불씨 기사단장 | 120 → **450** | 16 |
  | 90F 밀랍 조각사 | 190 → **550** | 20 |
  | 100F 녹지 않는 마녀 | 300 → **1090** | 26 |
  | 악마(60턴) | 220 → **830** | 31 |

  v0.5.0에서 "격자 보급이 끝나기 전에는 표시 단조성과 전투 길이를 동시에 만족시킬 수
  없다"고 적어 둔 간극이 **이 변경으로 사라졌다.** 이제 층이 오를수록 화면 숫자도
  같이 오른다(120 → 220 → 450 → 550 → 1090).

- 곡선 불변식 검사를 늘렸다: 모든 보스에 프로필이 있는가(새 보스에서 빠뜨리면 잡힌다),
  칸 수가 6 또는 9인가, 특수 칸이 격자를 다 먹지 않는가, 축소 시 내구도가 남은 체력에서
  파생되는가.

- **알려진 근사**: 밸런스 모델(`BossDamageBudget`)은 마녀의 3페이지 격자 축소를 반영하지
  않고 기본형(9칸)으로 계산한다. 축소 뒤 구간이 전체 전투의 1/3이라 1차 값으로 둔다.

### v0.6.0 — 2026-08-02

**모든 보스가 2페이지를 갖는다.** 지금까지 페이지는 30F·고양이·마녀·악마만 있었고,
기사단장과 조각사는 처음부터 끝까지 같은 패턴이었다.

- **절반 HP 페이지를 기사단장·조각사까지 확장.** `HALF_PAGE_BOSSES` 한 줄에 이름을
  넣으면 붙는다. 격자가 켜진 보스(백작·고양이)는 그 자리에서 **부위를 하나 더 깨야**
  열리는 리미트가 되고, 격자가 없는 보스(기사단장·조각사)는 닿는 순간 열린다 —
  요구 조건은 `bossPageGate()`가 격자 유무로 자동으로 고른다.
  - 상태 이름을 `cellPage` → `halfPage`로 바꿨다. 이제 격자 전용이 아니다.
  - 전환 순간 1회성 능력은 `applyHalfPageTwoEntry()` 한 곳에 모았다. 매 턴 도는 능력은
    각자의 턴 처리에 남는다 — 둘을 섞으면 "언제 걸리는 능력인지"가 흐려진다.

- **새싹 양초 고양이 — 페이지로 성격이 뒤집힌다.**
  - 1페이지: 공격 주기마다 손패를 **한 장 준다**. 온보딩 보스라 첫 보스전에서 손이
    마르지 않게 하는 배려이고, "장난치며 갖고 논다"는 성격에도 맞는다.
  - 2페이지: 반대로 **한 장 가져간다**(기존 강탈). 준 것을 도로 뺏는 전환이 그대로
    페이지의 체감이 된다. 촛농·양초·불씨를 삼키면 회복하는 규칙은 그대로다.
  - 실측: 1페이지에서 손패 4→10장으로 차오르고, 2페이지 진입 뒤 10→8로 줄어든다.

- **60F 불씨 기사단장 2페이지 — 손패 3장 + 각 수치 +1.** 1페이지는 그대로다.
  효과가 3종뿐이라 3장이면 방패·회복·타격이 전부 나온다.

- **기사단장·마녀: 카드가 전부 발동된 뒤에 때린다.** 마지막 카드의 퇴장과 돌진이
  붙어 있어 거의 동시에 일어난 일로 읽혔다. 30F에 넣었던 것과 같은 `BOSS_TURN_BEAT_MS`
  한 박자를 사이에 넣었다.

- **90F 밀랍 조각사 2페이지 — 비대화 + 종복 광폭화(체력 +5).** 전환 순간 필드의 모든
  종복에게 소급 적용하고(시련 '광란'과 같은 경로라 합체 적·이미 입은 피해가 알아서
  맞는다), **그 뒤 새로 부르는 종복도 광폭화된 채로 나온다** — 1회성 버프로 두면 다음
  소환에서 판이 도로 가벼워진다.
  - 조각사 본체는 일러스트가 부풀어 오른다(느린 맥동). 타일 크기는 레일이 정하므로
    일러스트만 키운다. 보스별 연출을 고를 수 있게 face에 `data-boss-kind`를 실었다 —
    렌더러에 보스별 분기를 늘리지 않기 위해서다.

- **100F 마녀 3페이지 광폭화 수치를 공격력 +3 / 체력 +8로.** 기존 공격력 +3 / 체력 +10.
  상수로 뽑아 소환 코드와 특징 문구가 같은 값을 본다.

- **특징 카드 정정.** 마녀 1페이지가 "체력을 10 잃을 때마다 손패 2장 소각"으로 적혀
  있었으나 실제 코드는 **공격 주기마다**다. 문구를 코드에 맞췄다. 고양이·기사단장·
  조각사도 새 2페이지를 목록에 넣었다.

- **학습 시뮬 동기화.** 60F·90F 프로필에 절반 HP 경계를 넣고, 기사단장 2페이지(방패
  4→7 · 피해 2→4)와 조각사 2페이지(자가 회복 4→7 — 소환 모델이 없어 광폭화를 환산),
  고양이 1페이지 카드 지급을 반영했다.

### v0.5.0 — 2026-08-02

**밸런싱 기준 자체를 다시 세웠다.** v0.4.2의 식은 `공격력 × 타수`뿐이라 실제 전투와
크게 어긋나 있었다 — 플레이어는 직접 타격만 하지 않는다.

- **`npm run balance:boss` — 보스전 화력 분포 리포트 추가**
  (`src/tools/boss-balance-report.ts`). 실제 데이터 테이블에서 보스전 총 화력을
  몬테카를로로 뽑아 보스마다 **필요한 행동 수의 p10/중앙/p90**을 찍는다.
  샹들리에 유무를 나눠 찍는 것이 핵심이다 — 중앙값만 보면 그 폭이 안 보인다.
- **`BossDamageBudget` 신설 — 체력 산출과 리포트가 같은 모델을 본다.**
  검사용 사본을 따로 두면 그 사본이 먼저 썩는다(`Unmelting_Dev_Upgrade.md` §2 배울 점 1).

- **리포트가 드러낸 것: 기존 식이 격자 보스를 3~4배 과소평가했다.**
  구식(HP 100)에서 30F는 목표 13타인데 실제로는 **중앙 4.4행동**이면 끝났다.
  손패가 격자에서 곱절로 증폭되는 걸 식이 전혀 세지 않았기 때문이다:
  - 광역 손패 1장 = 성한 칸마다 한 번씩 → 3×3이면 ×10 남짓
  - 조준 단일 손패 = 약점 칸을 골라 맞히므로 ×2
  - 부위 파괴 보너스 = 쌓인 칸 피해에 비례해 ×1.82
  - 샹들리에(항상 해금, 드로우 8.5%) 한 장이 HP 100의 **55%**를 혼자 지웠다.

- **새 식** — 전투 가정 네 수에서 체력을 역산한다.
  ```
  체력 = 손패 3~4장의 기대 피해 + (목표 행동 수 − 손패 장수 + 지원 화력 환산) × 직접 1타
  ```
  `playerAttack` / `targetActions` / `handCards` / `supportHits` 넷만 고치면 곡선 전체가
  따라 움직인다. 레시피·유물 시너지는 퍼센트로 숨기지 않고 `supportHits`(추가 직접 타격
  환산, 등반 보스 2 / 온보딩 0)로 **눈에 보이게** 적었다.

  | 보스 | 체력 | 손패 지급 간격 | 목표 행동 |
  |---|---|---|---|
  | 새싹 양초 고양이 | 60 → **120** | 10 → **15** | 11 |
  | 30F 양초 백작 | 100 → **220** | 30 (유지) | 13 |
  | 60F 불씨 기사단장 | 110 → **120** | 30 (유지) | 16 |
  | 90F 밀랍 조각사 | 180 → **190** | 35 (유지) | 20 |
  | 100F 녹지 않는 마녀 | 290 → **300** | 25 (유지) | 26 |
  | 악마(60턴) | 190 → **220** | 20~30 | 31 |

  격자 없는 보스(60/90/100F)는 거의 그대로다 — 구식이 이미 그쪽에는 맞았다는 뜻이다.
  **바뀐 건 격자 보스 둘뿐이고, 그게 정확히 틀렸던 지점이다.**

- **알려진 간극: 30F(220)가 60F(120)보다 화면 숫자가 크다.** 같은 전투 길이를 유지하려면
  격자 보스는 체력이 3배 이상 두꺼워야 해서, 30F만 격자가 켜진 지금은 **표시 단조성과
  전투 길이를 동시에 만족시킬 수 없다.** 전투 길이를 택했다 — 숫자 순서는 보기 문제지만
  전투 길이는 재미 문제다. 60F·100F에도 격자를 켜면 자연히 풀린다(90F는 행별 오프셋이
  먼저 필요하다). 그래서 곡선 검사도 **체력 단조가 아니라 전투 길이 단조**로 바꿨다.

- **새싹 고양이 목표 행동을 9 → 11로.** 9에서는 샹들리에 한 장이 전투의 52%를 지워
  부위 파괴를 배우기 전에 끝났다. 격자를 처음 가르치는 자리라 한 장으로 무너지지 않을
  만큼만 늘렸다. 이 성질은 `BossSpecs.test.ts`가 "광역 1장 < 체력 50%"로 못 박는다.

- **악마 프리미엄을 체력이 아니라 목표 행동 수에 곱하도록 고쳤다.** 체력에 곱하면 손패
  피해가 함께 커지지 않아 실제 전투가 의도(1.35배)보다 더 길어진다(측정 1.47배).

- **에나 재피팅은 돌렸으나 채택하지 않았다.** held-out 300시드×2정책에서 기본 23.7 /
  기존 스냅샷 **26.5** / 재피팅 26.1로, 기존 스냅샷이 더 나았다. 규칙대로 나은 쪽만
  남긴다. 새 곡선에서도 기존 토대가 그대로 잘 일반화한다는 뜻이다.

### v0.4.2 — 2026-08-02

보스 체력 곡선 1차 재산정 + 에나 자아 재동기화.

- **보스 체력·손패 지급을 하나의 식에서 뽑는다.** 30F에 격자가 붙으며 체력이 50→100이
  됐는데 60F가 80이라, **층을 올라갈수록 화면의 숫자가 내려가는** 상태였다. 감으로 적힌
  수치들을 `BossSpecs`의 두 식으로 통일했다.
  ```
  maxHp        = 층 예상 공격력 × 목표 타수 × 격자 배수
  handGiftStep = 층 예상 공격력 × 격자 배수 × 4     (대략 4타마다 손패 1장)
  ```
  - **격자 배수 2**: 칸 기믹 보스는 약점 칸(×2)을 골라 때리므로 체력을 두 배로 잡아야
    체감 타수가 격자 없는 보스와 같다. 광역 손패가 칸마다 꽂히는 이득은 보상으로 남는다.
  - **층 예상 공격력**의 30F 값(4)은 헤드리스 시뮬 실측(진입 시 평균 4.5)에서 나왔다.
    이후 층(7·9·11)은 유물·강화팩 성장을 반영한 **1차 가정**이며, 실측이 쌓이면 이 표부터
    고친다. 이 식이 30F의 기존 수치(100/30)를 그대로 재현한다는 점으로 한 번 검증했다.

  | 보스 | 체력 | 손패 지급 간격 | 지급 횟수 |
  |---|---|---|---|
  | 새싹 양초 고양이 | 30 → **60** | 5 → **10** | 6 |
  | 30F 양초 백작 | 100 (유지) | 30 (유지) | 3.3 |
  | 60F 불씨 기사단장 | 80 → **110** | 15 → **30** | 3.7 |
  | 90F 밀랍 조각사 | 130 → **180** | 15 → **35** | 5.1 |
  | 100F 녹지 않는 마녀 | 270 → **290** | 15 → **25** | 11.6 |
  | 악마(이벤트) | 130+턴 → **110+턴×1.4** | 15 → 20~30 | ~7 |

  - 100F만 지급 간격을 식의 절반으로 좁힌다 — 마녀는 공격 주기마다 손패 2장을 태우므로
    식대로면 태우는 속도를 못 따라가 손이 빈 채로 3페이지를 맞는다. **소각형 보스 전용
    보정**이며 다른 보스에는 적용하지 않는다.
  - 온보딩 고양이만 지급 간격을 식(24) 대신 손으로 10에 둔다 — 처음 겪는 부위 파괴에서
    손이 비면 배우기 전에 진다.
  - 60F·90F는 지급 횟수가 8.7 → 5.1처럼 줄어든다. 체력만 올리고 지급을 두면 후반 보스가
    손패 공장이 되기 때문인데, **1차 값이라 실플레이에서 부족하면 먼저 되돌릴 자리다.**
- **100F 페이지 경계를 최대 체력에서 파생**(2/3 · 1/3, `waxWitchPageFloors()`). 180/90이
  상수로 박혀 있어 체력을 옮기면 경계가 엉뚱한 자리에 남았다. 학습 시뮬도 같은 비율이다.
- **에나 자아 재동기화.** 보스 수치가 바뀌면 시뮬 세계가 바뀌므로 기본 성향
  (`SIM_FITTED`)을 다시 피팅했다. 옛 세계에서 맞춘 토대를 두면 에나는 존재하지 않는
  게임에 맞춰 돕는다.
  - 피팅에 쓰지 않은 **held-out 300시드 × 2정책** 검증: 기본 26.4 / 이전 스냅샷 29.4 /
    새 스냅샷 **29.9**. 채택.
  - 방어·회복 가중이 오르고 자원·청소 가중이 내려갔다. 새 곡선에서 보스전이 길어져
    버티는 축의 값이 커진 결과로 읽힌다.
  - `ExperienceAxes` 캘리브레이션 검사 하나가 걸렸다. 각성(불굴)이 올라 '가까운 한 쌍'이
    온정·불굴에서 수호·불굴로 옮겨 간 것이라, **특정 쌍을 고정하던 단언을 의도(축이 서로
    붙지 않는다)대로 다시 썼다** — 임계값만 늘려 통과시키지 않았다.

### v0.4.1 — 2026-08-02

v0.4.0 페이지 시스템의 마감. 새로 만든 규칙이 화면에서 읽히게 하는 쪽이 대부분이다.

- **리미트 경고를 피해 수치와 같은 양식으로.** 문구를 `칸을 파괴해야합니다.`로 정하고,
  전용 알약 배지를 버린 뒤 `damage-float` 변형(`--gate`)으로 바꿨다. 이 문구는 피해
  수치가 **뜰 자리에 대신** 뜨는 것이라 움직임·위치가 같아야 "원래 수치가 나올 자리"임이
  읽힌다. 색만 밀랍 호박빛으로 갈고, 읽어야 하는 문장이므로 980ms → 1800ms(도달 1회는
  2600ms)로 늘렸다.
  - `animateDamageNumberAt`을 `animateFloatTextAt`으로 일반화해 수치와 문구가 같은
    구현을 쓴다. 양식을 두 벌로 두면 다음에 한쪽만 손대게 된다.
- **30F 특징 카드를 다른 보스 양식에 맞췄다.** 2페이지가 생겼으므로 100F·악마와 같은
  `첫 번째 :`/`두 번째 :` 목록으로 바꾸고, 꾸밈용 긴 대시를 뺐다. 리미트 조건도 한 줄로
  적어 인트로만 보고도 무엇을 해야 하는지 알 수 있게 했다.
  - 90F 특징만 존댓말이라 어미가 튀어 있었다 — 다른 보스와 맞췄다.
- **HP바에 30F 페이지 경계선.** 경계 표기는 이제 보스 컨트롤러가 정하고
  (`bossPageMarkers()`) 렌더러는 그리기만 한다 — 페이지 규칙의 출처가 둘로 갈리지 않게.
  이미 뚫린 경계는 점선으로 남아 "여기는 지났다"와 "남은 페이지가 몇인지"를 함께 보인다.
  - **통짜 세로선으로 그렸다가 다시 만들었다.** 30F는 경계가 정확히 50%라 가운데
    HP 숫자(`50 / 100`의 슬래시)와 겹쳐 선이 통째로 사라졌다. 막대는 `overflow: hidden`
    이라 밖으로 뺄 수도 없어, 위아래 짧은 눈금 한 쌍으로 바꾸고 가운데를 비웠다.
- **페이지가 오른 보스는 카드에 열기가 돈다.** 2페이지부터 호박빛 열기 레이어가
  얹히고 3페이지에서 짙어진다(느린 일렁임 포함). 수치를 읽지 않아도 "강해졌다"가
  보이게 하는 장치다. 이벤트로 불려 나온 악마만 보랏빛 — 출신이 다르다는 표시다.
  일러스트를 덮지 않도록 더하는 빛(`screen`)만 쓴다.
- **30F 2페이지 전환 대사** 추가. 백작이 플레이어를 다시 보는 순간이다.
- **새싹 30F 미니보스(양초 고양이)에도 같은 틀을 적용.** 격자(약점 3 / 경화 1 — 처음
  겪는 자리라 더 관대하게)와 절반 리미트 페이지, 경계선, 열기 필터가 함께 켜진다.
  **2페이지 고유 능력은 아직 비어 있다**(전환만 성립). 학습 시뮬 새싹 아크도 같은
  경계·격자 기대값을 쓴다.
  - 페이지 상태는 보스 종류가 아니라 **격자가 켜졌는지**로 갈린다(`cellPage`).
    다음에 격자를 켜는 보스는 리미트 페이지를 자동으로 물려받는다.

### v0.4.0 — 2026-08-02

30F 보스전을 "약점만 긁는 진행"에서 "칸을 고르고 부수는 진행"으로 바꾸는 묶음이다.
다섯 변경이 하나의 루프를 이룬다: 배율이 계속 옮겨 다니고(리롤), 절반에서 한 번
반드시 부숴야 하며(리미트 페이지), 그 대가로 보스의 반격이 순서대로 무겁게 온다.

- **칸 배율을 타격마다 다시 굴린다.** 배율이 고정이면 약점 한 칸만 파먹는 게
  최적해가 되고, 부위 파괴만으로는 그 유혹을 절반밖에 못 막았다. 이제 타격 연출이
  끝날 때마다 성한 칸의 종류만 다시 섞이고(`BossGimmickManager.rerollKinds()`),
  **누적 손상과 파괴 상태는 그대로 남는다** — "때려 둔 칸을 마저 깨서 파괴 보너스를
  받을지, 새로 뜬 약점을 노릴지"가 매 턴 선택으로 돌아온다.
  - 깨진 칸은 후보에서 빠지므로 특수 칸 수는 성한 칸 수에 맞춰 자연히 줄어든다
    (9칸 프로필의 특수 4칸을 남은 3칸에 그대로 얹지 않는다).
  - 화면은 두 박자다: 리롤 직전에 지금 표기를 사그라뜨리고(`.is-relabel-out`),
    다음 렌더에서 새 배율이 떠오른다(`.is-relabeling`). 모델을 먼저 굴려 버리면
    배율이 툭 바뀌어 **무엇이 달라졌는지 읽히지 않는다**.
  - 리롤은 피해 수치 연출이 **끝난 뒤** 돈다. 먼저 돌면 방금 어느 배율로 맞았는지가
    화면에서 사라진다. 손패 경로는 `setBossCellStrikeSource`의 두 번째 인자
    (연출 완료 콜백)로 같은 순서를 보장한다.

- **30F 리미트 페이지 — 절반에서 부위를 부숴야 뚫린다.** 절반 HP(50)에 닿으면 HP가
  더 내려가지 않고, 그 뒤 부위를 하나 더 깨야 페이지가 열린다. 막혀 있는 동안에는
  피해 수치 대신 `부위를 부숴야 한다` 경고가 은은하게 떴다 사라진다(막힌 것은
  사건이 아니라 상태라 수치처럼 튀어 오르지 않는다).
  - **모든 보스의 페이지 경계를 한 창구로 모았다**(`bossPageGate()`): 하한(floor) +
    요구 조건(requirement)의 쌍이다. `'none'`은 닿는 순간 스스로 열리고(기존
    100F 마녀 180/90, 악마 65%), `'cell-break'`는 부위 파괴를 요구한다. 새 보스에
    리미트를 붙이려면 분기 한 줄과 전환 처리 하나면 된다.
  - 요구 조건에 `'cell-break'`를 쓸 수 있는 것은 **격자가 있는 보스뿐**이다. 지금
    격자가 켜진 보스는 30F 하나라, 다른 보스는 하한만 같은 창구를 지나고 전환 조건은
    기존 그대로다. 90F처럼 `occupiedDistRows ≥ 2`인 보스는 격자부터 열어야 한다.
  - 깰 칸이 하나도 안 남은 상태에서 리미트에 닿으면 잠그지 않고 연다 — 요구 조건이
    성립하지 않는데 잠그면 진행이 막힌다.
  - 학습 시뮬도 30F에 같은 경계를 넣었다(`pages: [maxHp/2, 0]`). 칸이 없어 파괴
    요구는 옮길 수 없지만, 경계에서 반격 주기가 초기화되는 리듬은 같아진다.

- **모든 보스의 직접 타격을 '들어올림 → 뒤로 당김 → 쾅'으로 통일**
  (`animateBossSlamAttack`). 일반 적의 돌진은 평면을 미끄러지는 움직임이라 거대한
  보스 타일이 쓰면 무게가 없었다. `perspective`로 깊이를 주고 **한 번 화면 안쪽으로
  물러났다가**(작아진다) 앞으로 튀어나오며(커진다) 박는다 — 물러남이 있어야 '쾅'이
  선다. 착지 프레임에 화면이 짧게 눌리고, 그 순간 피해 수치가 뜬다(반동을 기다리지
  않는다). 소환 적은 기존 `animateEnemyAttacks` 그대로다.

- **30F 보스 턴을 순차로 나눴다.** 살포와 타격이 거의 동시에 나가 무슨 일이
  일어났는지 남지 않았다. 이제 세 박자다(사이 간격 `BOSS_TURN_BEAT_MS`):
  1. 보스가 **화면 가득 금화를 흩뿌리고**(포물선 + 여분 금화), 흩어진 채로 머문다.
  2. 그 금화가 하나씩 **짤랑이며 손패 슬롯으로 빨려 들어간다**.
  3. (2페이지) 탐욕의 값 → 타격.

- **30F 2페이지 '탐욕의 값'.** 손에 쥔 `탐욕의 동전` 1장당 1피해. 동전이 순서대로
  떠올라 개수를 세고(웨이브), 그 자리에서 플레이어에게 한 발씩 꽂힌다. 쓰면 이득이고
  쥐고 있으면 매 주기 값을 치르는 이지선다가 목적이다.
  - 동전 소리 전용 소스가 없어 클릭음을 높은 음정으로 굴려 쓴다(`sfx.playCoin()`).

### v0.3.7 — 2026-08-01

- **깨진 칸을 연하게 — '꺼짐'만 알리고 일러스트는 살린다.** 그을음을 어둠으로 덮으니
  격자에 구멍이 뚫린 것처럼 보였다. 대비를 **밝기가 아니라 채도**로 낸다:
  `grayscale(0.72) brightness(0.82)` + 아주 옅은 그을음(불투명도를 전반적으로 1/3 수준
  으로 낮춤). 색이 빠진 것만으로 '이 칸은 끝났다'가 충분히 읽히고, 보스 그림은 그대로
  보인다. 노이즈 텍스처는 남기되 질감 힌트 수준(0.08)으로만 얹는다.

### v0.3.6 — 2026-08-01

- **깨진 칸에 그을음 노이즈 텍스처.** 평평한 그라디언트라 "탄 자국"이 아니라
  "UI가 어두워진 것"으로 보였다. 노이즈는 gradient로 흉내 낼 수 없어 이 자리에만
  `feTurbulence` SVG를 쓴다(큰 숯 얼룩 1장 + 고운 재 알갱이 1장).
  - **세 번 헛디뎠고 셋 다 이유가 다르다.** 남겨 두는 이유는 다음에 노이즈를 쓸 때
    같은 함정을 다시 밟지 않기 위해서다.
    1. `background-repeat`를 레이어별로 안 적어 부모의 `no-repeat`를 물려받았다 —
       노이즈가 칸 왼쪽 위에 **한 장만** 찍히고 나머지는 매끈했다.
    2. `overlay` 합성으로 진하게 깔았더니 중간 회색이 끌어올려져 깨진 칸이 이웃보다
       **더 밝아졌다**(TV 스노우). 어둠이 먼저 오고 노이즈는 질감만 얹어야 한다 —
       `soft-light` + 농도 0.12로 낮췄다.
    3. `feTurbulence` 원본은 RGB 채널이 따로 노는 **컬러 노이즈**다. 그냥 쓰면
       색 점이 튄다. `feColorMatrix type='saturate' values='0'`으로 흑백화하고,
       `numOctaves`를 3 → 2로 줄여 잔알갱이 대신 큰 얼룩이 남게 했다.

### v0.3.5 — 2026-08-01

- **부서진 칸을 눌러도 성한 칸이 대신 맞던 버그 수정.** 깨진 칸에 `pointer-events: none`
  + `disabled`를 걸어 뒀더니 클릭이 **아래 보스 타일로 새어** `cellIndex`가 undefined가
  되고, 모델이 중앙의 성한 칸으로 접어 그 칸을 때렸다. 화면상 "죽은 칸을 눌렀는데 왜
  피해가 들어가지" 상태였다.
  - 칸이 클릭을 **받되 삼키도록** 바꿨다(`data-broken="1"` + 클릭 핸들러에서 조기 반환).
    비워 두면 새고, 막으려면 받아서 버려야 한다.
- **직접 타격에서 발사체(블라스트)를 뺐다.** `animatePlayerAttack`의 돌진이 이미
  "내가 가서 때렸다"를 말하는데 그 위에 블라스트를 얹으니 원거리 사격처럼 읽혔다.
  이제 일반 레일 공격과 같은 어휘 — 돌진 + 칸 버스트 + 칸 위 수치.
  손패는 반대로 화면 중앙에서 칸으로 날아가는 블라스트가 맞으므로 그대로 둔다.
- **부서진 칸을 덜 검게.** 배경 0.66 → 0.42, 안쪽 그림자 0.78 → 0.48,
  `grayscale(1)` → `0.8`, 잔해 그라디언트도 함께 낮춰 일러스트가 비쳐 보이게 했다.
  완전히 까맣게 죽이면 격자에 구멍이 뚫린 것처럼 보인다.

### v0.3.4 — 2026-08-01

- **손상 단계를 '탄 영역이 안쪽으로 파고드는' 안에서 원래의 '달아오름'으로 되돌렸다.**
  - 되돌린 이유: 아트 디렉션 판단. 잉걸불이 칸 전체로 번지는 쪽이 촛불 톤에 더 붙고,
    번짐의 세기만으로도 남은 내구도가 충분히 읽힌다. 탄 영역을 밀어 넣는 안은
    "타 들어간다"가 더 정확하긴 했지만 화면에서 도형(주홍 링)처럼 보였다.
  - 같은 시도를 두 번 하지 않도록 코드 주석에도 남겨 뒀다.

### v0.3.3 — 2026-08-01

- **부위 손상 표현을 유리 파편 → 촛농·그을음으로 교체.** little_token의 표현을 기법째
  가져왔더니 "깨진다"가 이 게임의 어휘가 아니었다. 다섯 물질이 촛불·양초·밀랍·불씨·
  제물이고 보스는 밀랍 덩어리인데, 유리처럼 쪼개지면 톤이 어긋난다.
  - 균열(뾰족한 흰 선) → 윗변 그을음 + 흘러내리는 촛농. 파괴 순간의 clip-path 다각형
    모프 + 방사형 파편 → 달궈진 밀랍판이 처지며 아래로 떨어지는 촛농 방울.
  - **두 번 갈아엎었다.** 처음엔 흘러내리는 촛농으로 그렸는데, 1920×1080으로 띄워
    보니 보스 일러스트 자체가 온통 흘러내린 밀랍이라 손상 표시가 그림에 묻혀 아무것도
    안 보였다. 밝은 밀랍 위에서 대비를 내는 건 '녹는 밝음'이 아니라 **'타 들어간 어둠'**
    이다 — 불씨가 양초를 태우는 게 이 게임의 어휘다. 가장자리부터 검게 번지고 탄
    경계에 잉걸불이 남는 쪽으로 다시 만들었다.
  - 무너지는 순간도 처음엔 불투명하게 채워 칸이 통째로 **노란 사각형**이 됐다.
    `mix-blend-mode: screen`으로 바꿔 칠하는 대신 빛을 더하게 했고, 가운데를 비운
    radial-gradient로 가장자리만 타오르게 했다.
  - 이 교체로 CSS 안의 SVG 데이터 URI가 0개가 됐다(전부 gradient).
  - 파편 버스트 톤도 `wax-freeze`(차가운 유리) → `boss-wax-drip`.
- **칸 타겟팅 상태 정리** — 칸을 더 밝히는 대신 주변을 눌러 대비를 만든다.
  - 조준 발광을 얇게 낮추고 맥동을 1.25s → 2.4s로 늦췄다. 아홉 칸이 한꺼번에 화려하게
    빛나면 오히려 어디를 고를지 안 보인다.
  - 보스 일러스트를 어둡게(`brightness(.34)`), 이름·HP바·ATK칩·턴 뱃지·굳음 표기를
    뒤로 밀고(`opacity: .22`), 이미 깨진 칸은 더 가라앉힌다(`opacity: .3`).
    지금 결정에 필요한 건 '고를 수 있는 칸'뿐이다.
- **체인 배너는 위로 이동하지 않고 제자리에서 투명해진다.** 레일 대기 행을 투명도로만
  누르는 것과 같은 어휘 — 이동을 섞으면 글자가 눈을 끌고 도망가 읽히지 않는다.

### v0.3.2 — 2026-08-01

- **보스전에서 가시 방패·수혈이 아예 발동하지 않던 버그 수정.**
  - 원인: `GameState.damageRandomFrontEnemy`/`FieldEnemy`가 후보를 `CardType.ENEMY`로만
    걸렀다. 보스전 필드에는 `CardType.BOSS` 한 장뿐이라 후보가 0이 되고, 두 함수 모두
    `null`을 돌려준 뒤 호출부가 **조용히 종료**한다(로그도 연출도 없다). 같은 이유로
    헌혈팩·칼날 파편·품격있는 대처도 보스전 내내 죽어 있었다.
  - 판정을 `HandSystem.strikeRandomEnemy`/`strikeEnemyById`로 옮겼다. 보스를 후보에
    넣고, 격자를 낀 보스는 `hitCard`를 지나 칸 배율·부위 파괴를 그대로 받는다.
    보스 카드는 제거하지 않는다 — 격파 시퀀스는 `BossEventController`가 소유한다.
  - 돌려주는 피해량을 요청값이 아니라 **실제로 깎인 체력**으로 바꿨다. 칸 배율이 걸리면
    둘이 달라져 화면 수치가 거짓말을 한다.
  - 보스를 모르는 `GameState` 헬퍼 3종은 호출부가 모두 옮겨져 제거했다.
- **광역 카드가 보스 칸마다 따로 꽂히도록** — 합계 하나만 뜨던 것을 칸별 블라스트 +
  칸별 수치로 바꿨다. 샹들리에 반복 라운드·조합·유물 경로까지 전부 포함된다.
  - 호출부 열 곳을 각각 고치는 대신 `setBossCellStrikeSource()` 한 곳만 배선했다.
    `animateDamageNumbersById`를 지나는 모든 피해가 자동으로 칸 표기로 갈린다.
    그 덕에 지난 버전에서 손패 경로에만 넣었던 개별 분기를 걷어냈다.
- **카드로 칸을 때릴 때도 피격 반동**(`is-enemy-hit`)이 나온다. 직접 공격에만 있어
  카드로 치면 "맞았다"가 읽히지 않았다.
- **보스 칸을 내부 테두리 발광 비네트로** 바꿨다. 칸을 붉은/푸른 색으로 통째로 덮으면
  보스 일러스트가 색에 잠겨 오히려 무엇을 때리는지 안 보인다. 가운데는 비우고
  가장자리에서만 스며들게 했고, 손상 단계가 오를수록 발광이 짙어진다.
  색은 `--cell-tint`/`--cell-edge` 두 변수로만 갈리므로 새 칸 종류도 톤만 고르면 된다.
- **레일 칸 이름을 상단으로** 옮기고(`justify-content: space-between`), 하단 전용이던
  어둠 그라디언트를 위아래 양끝으로 나눴다 — 가운데를 비워야 카드 그림이 죽지 않는다.
- **적 체력·공격력 표기 확대**(15~19px → 18~24px, 대기 행도 함께) 및 붉은 톤 적용.
  공격력 기본색을 옮겼으므로 `atk-empower-afterimage` 키프레임의 시작/끝 색도 함께
  갱신했다 — 한쪽만 두면 잔상이 끝나는 순간 색이 튄다.
- **무대 비율 고정** — `--stage-ratio-max`(1.85) / `--stage-ratio-min`(1.25)을 벗어난
  창은 UI를 늘리지 않고 남는 쪽을 배경으로 남긴다. 초광폭에서 레일이 퍼지거나 세로로
  긴 창에서 카드가 홀쭉해지던 것을 막는다. 폰(모바일 가로)은 예외로 꽉 채운다.
  - 함께 고친 것: `#app`이 확정 높이를 갖게 되면서 `.game-shell`의 `height: 100%`가
    해석되지 않아 보드가 얇은 띠로 접혔다. `#game-board`에 `flex: 1 1 auto`를 줘야
    높이가 내려온다.
  - little_token은 1920×1080 고정 무대를 통째로 `scale()`하지만, 언멜팅은 CSS 11k줄이
    `vw`/`vh` 기준으로 짜여 있어 그 방식은 사실상 재작성이다. 같은 결과(비율 유지 +
    남는 쪽 배경)를 얻는 최소 변경을 골랐다.
- **우측 하단 버전 배지.** 문자열의 단일 출처는 `package.json`이다 — 화면에 손으로
  적어 두면 릴리스마다 조용히 어긋난다.

### v0.3.1 — 2026-08-01

- **`/테스트`로 시작하면 직업이 즉시 자동 선택되던 버그 수정.**
  - 원인: 팔레트 입력창의 Enter가 `preventDefault`만 하고 **전파는 막지 않았다.**
    명령이 그 자리에서 `openJobSelect`를 열면 새 오버레이가 window에 keydown 핸들러를
    등록하는데, 아직 올라가는 중이던 그 Enter가 곧바로 그 핸들러에 닿아 가운데 직업을
    확정해 버렸다. "디스패치 중 추가된 리스너는 호출되지 않는다"는 규칙은 **같은 노드**
    에만 적용되므로, input에서 등록한 window 리스너는 그대로 호출된다.
  - 팔레트가 삼킨 키(Enter·Escape)는 `stopPropagation`으로 끊는다. 이 한 줄이 원인
    수정이고, 앞으로 어떤 명령이 새 화면을 열어도 같은 사고가 재발하지 않는다.
  - 더해서 직업 선택의 키 핸들러가 input/textarea에서 올라온 키를 무시하도록 했다.
    팔레트의 `/` 가드와 같은 규칙이다 — 전역 단축키는 타이핑 중인 키를 먹지 않는다.

### v0.3.0 — 2026-08-01

- **보스 칸 부위 파괴** — 기믹 격자의 칸이 누적 피해로 금이 가고 임계점에서 깨진다.
  깨진 칸은 조준·무작위·광역 대상에서 빠지고 화면에서도 반투명 검은 판으로 꺼진다.
  - 왜: 칸마다 배율만 있으면 **약점 한 칸만 무한히 파먹는 게 최적해**였다. 약점일수록
    두 배로 빨리 깨져 스스로 꺼지므로, 결국 격자 전체를 돌며 때리게 된다.
  - 밸런스 기준은 "칸의 절반쯤 깨면 보스가 쓰러진다" 하나다. 내구도 식
    `maxHp × (2/칸수 − 0.1)`은 `(칸수/2) × (내구도 + 파괴 보너스) = maxHp`를 그대로
    푼 값이라, 보스 HP나 칸 수를 바꿔도 별도 조정 없이 따라온다.
    30F 실수치는 내구도 12 · 파괴 보너스 10(최대 체력의 10%).
  - 부위 누적은 **배율을 먹인 뒤**의 피해로 쌓는다. 약점(×2)은 절반의 품으로,
    경화(×0.5)는 두 배의 품으로 같은 보너스를 얻는다.
  - `BossGimmickStrike.damage`를 "배율 피해 + 파괴 보너스"의 합으로 정의했다.
    보너스를 별도 필드로만 두면 호출부 하나가 더하기를 빠뜨려도 조용히 지나간다.
  - 기존 테스트 *"같은 칸을 여러 번 때려도 배율은 그대로다 — 칸이 닳거나 변하지
    않는다"* 는 이 변경으로 뜻이 뒤집혀 마모/파괴 테스트로 교체했다.
  - 학습 시뮬은 칸 상태를 들고 있지 않으므로 `bossGimmickBreakBonusFactor`(칸 수만으로
    정해지는 누적 배수)를 곱해 따라온다. 근사인 만큼 모델 실측과 대조하는 정합
    테스트를 함께 뒀다.
- **칸 단위 타격 피드백** — 손패/직접 타격이 어느 칸에 꽂혔는지 화면에서 읽히게 했다.
  - 왜: 보스는 판 전체를 덮는 큰 타일 하나라, 피해 수치가 타일 중앙에 뜨면 방금 어느
    칸을 때렸는지 알 수 없었다. 특히 손패는 조준 칸이 보이지 않아 도박에 가까웠다.
  - 출처(사용한 손패 중앙 / 플레이어 카드) → 맞은 칸으로 블라스트를 쏘고, 같은 자리에
    피해 수치를 띄운다. 부위 파괴 보너스는 한 박자 늦게 같은 자리에 얹는다.
  - 방패/페이지 경계에 잘린 경우 실제 HP 손실 비율로 표기를 줄여, 칸마다 뜬 수치의
    합이 보스가 실제로 받은 피해와 어긋나지 않게 했다.
  - 격자가 켜진 보스는 타일 전체 버스트를 생략한다 — 칸 블라스트와 겹치면 오히려
    어느 칸인지 흐려진다.
  - 파괴 연출(파편 12개 + clip-path 균열 판)은 칸 DOM이 아니라 body 오버레이에 올렸다.
    파괴 직후 재렌더로 칸 노드가 교체돼도 파편이 끊기지 않는다. 같은 이유로 깨진 칸
    자체에는 1회성 애니메이션을 걸지 않았다(재렌더마다 다시 재생된다).
  - 균열 표기는 little_token의 유리 파편 연출에서 기법만 가져왔다: 인라인 SVG
    데이터 URI 금 레이어(3단계) + clip-path 다각형 모프 + 랜덤 CSS 변수 파편.
- **칸 기믹 확장 이음매** — 앞으로 붙일 기믹(태그별 추가/반감, 직접 타격 전용 칸,
  광역 감쇠·반사)이 배선을 다시 깔지 않아도 되게 판정 창구까지 맥락을 통했다.
  기믹 자체는 넣지 않았다.
  - `resolveMultiplier`가 이제 `origin`(직접/손패/유물) · `scope`(단일/광역) ·
    `tags`(시너지 태그)를 받는다. 위 세 종류는 이 함수만 고치면 붙는다.
  - 출처는 인자로 실어 나르지 않고 `beginAction()`으로 행동 시작 시 한 번 세운다.
    피해 헬퍼가 40군데를 넘어 시그니처를 전부 고치는 비용이 실익보다 컸다.
    선언을 빠뜨린 경로는 `other`로 남아 **조건부 보정을 못 받는 쪽**이 기본값이다.
  - 칸 종류에 `tone`(hot/cold/neutral)을 붙이고 색·타격 애니메이션·버스트를 전부
    톤 기준으로 돌렸다. 새 칸 종류는 META 표 한 줄이면 연출까지 따라온다.
    (CSS 선택자도 `.is-kind-*` → `[data-tone=*]`로 옮겼다.)
  - 배율로 표현되지 않는 결과(반사 피해)는 **필드를 미리 만들어 두지 않았다.**
    쓰지 않는 확장 필드는 다음 사람이 의미를 되짚어야 하는 비용만 남긴다 —
    대신 어디를 고쳐야 하는지 파일 헤더에 적어 뒀다.
  - 이음매가 조용히 끊기지 않게, 맥락이 실제로 판정까지 닿는지 검사하는 테스트를
    모델(파생 클래스 관찰)과 손패 경로(`useSingle` → 태그 전달) 양쪽에 뒀다.

### v0.2.2 — 2026-07-28

- **Prettier 제거** (`.prettierrc.json` · `format` 스크립트 · devDependency).
  설정과 명령만 있고 아무도 실행하지 않아 133개 TS 파일 중 **100개가 설정과 어긋난**
  상태였다. 이대로 두면 누군가 `npm run format`을 한 번 치는 순간 100개 파일이
  동시에 바뀌어 리뷰 불가능한 커밋이 생기고 `git blame`이 통째로 덮인다.
  - 채택하지 않고 제거한 이유: 이 저장소는 밸런스 데이터를 **일부러 열 맞춰 정렬한
    표**로 관리하는데 Prettier가 그걸 되돌린다. `EmberSystem`의 티어별 스폰 가중치
    표(티어마다 수치가 얼마씩 오르는지 세로로 읽힌다), `Tags.ts`의 주석 열 27줄,
    `BossSpecs.ts`의 한 줄짜리 보스 스펙이 모두 무너진다. 반면 평범한 로직 파일
    (`LightEconomy.ts` 등)은 애초에 변경이 0줄이라 얻는 것도 없었다.
  - 코드 스타일 규칙은 ESLint가 계속 담당하고, `npm run verify`의 lint 단계로
    CI에서 강제된다.
  - 팀이 커져 스타일 통일이 필요해지면 그때 다시 도입한다. 그 경우 정렬을 유지할
    데이터 표에는 `// prettier-ignore`를 붙여야 한다.

### v0.2.1 — 2026-07-28

문서·주석이 코드와 어긋난 곳을 바로잡고 죽은 코드를 걷어냈다. 게임 동작 변경은 없다.

- **문서 수치 오류 2건 수정.** 둘 다 v0.2.0에서 `CLAUDE.md`를 압축할 때 원본의
  오류를 검증 없이 옮긴 것이다.
  - 100F 녹지 않는 마녀: `210/140/70 HP 페이지` → 실제는 `maxHp` 270에 페이지
    바닥 180/90/0(한 페이지 90). 근거는 `BossSpecs.ts:29`, `BossEvent.ts`의
    `waxWitchPageFloor()`.
  - 60F 불씨 기사단장: `3턴마다 손패 2장` → 실제 `attackInterval: 2`, 인게임
    특징 문구도 `'2턴마다 기사단장의 손패 2장 발동.'`이다.
- **`EnaPolicyNetwork` 헤더 주석의 낡은 차원 표기 수정** — `250차원 관측 / 21개 행동`으로
  남아 있었다(실제 336 / 27). 수치를 다시 적지 않고 `ENA_FEATURE_COUNT`·
  `ENA_ACTION_SPACE`를 단일 출처로 가리키게 바꿨다. 이 주석 드리프트가 바로
  `CLAUDE.md` 시스템 불변식이 경고하는 상황이다.
- **`BossEvent.waxWitch` 죽은 상태 `nextWitchHandBurnAt` 제거.** 선언과 초기화만
  있고 읽는 곳이 없었다. 소각이 누적·강화되는지 확인한 결과 **그런 동작은 없다**:
  소각 호출부는 `handleClick`(일반 턴)과 `advanceBossTurnsForLevatein`(레바테인
  가상 턴) 둘뿐이고, 양쪽 모두 `turn % attackInterval === 0` 게이트 안에 있어
  주기당 정확히 1회 2장이며, `burnRandomHandCardsFromWitch`는
  `Math.min(요청, 손패수)`로만 자를 뿐 카운터를 들고 있지 않다.
- `burnRandomHandCardsFromWitch`의 낡은 주석 수정 — "2페이지 이상 손패 콤보 도중"에도
  소각한다고 적혀 있었으나 `resolveWaxWitchPageTwoTurn`은 소각을 호출하지 않는다
  (방패/회복/피해 손패만 쓴다).
- **`src/ui/ActionUI.ts`(186줄) 삭제** — 완전 고아였다. `ActionUI` · `action-ui` ·
  `action-ui-styles` 어느 것도 파일 밖에서 참조되지 않았고, 헤더에
  `MVP: Shows action buttons for selected card`가 남아 있었다.
- **`tsconfig.json` 죽은 옵션 정리** — `jsx: "react-jsx"`(React를 쓰지 않는다),
  `declaration`·`declarationMap`·`sourceMap`(`noEmit: true`라 아무 효과가 없다),
  `@utils/*`(`src/utils` 디렉터리가 없다). `paths`는 `vite.config.ts`의
  `resolve.alias`와 짝을 맞춰야 한다는 주석을 남겼다.
- **`package.json`의 `main: "dist/index.js"` 제거하고 `private: true` 추가** —
  라이브러리가 아니라 앱이므로 진입점 필드가 의미가 없고, 실수로 publish 되는 것도 막는다.
- `CardFaceRenderer` 정규식 이스케이프 수정은 v0.2.0에 포함돼 있다.

### v0.2.0 — 2026-07-28

개발 환경 개편. 게임 동작 변경은 BGM 재생 방식 하나뿐이다.

- **BGM을 Howler HTML5 스트리밍으로 전환.** 기존 `BgmManager`는 3분짜리
  192kbps 스테레오 트랙을 `decodeAudioData`로 전량 디코딩해 **트랙당 약 60MB**의
  PCM을 만들고, 현재+다음 2트랙을 유지해 **상주 약 120MB**를 썼다. 크로스페이드마다
  다음 곡을 새로 디코딩하는 스파이크도 있었다. BGM은 앞에서부터 흘려보내기만 하면
  되므로 랜덤 액세스가 필요 없다 — 스트리밍이 맞는 도구다.
  - 크로스페이드 구간마다 새 `Howl` 인스턴스를 만든다. 같은 곡이 연달아 뽑혀도
    겹쳐 재생돼야 하는데 html5 스트림은 인스턴스당 오디오 요소 하나를 공유하기
    때문이다. 페이드아웃이 끝나면 즉시 `unload()`해 살아 있는 스트림을 둘로 제한한다.
  - **트레이드오프:** html5 스트림은 `AudioContext.currentTime` 기준 샘플 정확
    예약을 못 한다. 기존의 절대시각 예약(`scheduleIteration(startAt)`)이 타이머
    기반으로 바뀌어 크로스페이드 시작이 수십 ms 흔들릴 수 있다. 3초 페이드에서는
    들리지 않는 오차다. 박자 동기가 필요한 트랙이 생기면 그 곡만 버퍼 방식으로
    예외 처리한다.
  - 효과음(`SfxManager`)은 그대로 Web Audio 버퍼를 쓴다. 짧고 자주 나는 소리에는
    버퍼가 맞다.
- **검증 게이트 도입.** 테스트 38종 329개가 빌드·CI 어디에서도 실행되지 않고 있었다.
  `npm run verify`(type-check → lint → test → resources:check)를 만들고 `build`가
  이를 거치게 했다. `test`는 `vitest`(watch)라 CI에서 끝나지 않으므로 `vitest run`으로
  바꾸고 대화형은 `test:watch`로 분리했다.
- **CI를 verify / deploy로 분리.** `claude/**` 브랜치도 트리거에 넣어 PR 전에
  스스로 검증되게 했다. 배포는 Pages 환경 보호 규칙에 맞춰 `main`에서만 실행한다.
- **리소스 수명주기 검사 추가**(`src/tools/resource-lifecycle-check.ts`).
  이미지·오디오·DOM 복제 등의 생성 지점을 "어느 파일에서 몇 개까지"로 잠근다.
  웹 게임 렉의 주범은 알고리즘이 아니라 같은 리소스의 반복 생성이다. 지금 이 저장소가
  깨끗할 때(`new Image` 1건 · `new Audio` 0건) 잠가 두는 게 가장 싸다.
- **`.gitattributes` 추가** — 소스·문서 LF 고정. 협업자 OS가 섞여도 diff에 줄바꿈
  변경이 끼지 않는다.
- **`.gitignore` 수정** — `.claude/` 통째 무시를 `.claude/settings.local.json`으로
  좁혀 공유 설정을 커밋할 수 있게 했다. `docs/` 무시는 제거했다(`dist/`로 배포하므로
  문서 디렉터리를 날릴 이유가 없었다).
- **`CLAUDE.md` 재구성 43.8KB → 약 12KB.** 약 40%가 누적된 완료 보고였다.
  - 완전 중복 12줄 삭제(`코드 규칙`↔`코드 작성 규칙` 등 3쌍의 중복 절 병합)
  - 낡아서 코드와 어긋난 4줄 삭제. 그중 "제단 보스 인트로는 3x3 보스 칸 낙하"는
    같은 문서의 "거대 1칸 보스 타일"과 정면으로 모순이었다(코드 기준 후자가 맞다).
  - CSS 완료 보고 9줄 삭제(nth-child 지연, 460ms, hover 기울기 등). 스타일 파일이 원본이다.
  - 에나 성장/특화 상세는 `Ena_Companion_AI_Design.md` §12-1~12-3으로 이전하고,
    CLAUDE.md에는 불변식과 참조 경로만 남겼다.
  - `문서 라우팅` 절을 새로 두어, 어떤 작업에 어떤 문서를 열어야 하는지 명시했다.
    문서를 항상 다 읽지 않고 필요할 때만 가져다 읽는 구조로 바꿨다.
- **`시스템 불변식` 절 신설.** "무엇을 했다"(시간이 지나면 썩는다)를 "무엇을 깨면
  안 된다"(늙지 않는다)로 되썼다. 여기에 **에나 성향·성장·특화를 바꾸면 헤드리스
  시뮬레이션도 같은 커밋에서 함께 고친다**는 규칙을 명문화했다. 어긋나면 에나가
  실제로 존재하지 않는 게임을 학습하는데, 테스트도 화면도 멀쩡해서 조용히 묻힌다.
- `CardFaceRenderer`의 정규식 문자 클래스 안 불필요한 이스케이프(`[^\|]` → `[^|]`)
  수정 — lint를 CI 게이트로 올리면서 걸린 기존 오류다. 동작은 동일하다.
