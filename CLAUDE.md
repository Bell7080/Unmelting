# CLAUDE.md

Unmelting 협업용 핵심 규칙. **현재 코드 기준**으로만 유지한다.

이 문서는 "지금 지켜야 하는 것"만 담는다. "무엇을 했다"는 `VERSION.md`로 간다.

## 문서 라우팅 — 필요할 때만 열어 본다

이 문서는 항상 읽는다. 나머지는 **작업에 해당할 때만** 연다.

| 문서 | 언제 여는가 |
|---|---|
| `Unmelting_Game_Concept.md` | 원형 기획서. 거점 9칸 배치(§12) 등 기획 의도를 확인할 때 |
| `Unmelting_Story.md` | 세계관 원문 + 해석. 새 손패/유물/적/보스/대사의 톤·명명을 정할 때 |
| `Ena_Companion_AI_Design.md` | 에나 AI 전반. **성향·성장·특화·관측/행동 계약·시뮬 연동을 만질 때 필수** |
| `VERSION.md` | 변경 이력. 과거에 무엇을 왜 그렇게 했는지, 무엇을 왜 되돌렸는지 |
| `Unmelting_Dev_Upgrade.md` | 개발 환경 개선 잔여 작업(에셋 규격 검사, 밸런스 리포트 CLI) |

원본이 코드인 것은 문서로 옮겨 적지 않는다. 문서에는 **어느 파일이 그 규칙의 단일
출처인지**를 적는다.

## 프로젝트 요약

- 장르: 3레인 × 3행 카드 레인 로그라이트.
- 플레이: `녹지 않는 소녀` 1장 중심 + 손패/조합/유물.
- 톤: 따뜻한 촛불, 낡은 종이, 어두운 남보라/먹색 다크판타지.

## 스토리 톤/테마 연계 (신규 콘텐츠 시 상시 참고)

세계관 원문·해석은 `Unmelting_Story.md`. 새 손패/유물/적/보스/대사는 아래 톤에 맞춘다.

- **다섯 물질/힘이 세계의 전부**: 촛불(불빛)·양초(wax)·밀랍(굳음)·불씨(flame)·제물(sacrifice). 새 요소는 이 팔레트 안에서 뽑는다.
- **자기연소 루프**: "자신을 불태워라" — 제물·자해로 스스로를 태워 어둠을 이기는 게 코어 정서. 주인공은 굳지 않고 계속 타는 "녹지 않는 소녀(=에나)".
- **탐욕이 파멸의 방아쇠**: 도박·지식·마법에의 탐욕이 나라를 멸망시켰다 — 이벤트(도박)·경제·제물 톤의 뿌리.
- **밀랍 굳음 = 어둠에 잠식**: 굳음 관련 표현은 잠식/멈춤의 어둠 톤으로.
- **손패 = 직접 행사하는 기술(마법·연성 모두), 유물 = 마도서 같은 지닌 아이템**. 명명·설명을 이 구분에 맞춘다.
- **떡밥은 암시만**: 캐릭터·흑막(레온하르트·제피르·가주·악마)은 직접 설명하지 않고 대사로만 흘린다. 귀족 배경은 단정한 단어 선택.
- **꿈(1~100)/현실(101~200)**: 현재는 밀랍이 된 에나의 꿈(하급 적 위주). 현실 층은 인간형 강적·보스 강화판·악마 본체.

## 실행/검증

```bash
npm install
npm run dev
npm run verify   # type-check + lint + test + resources:check — 이것만 통과시키면 된다
npm run build    # verify 포함
```

- 기능·데이터 변경 후에는 **반드시 `npm run verify`** 를 실행하고 결과를 보고한다.
- 실패한 검사를 우회하거나 테스트를 지워서 통과시키지 않는다. 규칙이 틀렸다고
  판단되면 규칙을 고치고 그 이유를 보고에 남긴다.
- `resources:check`의 허용 표를 고쳐야 한다면 커밋에 이유를 남긴다.

## 코드 구조(매니저 맵)

- `src/index.ts`(~3.9k줄): 컴포지션 루트. 턴 루프/손패 해석/점수·활동 로그/보드 채움과 매니저 배선(deps 주입)만 남긴다.
- `src/app/` 런 흐름 매니저: `DevCommandPalette`(디버그 팔레트) · `SettlementScreen`(정산) · `CompanionDirector`(에나 바크/예측/클러치/런 종료 성장 적응) · `RelicEffectsManager`(유물 발동) · `ShopFlowManager`(상점/제단/강제 시련 흐름) · `EventFlowManager`(이벤트 문 흐름) · `FeedbackTypes`(트레일 공용 타입).
- `src/ui/GameBoardRenderer.ts`(~4.5k줄): 렌더 퍼사드. 보드/HUD/손패 렌더와 카운터 롤링을 소유하고, 나머지는 `readonly` 컴포지션 서브뷰로 위임한다. 외부(index/BossEvent)는 퍼사드 공개 API만 호출한다.
- `src/ui/renderer/` 서브뷰: `RendererTypes`(뷰 계약 타입) · `Html`(escapeHtml) · `CardFaceRenderer`(카드/유물 face HTML, `faces`) · `CompendiumView`(도감) · `ExperienceView`(경험 탭) · `ResourceTrailFx`(자원 트레일 엔진, `trails`) · `JobSelectView`(직업 선택) · `EventOverlayView`(이벤트 씬/미니게임) · `BossFxView`(보스 연출/악마 커튼) · `ShopOverlayView`(상점 오버레이/셔터/팩 피커).
- `src/audio/`: `BgmManager`(Howler html5 스트리밍) · `SfxManager`(Web Audio 버퍼).
- `src/tools/`: 빌드 게이트용 검사 스크립트. 게임 런타임에서 import 하지 않는다.
- 서브뷰 패턴: `constructor(private readonly host: GameBoardRenderer)` + `import type`(런타임 순환 없음). 렌더 상태 단일 출처는 호스트(공유 접근자 getGameState/getLockedCardIds 등). 새 뷰 분리 시 같은 패턴을 따른다.
- 앱 매니저 패턴: deps 인터페이스 주입. 수치 상태(불빛/화폐/펄스 키)와 게임 플래그는 index가 소유하고 getter/setter 브리지(`resources`)나 클로저로 공유한다. 데이터 테이블/시스템 클래스는 deps가 아니라 직접 import 한다.

## 시스템 불변식

깨면 조용히 망가지는 계약들이다. 여기 있는 줄은 "이렇게 구현돼 있다"가 아니라
"이걸 깨지 마라"로 읽는다.

### 에나 (Companion AI)

- ★ **에나의 성향·성장·특화·개입 규칙을 바꾸면 `src/rl/EnaTrainingSimulation.ts`도
  같은 커밋에서 함께 고친다.** 런타임과 시뮬이 어긋나면 에나는 실제로 존재하지 않는
  게임을 학습하고, 테스트도 화면도 멀쩡해서 그 사실이 묻힌다. 상세는
  `Ena_Companion_AI_Design.md` §12-3.
- 관측/행동 계약(`ENA_FEATURE_COUNT` 344 · `ENA_ACTION_SPACE` 27)은 시뮬과 트레이너가
  공유한다. 차원이 바뀌면 `EnaPolicyStore`가 구버전 정책을 거부하고 교사 정책으로
  폴백한다 — **재학습이 필요한 주(major) 버전급 변경**이다.
- 보스 체력은 `BossFightBudget`(손패 3~4장 + `supportHits`)에서 역산한다. **시뮬도 그
  화력을 실제로 넣어야 한다**(`supportStrikeChance`) — 빼면 체력을 뽑은 세계와 에나가
  배우는 세계가 달라져 보스가 영영 안 죽고 보스전 학습 신호가 사라진다.
- 화면에 보이는 보스 정보(격자 칸 수·약점 배율·페이지 게이트 잠금·종복)는 관측에도
  넣는다. 빠지면 에나는 이미 막힌 경계를 계속 때리라고 조언한다.
- 성장 앵커의 단일 출처는 `computeEnaGrowth()`(`EnaDisposition.ts`)다. 메타 상점
  해금형으로 바꿔도 이 함수만 교체한다.
- 특화 0이면 기존 동작과 **완전히 동일해야** 한다. `saveDisposition`/`loadDisposition`
  양쪽에 특화를 함께 넘기지 않으면 초과값이 잘린다.
- 판정 로직은 런타임과 시뮬에 복사하지 말고 순수함수로 뽑아 공유한다
  (예: `CompanionForesight.estimateImminentWebMergeFromCells()`).
- 에나 지원 카드를 코드에 하드코딩하지 않는다. 새 손패/유물은 데이터 테이블
  (`HandCards`/`Relics`)에 `synergyTags`(+공격 카드는 `damageProfile`)를 달아 등록하면
  `HandCardAdvisor`·`EnaKnowledgeAdapter`·시뮬 학습에 자동 반영된다.
- `damageProfile`은 `HandSystem` 실제 공식의 **보수 근사**다. 공식을 바꾸면 근사도
  함께 갱신한다.
- 에나 대사는 LLM/API 없이 런타임에서 고르는 **작성형 풀**이다(`CompanionLines.ts`).
  손패 평은 실제 설명문 효과와 어긋나지 않게 쓴다(예: 방패 밀치기 = 방패 수치 피해이지
  이동/넉백이 아니다). 귀족 배경은 단정한 단어 선택으로만 남기고, 고풍 어미·긴 작대기
  쉼표·클릭 멈춤 반응은 쓰지 않는다.
- 대사 품질은 `CompanionLines.test.ts`가 전 풀을 슬롯×말투 전수 렌더
  (`enumerateLineRenders`)해 잔여 토큰/이중 공백/문장부호/종결/56자 상한을 검사한다.
  조사 특수쌍은 `[으로/로]`·`[으니/니]`(받침 없거나 ㄹ받침 → 뒤항)이고
  `[이었/였]`·`[이라/라]`는 일반쌍 규칙을 따른다.
- 첫 조우 소개(교육 바크)는 영구 first-seen 게이트로 **태어나서 처음 겪는 순간 1회만**
  발화한다. 필드 3종 저장 키는 기존 `field:*`를 유지하고 나머지는 `encounter:*`다.
- 강적 미숙 대사(miss-shield)는 개입각에 더해 '최근 2턴 내 피격/처치'가 있어야만
  나간다 — 싸우지도 않았는데 지원 부족을 사과하지 않는다.

### 보스

- 보스도 밀랍 굳음 적용 대상이다. 굳은 보스는 직접 공격 피해와 행동 비용은 받지만
  반격 주기/공격 카운트다운이 줄지 않고 반격·특수행동을 건너뛴다.
- 보스 페이지 능력 수치(절반 페이지 보스 목록·조각사 비대화·마녀 광폭화·탐욕의 값·
  기사단장/마녀 손패 수치·페이지 경계식)의 단일 출처는 `src/data/BossPages.ts`다.
  본편(`BossEvent`)과 학습 시뮬이 **같은 상수를 import** 한다 — 한쪽에만 적지 않는다.
- 보스 칸 배율 결정은 `BossGimmickManager.resolveMultiplier()` **단일 창구**를 지난다.
  바깥에서 태그/시너지 보정을 계산하면 태그 반응 사각지대가 생긴다.
- 판정에 필요한 맥락(`origin`·`scope`·`tags`)은 이미 그 창구까지 닿아 있다. 태그별
  추가/반감, 직접 타격 전용 칸, 광역 감쇠는 **호출부를 건드리지 않고** 붙는다.
- 배율로 표현 못 하는 결과(광역기 반사 피해 등)를 넣을 때만 `BossGimmickStrike`에
  결과 필드를 늘리고, 그 값을 적용할 두 호출부(`handleClick`/`HandSystem`)를 함께 고친다.
- 행동 출처는 `beginAction()`으로 **행동 시작 시 1회** 세운다(손패 = `useSingle`,
  유물 = `damageAllEnemies`, 직접 타격 = `handleClick`). 선언을 빠뜨린 경로는 `other`로
  남아 조건부 보정을 타지 않는다 — 빠뜨려도 조용히 이득이 생기지 않는 쪽이 기본값이다.
- 칸 종류를 늘릴 때 필요한 것은 `BOSS_GIMMICK_KIND_META` **한 줄**이다(label·multiplier·
  tone). 색·타격 애니메이션·버스트는 `tone`으로 갈리므로 CSS/렌더러 분기를 새로 쓰지
  않는다. 전용 색이 필요할 때만 `.is-kind-*` 규칙을 덧쓴다.
- 부위 파괴 수치(칸 내구도·파괴 보너스·시뮬 환산 배수)의 단일 출처는
  `BossGimmickManager`의 `bossGimmickCellDurability`/`bossGimmickBreakDamage`/
  `bossGimmickBreakBonusFactor`다. 셋은 같은 식에서 파생되므로 한쪽만 고치지 않는다.
- 기준은 **칸의 절반을 깨면 보스가 쓰러진다**이다. 내구도 식이 이 관계를 그대로
  풀어 쓴 값이니, 보스 HP나 칸 수를 바꿔도 별도 조정 없이 따라온다.
- 칸 배율은 타격 beat마다 다시 굴린다(`rerollKinds()`). **누적 손상·파괴 상태는 남긴다** —
  배율만 옮겨 다녀야 "때려 둔 칸을 마저 깰지, 새 약점을 노릴지"가 선택으로 남는다.
- 리롤은 피해 수치 연출이 **끝난 뒤** 돈다. 먼저 돌면 방금 어느 배율로 맞았는지가
  화면에서 사라진다. 손패 경로는 `setBossCellStrikeSource`의 연출 완료 콜백을 쓴다.
- `BossGimmickStrike.damage`는 배율 피해 + 부위 파괴 보너스의 **합**이다. 호출부는 이
  값만 쓰고, `cellDamage`/`breakDamage` 내역은 수치 표기를 나눌 때만 읽는다.
- 깨진 칸은 조준·무작위·광역 대상에서 빠진다. 성한 칸이 없으면 격자는 null/빈 배열을
  돌려주고 호출부가 격자 없는 피해로 되돌아간다.
- 깨진 칸은 클릭을 **받아서 버린다**(`data-broken` + 핸들러 조기 반환). `pointer-events`로
  비우면 클릭이 보스 타일로 새어 성한 칸이 대신 맞는다.
- 직접 타격은 블라스트를 쏘지 않는다 — 돌진(`animatePlayerAttack`)이 곧 타격 표현이다.
  발사체는 손패(화면 중앙 → 칸) 전용이다.
- 칸 타격 기록(`takeHits()`)은 **이번 beat 것만** 남아야 한다. 연출이 소비하는 경로
  (`index.ts` 손패 흐름)는 카드 사용 직전에 남은 기록을 먼저 버린다.
- 칸 균열/파괴가 바뀌면 `BossEventController.syncGimmickGrid()`를 렌더 전에 불러야
  한다 — 격자 뷰는 푸시형 스냅샷이라 저절로 갱신되지 않는다.
- 보스 칸 타격 연출은 `GameBoardRenderer.playBossGimmickStrikes()` 한 창구다
  (출처 → 칸 블라스트 → 균열/파편 → 칸 위 피해 수치). 보스 타일 중앙에 수치를 띄우면
  어느 칸을 때렸는지 알 수 없다.
- 손패 피해가 기믹 격자를 타는 진입점은 `HandSystem.hitCard`(단일/무작위 칸)와
  `hitCardAsAreaDamage`(필드 전체 = 칸마다 1회) 둘뿐이다. 새 공격 손패는 이 둘을
  거쳐야 자동 반영된다.
- 유물의 '무작위/지정 적 1체' 타격은 `HandSystem.strikeRandomEnemy`/`strikeEnemyById`
  한 경로다. **보스도 적이므로 후보에 넣는다** — 빼면 보스전 내내 가시 방패·수혈·
  헌혈팩·칼날 파편이 후보 0으로 조용히 죽는다. 보스 카드는 여기서 제거하지 않는다
  (격파 시퀀스는 `BossEventController` 소유).
- 보스 칸 피해 표기는 `GameBoardRenderer.setBossCellStrikeSource()` **한 곳만 배선**하면
  `animateDamageNumbersById`를 부르는 모든 경로가 칸 단위 표기를 탄다. 호출부마다
  따로 분기하지 않는다.
- 격자 행 수는 보스가 **실제로 점유하는 레일 행 수**와 같게 맞춘다(3×3 또는 2×3).
  `occupiedDistRows ≥ 2`면 타일마다 격자 한 행씩 그린다(`tileRows`·`startDistance`).
- 전투 중 몸집이 바뀌는 보스는 `syncGimmickShapeToBody()`가 `occupiedDistRows` 변화를 보고
  격자를 자동으로 다시 짠다(줄어들 때도 늘어날 때도). 보스별 `resize()` 호출을 심지 않는다.
- 격자를 다시 짜면 칸은 새로 굴리고(약점 재노출) 내구도는 **남은 체력**에서 다시 뽑는다 —
  최대 체력 기준이면 마지막 페이지에서 못 깬다.
- 1회성 격자 연출 플래그(등장·리롤)는 **렌더 1회 단위**로 소비한다(`getRenderPass`).
  타일마다 소비하면 다행 보스의 첫 행만 연출을 먹는다.
- 칸 개념이 없는 학습 시뮬은 `bossGimmickExpectation()` 요약(조준 = 최고 배율,
  광역 = 칸 수 × 평균 배율, 부위 파괴 = `breakBonusFactor` 누적 배수)으로 같은
  밸런스를 따라온다.
- 보스전은 별도 게임 화면이 아니라 **일반 레일 톤 이벤트**다. 거대 1칸 보스 타일 →
  레일형 3보상 드롭 → 상점 톤 시련 탭(EXIT 후 셔터 상승) 순서를 유지한다.
- 보스 이벤트의 1/2/3턴은 **가상 턴**이다. 실제 런 턴 카운터는 전투/시련이 끝날
  때까지 고정한다.
- 모든 보스의 페이지 경계는 `bossPageGate()` **단일 창구**다 — 하한(floor) + 요구 조건
  (`none`이면 닿는 순간 자동, `cell-break`면 부위를 하나 더 깨야 열린다)의 쌍이다.
- `cell-break` 요구는 격자가 켜진 보스만 쓸 수 있다. 깰 칸이 없으면 잠그지 않고 연다 —
  성립하지 않는 조건으로 잠그면 진행이 막힌다.
- 하한이 피해를 막고 있는 beat는 피해 수치 **자리에** 경고 문구를 띄운다
  (`pageGateWarning()` → `playBossPageGateWarning`). 양식도 `damage-float` 그대로 쓰고
  색·노출 시간만 바꾼다 — 수치가 뜰 자리임이 읽혀야 한다.
- 절반 HP 페이지를 쓰는 보스는 `HALF_PAGE_BOSSES` 한 줄로 정한다(`halfPage`). 격자가
  켜져 있으면 리미트(부위 파괴 요구), 없으면 닿는 순간 자동으로 열린다.
- 페이지 **전환 순간 1회성** 능력은 `applyHalfPageTwoEntry()`에, **매 턴 도는** 능력은
  각자의 턴 처리에 둔다. 섞으면 언제 걸리는 능력인지가 흐려진다.
- 조각사처럼 판을 통째로 바꾸는 강화는 **그 뒤 소환에도 계속 붙인다**. 전환 1회만
  적용하면 다음 소환에서 판이 도로 가벼워진다.
- **비대화(체력만·`swollen`)와 광폭화(공격력까지·`is-ember-empowering`)는 다른 어휘다.**
  비대화는 붉게 달구지 말고 몸집으로만 말한다 — 섞으면 무엇이 오른 강화인지 안 읽힌다.
- 필드 적 스탯 소급 부여는 `Card.applyTrialEnemyStatBonus()` 한 경로다(시련 '광란'과 공용) —
  합체 적 배수·이미 입은 피해가 그 안에서 맞는다.
- HP바 경계선이 무엇인지는 `bossPageMarkers()`가 정하고 렌더러는 그리기만 한다.
  뚫린 경계는 지우지 않고 점선으로 남긴다 — 남은 선 수가 남은 페이지 수다.
- 경계선은 위아래 짧은 눈금 한 쌍이다. 통짜 선은 경계가 50%인 보스에서 가운데 HP
  숫자에 가려 사라진다(막대는 `overflow: hidden`이라 밖으로 뺄 수 없다).
- 2페이지 이상 보스는 카드에 열기 레이어가 얹힌다(`bossPagePhase()` → `data-page`).
  색은 톤으로 갈린다: 일반 보스 호박빛, 이벤트로 불려 나온 악마만 보랏빛.
- 보스 직접 타격 연출은 `animateBossSlamAttack()` 하나다(들어올림 → 뒤로 당김 → 쾅).
  소환 적은 `animateEnemyAttacks` 그대로 — 보스 타일만 3D 복제를 쓴다.
- 30F에서 다듬은 보스전 어휘(슬램 타격 · 칸 블라스트 · 칸 위 피해 수치 · 피격 반동 ·
  배율 리롤 · 페이지 열기 필터)는 **모든 보스에 같은 경로로 적용된다** — 보스별로 따로
  붙이지 않는다. 새 보스도 격자만 켜면 그대로 따라온다.
- 보스 턴의 특징 연출과 타격 사이에는 `BOSS_TURN_BEAT_MS` 간격을 둔다. 붙여 두면
  동시에 일어난 일로 읽혀 무엇이 있었는지 남지 않는다. 카드를 펼치는 보스
  (기사단장·마녀)는 **카드가 전부 발동된 뒤** 그 박자를 두고 때린다.
- 보스/90F 규칙 타이틀은 제목 부상 완료 후 짧은 딜레이가 지나야 하단 클릭 문구가
  점멸하고 입력이 열린다.

### 경제·스폰

- 불빛 턴 보정은 완만한 선형식(`1 + 턴 × 0.015`)을 유지해 90턴대 경제 폭증을 제한한다.
- 밸런스 수치는 `src/data` 또는 공용 상수에 두고 뷰에 직접 넣지 않는다. 같은 수치가
  두 곳에 있으면 한쪽을 단일 출처로 정하고 나머지는 import 한다. 근사가 불가피하면
  **정합 테스트를 함께 둔다** — "함께 갱신한다"는 문서 약속만으로는 지켜지지 않는다.
- 보스 체력은 `BossSpecs`의 **전투 가정**(`playerAttack`·`targetActions`·`handCards`·
  `supportHits`)에서 역산한다. 계산은 `BossDamageBudget` 단일 출처다.
- 체력을 `공격력 × 타수`로 잡지 않는다. 플레이어는 공격 손패 3~4장을 함께 쓰고,
  격자에서는 그 손패가 광역 ×칸수 · 조준 ×2 · 부위 파괴 ×1.8로 증폭된다.
- 레시피·유물 시너지는 퍼센트로 숨기지 말고 `supportHits`(추가 직접 타격 환산)에 적는다.
- 수치를 바꾸면 `npm run balance:boss`로 분포를 확인한다. 체력 산출과 같은 모델을 쓰므로
  리포트가 어긋나면 둘 중 하나가 틀린 것이다.
- 광역 손패 한 장이 보스 체력의 절반을 넘게 지우면 곡선이 아니라 뽑기 게임이 된다
  (`BossSpecs.test.ts`가 잡는다).
- 곡선의 단조 불변식은 체력이 아니라 **전투 길이**다. 격자 보스는 같은 길이를 위해
  체력이 3배 이상 두꺼워진다 — 모든 보스에 격자가 깔린 지금은 표시 체력도 함께 오른다.
- 보스 수치를 바꾸면 에나 기본 성향(`SIM_FITTED`)을 **다시 피팅한다**. 세계가 달라졌는데
  토대가 옛 세계 값이면 에나는 없는 게임에 맞춰 돕는다. held-out 시드로 이전 스냅샷과
  비교해 나은 쪽만 채택한다.
- 손패 `동전`은 보물상자 전용 보너스 드롭이다. 일반 적 처치/드로우 풀에 섞지 않는다.
- 적 처치 전리품은 `0 ~ Card.defeatDropCount` 균등이고 굴림의 단일 출처는
  `Card.rollDefeatDrops()`다. 카드에 적힌 `0~N장` 표기가 곧 이 분포다.
- 일반 적의 상한은 폭과 같다(합체 시 합산 — 1칸 0~1 · 2칸 0~2 · 3칸 0~3). 특수 적
  (미믹·괴물꽃)만 `defeatDropCount`를 직접 지정한다.
- 손패·조합 처치는 `rollDefeatDrops(true)`로 **두 번 굴려 낮은 쪽**을 쓴다(0~1이면 25%).
  범위는 그대로 두므로 카드 표기를 바꾸지 않는 '보이지 않는 제약'이다.
- ★ **손패·조합으로 잡은 전리품은 행동 1회당 1장이 상한이다**(`awardHandKillDrops`).
  손패는 턴을 넘기지 않으므로, 잡은 수만큼 돌려받으면 턴을 올리지 않고 손패·불빛을
  무한히 불릴 수 있다(광역기 → 3장 회수 → 다시 광역기). 상한 1이면 쓴 것보다 더 받을
  수 없어 그 고리가 성립하지 않는다. 직접 타격은 턴을 소모하므로 0~N을 그대로 받는다.
- 이 상한은 학습 시뮬에도 같은 형태로 있다(`handKillDropBudget`) — 한쪽에만 두면
  에나가 실제로 없는 무한 고리를 학습한다.
- 새싹 병아리(온보딩) 런에서는 화폐($) 획득 요소를 전부 잠그고, 이벤트 문도 스폰하지
  않는다(RL 시뮬 새싹 아크도 동일). 졸업 후 정규 런부터 열린다.
- 새싹 병아리 스폰 보정의 단일 출처는 `SPROUT_SPAWN_ADJUST`(`EmberSystem.ts`)다.
  런타임(`CardSpawner.setDifficultySpawnAdjust`)과 학습 시뮬이 **같은 상수를 import** 한다.
- 난이도 보정은 직업 보정과 **별도 축**이다. 한 축에 합쳐 두면 직업 선택이 난이도
  보정을 덮어써 온보딩 판이 조용히 정규 난이도로 돌아간다.
- `/리셋`은 에나 3종 저장 키에 더해 `unmelting.` 접두사 localStorage 전부를 지운다.
  **새 진행도 키는 반드시 `unmelting.` 접두사를 쓴다** — 접두사가 없으면 리셋 대상에서
  조용히 빠진다.

### 강제 시련

- 강제 시련 카드는 `TRIAL_DEFINITIONS`(`src/data/Trials.ts`)에서 파생한
  `FORCED_TRIAL_CARDS`로 관리한다. 선택 시 즉시 런 modifier를 적용하고 EXIT는
  선택 이후에만 허용한다.
- 독립 팝업을 만들지 않고 상점과 같은 shell/content-bundle 레이어를 재사용한다.

## 구현 사실 (현재 코드 기준)

규칙이 아니라 "지금 이렇게 되어 있다"는 사실이다. 코드가 바뀌면 여기도 바꾼다.
연출 타이밍·CSS 수치처럼 스타일 파일이 원본인 것은 적지 않는다.

### 부팅 / 거점(촛대)

- 진입점은 `src/ui/hearth/HearthScene.ts`(`/시작`). 9칸 배치·역할은 `Unmelting_Game_Concept.md` §12.
- 부팅은 검은 타이틀 게이트 → 실제 로딩(폰트 + 핵심 스프라이트, 태스크별 8초 상한)을 따르는 다이아 5개 게이지(별당 최소 150ms 페이싱) → `Click to Start` → 클릭 1회가 오디오 언락·모바일 전체화면을 흡수 → 장면 DOM을 게이트 아래에서 먼저 구성한 뒤 오버레이를 걷는다(커튼 첫 프레임 보전).
- 최초 실행(`unmelting.boot.firstRunStarted` 미마킹)만 로비 없이 새싹 병아리로 직행하고 인트로 시네마틱을 1회 재생한다. 마킹은 런을 온전히 마친 `endGame` 시점에만 하므로 중도 이탈 후 재부팅은 다시 첫 시작을 탄다.
- 레일 드롭은 `rail-in`(1회) → `rail-done`(정적) 클래스 전환으로 재렌더 시 반복 재생을 막는다.
- 칸 개방: 모험은 상시, 무역은 새싹 졸업 시. 나머지(서고 포함)는 잠금이며 다음 목표 칸만 살짝 밝고 자물쇠가 맥동한다. 서고는 항상 개방이고 통산 기록(`LifetimeRecord`)을 `HearthHandlers.getLifetimeRecord`로 주입받아 원장으로 보여 준다.
- 새싹 졸업은 화폐(currency) 메타도 함께 해금한다. 졸업 후 첫 로비에서 무역 칸 개방 축하와 같은 beat에 화폐 패널이 등장한다(`unmelting.hearth.tradeUnlockCelebrated`로 1회 보장).
- 만찬에서 만든 음식은 실제 유물 `최후의 만찬(last-supper)` 카드가 되고, 이름/효과/설명은 선택한 음식·소스·토핑 조합과 스탯 합산값으로 생성된다. **스탯 효과는 로비 획득 즉시가 아니라 인게임 시작 시 1회 발동한다.**
- 모험 셔터의 캐릭터 선택은 현재 에나(0번) 고정으로 패스되어 곧바로 난이도 선택으로 간다(추가 동행 해금 전까지). 난이도 캐러셀 포커스는 마지막 선택 저장값을 따른다.
- `/테스트` 명령과 `?test=1` 부팅은 메타 전부 해금 + 테스트 런 직행(메타 게이팅 우회).
- 정산(온보딩 클리어/100F 클리어/사망)은 `app/SettlementScreen` 단일 매니저가 같은 골격으로 렌더한다. `/사망`·`/승리` 디버그 명령으로 실제 종료 흐름 그대로 점검할 수 있다.
- 모바일(≤700px): 거점 칸 자물쇠/라벨 축소(잠금 아이콘 `pointer-events` 차단 + 칸 `overflow: hidden`으로 이웃 칸 클릭 가로채기 방지), 셔터 뒤로가기 버튼은 셔터 바로 아래, 서고 원장·정산 화면은 세로 플로우 축소판, 말풍선은 `width: max-content` + 최종 문장 선측정으로 가장자리 클램프. 터치 기기 거점 칸은 첫 탭 인스펙터·둘째 탭 진입.

### 턴 루프 / 필드

- 턴 루프: 플레이어 행동 → 적/보물/폭탄/꽃 처리 → 불씨/레일 하강·리필 → 포자 카운트 0 표시 → 전염/2턴 리셋. 굳음 해제도 0턴을 짧게 표시한 뒤 해동 연출로 풀린다.
- 시작 직업 선택은 빈 3×3 레일 위 암막/커튼 레이어에서 진행하고, 선택 후 최초 보드를 채운 뒤 커튼을 걷는다.
- 이벤트 문 진입: 레일 커튼이 충분히 닫힌 뒤 문 소비 → 짧은 딜레이 → 씬 일러스트 세로 슬릿 공개 → 대사 → 선택 버튼. 종료는 버튼 숨김 → 마무리 대사 → 일러스트 접힘 → 커튼 열림 뒤 입력 해제.
- 이벤트 NPC 대사는 레일 하단 말풍선(상단 꼬리)이며 클릭으로 타이핑 완료/다음 줄 넘김을 지원한다. 선택지는 대사 종료 후 긴 햄버거형 행 버튼.
- 키틴은 전방 선택 함정 1장을 제거한다. 단일은 2칸 이하, 트리플은 3칸 이하 폭(`groupCount`)까지 허용한다.

### 상점 / 제단

- 10·20턴 상점, 30턴 제단 모드.
- 상점: 유물 슬롯 리롤(무료/팩 고정)과 팩 피커 재뽑기(`1 + 횟수`$)를 별도로 지원한다. 팩 가격은 방문 내 동일 팩 구매마다 시작가를 더한다.
- 일반 상점 3팩(자원/조합/해금) 공통 시작가는 10층 120에서 10층마다 +40(20F 160 · 40F 240 …). 제단은 30F 500 · 60F 1500 · 90F 2500.
- 제단: 리롤 없음, 무료 카드 2장(기본 + 동전 한 닢 1$), 팩 4종(축복/자원/강화/삭제). 동전 한 닢은 제단 방문마다 초기화된다.
- 제단 유물 오퍼는 상점과 같은 전체 풀에서 등급 가중치(`relicDrawWeight`)로 3장 비중복 노출하며 **무료 단일 픽**이다(가격 라벨 숨김, 1장 선택 시 나머지는 사그라든다).
- 제단 자원팩은 8종 풀(두꺼운 의복/가열/큰 배낭/성냥갑/두꺼운 심지/조커 카드/랜턴/동전 한 닢)에서 weight 기반 3장을 제시한다.
- 공용 `무료 카드`(`free_001`)는 방문마다 6종(✦300 / 1$ / 체력 +5 / 콤보 게이지 +3 / 빛 게이지 +3 / 랜덤 손패 +2) 중 하나로 고정되고 사용 즉시 소모된다. 획득 블라스트는 보상 타입별 목적 HUD로 분기한다.
- 유물/팩/무료카드 희귀도는 `ShopPools` 공통 팔레트(common~legendary)를 쓰고, 상점/제단 유물은 등급별 발광을 유지한다.

### 보스 / 후반부

- **모든 보스에 기믹 격자가 켜져 있다**(`BOSS_GIMMICK_PROFILES`). 배치는 조우마다 다시 섞고 칸 종류·배율은 처음부터 표기된다.
  백작 3×3(약점 2 / 경화 2) · 고양이 3×3(약점 3 / 경화 1) · 기사단장 3×3(약점 2 / 경화 3) ·
  조각사 2×3(약점 2 / 경화 1) · 마녀 3×3(약점 2 / 경화 3) · 악마 3×3(약점 2 / 경화 2).
- 30F 리미트 페이지: HP 절반(50)에서 멈추고, 그 뒤 부위를 하나 더 깨야 2페이지가 열린다.
- 30F 2페이지 '탐욕의 값': 공격 주기마다 손에 쥔 `탐욕의 동전` 1장당 1피해.
- 30F 보스 턴 순서: 금화 살포(화면 흩뿌림 → 손패 유입) → [2P] 탐욕의 값 → 타격.
- 새싹 30F 양초 고양이도 격자(약점 3 / 경화 1)와 절반 리미트 페이지를 쓴다.
  공격 주기마다 손패 `CAT_GIFT_CARDS`장을 **주는 것이 모든 페이지의 기본**이고(온보딩 배려),
  2페이지는 **그 뒤에** `CAT_PAGE_TWO_STEAL_CARDS`장을 도로 뺏는다.
- 고양이의 주기는 **주기 → 뺏기 순서**를 지킨다. 방금 받은 장도 뺏기 후보가 돼야
  "줬다 뺏는다"가 한 주기 안에서 읽힌다 — 뺏기를 먼저 굴리면 그냥 두 사건이 된다.
  두 박자 사이에는 `pauseBeat()`를 둔다.
- 60F 기사단장 2페이지는 손패 3장 + 각 수치 +1. 90F 조각사 2페이지는 **소환하는 양초
  조각이 비대화**된다(체력 +5, 이후 소환도 포함 — 조각사 본체는 그대로).
  100F 마녀 3페이지 광폭화는 공 +3 / 체 +8.
- 칸 내구도·부위 파괴 보너스는 보스 최대 체력에서 파생된다(30F 160 → 20·16).
  약점은 두 배로 빨리 깨지고 경화는 두 배의 품이 들어, 결국 격자 전체를 돌며 때리게 된다.
- 부위 손상은 **그을림**으로 읽힌다(3단계, `BOSS_GIMMICK_CRACK_STAGES`). 깨진 칸은
  타 버린 판으로 꺼진다 — 어둠으로 덮지 말고 **채도를 빼서** 알린다(격자에 구멍이
  뚫린 것처럼 보이지 않게 일러스트는 그대로 보여야 한다). 손상 단계 표시는 반대로
  촛농(밝은 색)으로 그리지 말 것 — 보스 일러스트가 이미 흘러내린 밀랍이라 묻힌다.
- 단계는 **잉걸불이 칸으로 번지는 '달아오름'**으로 표현한다(번짐의 세기 = 남은 내구도).
  탄 영역을 안쪽으로 밀어 넣는 안은 화면에서 주홍 링처럼 보여 되돌렸다.
- 칸을 덮는 연출은 `mix-blend-mode: screen`으로 빛을 더한다. 불투명하게 채우면
  일러스트가 사라지고 색 사각형만 남는다.
- **깨진 칸에는 회색 `파괴` 글자 하나만 남긴다**(`.boss-gimmick-cell-broken`). 잔해
  텍스처를 깔지 않는다 — 배경 타일이 칸보다 커서 작은 화면에서는 한 귀퉁이만 찍혀
  잘린 얼룩이 되고, 그게 무엇인지도 읽히지 않는다.
- 무너지는 연출은 body 오버레이(`.boss-cell-shatter`)라 재렌더에 끊기지 않는다.
- 부위 파괴 beat는 **순차**다: 배율 피해 수치 → (한 박자) 파괴 연출 + 수치 자리의
  `칸 파괴` 표기 → 부위 파괴 추가 피해 수치. 겹쳐 내면 몇 대 맞아 깨졌는지가 뭉개진다.
- 손패·레시피가 칸/카드로 쏘는 것은 **곡사 블라스트**다(`ResourceTrailFx.animateStrikeLob`) —
  자원 트레일과 같은 정사각 조각이 높이 떠올랐다 목표 **위에서 수직으로** 내리꽂힌다.
- 곡사와 '흘러듦'을 가르는 것은 정점의 위치다. 정점은 항상 목표보다 `LOB_PLUNGE_PX` 위에
  오도록 제어점을 역산한다 — 거리 비례로만 띄우면 가까운 목표에서 내리꽂는 구간이 사라진다.
- 착탄 버스트는 `STRIKE_LOB_FLIGHT_MS`에 맞춰 터진다. 키프레임 길이를 바꾸면 착탄
  프레임(offset `IMPACT`)이 이 값에 오도록 함께 역산한다.
- 효과가 건드린 칸에는 **하나도 빠짐없이** 한 발씩 간다(`playHandTargetBlasts`) — 광역기·
  조합·함정 제거도 같은 경로다. 대상 수가 많으면 발사 간격만 좁히고 발수는 줄이지 않는다.
- 손패 획득만 파편이 아니라 **카드 토큰**으로 낸다(`animateHandCardTokens`, 목적지 `hand`).
  전용 도형이 아니라 같은 사각 조각의 **비율 변주**다 — 세로로 길어야 '카드'로 읽힌다.
- 토큰의 박자는 셋이다: 곡사로 튀어나와 착지 → **살짝 표류** → 손패로 빨려 들며 블라스트.
  멈춰 세우면 몇 장이 나왔는지 세는 구간이 정지 화면이 된다.
- 토큰은 **얻은 칸 자리**에서 나온다. 처치 전리품은 그 칸이 지워지는 `render()`보다 먼저
  rect를 잡아 넘긴다 — 놓치면 화면 중앙에서 나와 어디서 얻었는지가 사라진다.
- 착지는 줄 세우지 않는다. 각도·높이를 장마다 어긋내야 '진열'이 아니라 '떨어뜨린 것'이 된다.
- 아이콘이 대신 말하는 낱말은 글자에서 지운다(`(하트) 체력 +5` → `(하트) +5`).
  단, **문장 맨 앞 + 바로 뒤가 수치**일 때만 — 아니면 문장이 무너진다. `$`는 수치 뒤에
  붙는 단위라 앞머리 아이콘을 아예 붙이지 않는다.
- 칸 타겟팅 대비는 **칸을 밝히는 게 아니라 주변을 어둡게** 만들어 낸다
  (`.boss-face.is-cell-targeting`: 일러스트·이름·HP바·뱃지·깨진 칸을 누른다).
- 60F `불씨 기사단장`의 숨은 정체는 레온하르트이며 플레이어 응답 대사로만 암시한다. 3×3 `boss_002`, 2턴마다(`attackInterval`) 기사단장 손패 2장 사용(2P는 3장 + 수치 +1), 방패 획득 시 HP 게이지 좌상단 큰 방패 아이콘 안에 수치를 표시한다.
- 90F 조각사 소환 적은 사망 즉시 모델에서 제거한 뒤 보상 손패를 렌더해 재등장 잔상을 막는다.
- 90F 보상·강제 시련이 끝나 셔터가 오른 직후 `잿빛 굴레` 타이틀과 함께 90~100층 별빛 규칙이 발동된다: `turnkey_001` 비합체 특수 보물 `별빛` 획득만 턴 +1, 다른 행동은 턴을 올리지 않는다.
- 별빛으로 100턴에 도달하면 즉시 클리어하지 않고 100F 보스 `녹지 않는 마녀`로 진입한다. 3×3 `boss_004`, 2턴 주기, 페이지 경계(바닥)는 최대 체력의 2/3 · 1/3 · 0(`waxWitchPageFloors()`) — 한 페이지가 1/3씩이다. 경계 초과 피해 컷 + HP바 경계선 표시. 1P 손패 소각 · 2P 4장 동시 전개 보스 손패(중복 시 추가 카드) · 3P 광폭화 소환(공 +3 / 체 +8). 해금된 페이지 능력은 이후 페이지에도 유지되고, 페이지 전환 이벤트는 최초 하향 돌파 1회만 발생한다.
- 보스 현상금은 1~3$이며 1$ 단위 트레일/카운터로 증가한다. 전리품 유물 보상 트레일 목적지는 보유 유물 팬이다.

### 이벤트 미니게임

- 미니언 흥정(`event_002`) 불빛 수치는 층 인플레이션(`1 + 층 × 0.02`)을 성공/실패 양쪽에 적용한다(표기·실지급 동일, 리스크 프리미엄은 그 위에 곱함). 방패 조르기 +2 · 촛농 훔치기 콤보 +1. RL 시뮬도 같은 식을 쓴다.
- 백작 RPS(`event_003`) UI는 레일 전체에 분포한다: 상단 중앙 카드 슬롯 아래 종합 확률 칩(백작이 쥔 남은 장수 `n장` 표기, 소진 패는 흐림)·선언, 좌측 아이템 세로 열(장전 아이템은 보랏빛 발광 + `이번 판 적용`), 우측 판돈 세로 열, 하단 던지기 타일, 물러나기는 우상단 연승 아래. 판정과 불빛 증감은 `.rps-toast`(최대 2개 스택, 2.6초 유지)로 표시하고 획득 블라스트 출발점도 토스트 rect다. 낮은 화면(≤880px)은 슬롯 열·타일을 축소해 토스트 레인을 확보한다.
- 미니게임 에나 대사는 `EVENT_MINIGAME_LINES` 풀을 렌더러 `onMoment` 훅이 결과 확정 순간에 알리고 index가 발화한다. 잭팟/유물/연승은 확정, 잔잔한 승패는 0.5 확률.

### 손패 / 유물 수치

- 공격 카드 기본 피해: **불씨** 단일 = 공격력 × 1.0 + 1, 트리플 = 공격력 × 3.0 + 5. 강화팩 보너스는 별도 합산.
- 도감/손패 미리보기 기본값은 합산 피해 수치이고, Shift 누름 중에만 수식(검 아이콘 + 상수)으로 전환된다.
- Shift 자세히보기: 생성자에서 `is-shift-detail`을 초기 클리어한 뒤 keydown/keyup·`window.blur`·boardElement mousemove(`e.shiftKey` 확인)로 클래스 잔류를 방지한다. 도감 chip은 `display: none ↔ inline-block`으로 전환하고(phantom-width 방지), `.codex-chip-body`로 label+value를 단일 flex item으로 묶어 두 칸 분리 레이아웃을 막는다.
- 사치품 유물 최대 공격력 보너스 +3. 붉은 포션은 직접 공격·손패·체인 포함 모든 처치에 적용된다. 샹들리에는 기본 해금(`runLocked: false`). 자원팩 갑옷 장착 방패 +5. 시련 '역경' 적용 시 기존 함정 카드에도 즉시 피해 보너스가 반영된다.

### 에나 런타임 동작

- 지원 카드 판단은 `src/systems/HandCardAdvisor.ts` 범용 스코어러가 "기대 HP 절약 / 자원 가치"로 환산한다(청소 = 임박 병합 실효 피해 × 확률, 3칸 즉사 후보는 최상위 고정, 1칸 웹이 `WEB_FIELD_CLUTTER_THRESHOLD` 이상이면 병합각 없어도 광역 청소 / 처치 = 실효 공격력 × 반격 임박도 / 방어 = 흡수 기대 피해 / 성냥 = 불씨 소멸 스폰 압박 비용 / 회복 = 저체력 비선형). 고정 서열은 동점 ε 타이브레이크로만 쓴다.
- 레일 예고 큐, 트리플 완성각(비합체 2장 보유 시 지급 허용 + 가점), `EnaDisposition.supportRoleWeights` 보정을 받는다. 후보가 해금 레시피의 마지막 재료가 되면 효과를 같은 축으로 보수 매핑해 `RECIPE_SUPPORT_DISCOUNT`(0.55) 할인 가산한다 — 직접 효과 카드가 항상 레시피 경로보다 우선한다.
- 지속 감정: 런 내 `mood`(-1~1, 피해·거미줄·포자·폭탄 하락 / 처치·보물·꽃·레시피·보스 격파 상승, 턴당 0.02 자연 회복)와 런을 넘는 `bond`(0~1, 대사 열람·클러치·런 종료로만 단조 상승)를 `CompanionSystem`이 유지한다. `bond`는 `unmelting.ena.self-learning.v1`에 optional 필드로 영속된다.
- 최근 사건 링버퍼(8건, `resetForRun` 초기화)가 복수/연속 피격/클러치 뒤 보물 콜백 대사(`CALLBACK_LINES`, 확률 0.25)를 일반 풀보다 우선한다.
- 소소한 클러치에 `ember`(불씨 0 직전 +1, 고품격 뗄감 보유 시 미개입)와 `cleanse`(포자 2칸+ 전염 시 새 1칸 포자 1장 제거)가 있다. 큰 의지 클러치는 회복/방패에 더해 불씨 부족 시 성냥, 예지 위협·조합각 시 추천 손패 1장 보급까지 허용한다.
- 침묵 구간 대사(보스 등장/국면 전환/격파, 게임오버/클리어, 강제 시련 선택, 팩 구매, 별빛 수집)는 `companionWorldCanSpeak` 게이트를 의도적으로 우회해 각 지점에서 직접 발화한다.
- 비긴급 바크는 `BarkSequencer`(ui) 순차 큐(최소 노출 1.5~3초, 상한 3, 낮은 중요도부터 드롭)로 출력되고 urgent/클러치는 즉시 교체를 유지한다.
- 새 런 회상은 `EnaAutonomousLearner`의 구조화 기억(도달 층/사망 원인, 최근 12개, 직전 회상 키로 연속 반복 금지)으로 사실 기반 문장을 만든다.

## UI/UX 규칙

- ★ **화면이 전체 새로고침으로 번쩍이거나 끊겨서는 안 된다.** `render()`는 보드를
  `innerHTML`로 통째 교체하므로, 두면 진행 중이던 CSS 애니메이션이 **전부 0프레임으로
  되감겨** 화면이 한 번 튄다(촛불 발광·함정 맥동·보스 뱃지/열기 호흡·약점 칸 맥동).
- 되감김을 막는 단일 창구는 `captureBoardAnimationPhases()`/`restoreBoardAnimationPhases()`다.
  무한 루프는 공용 시계(`loopEpoch`) 위상으로, 유한 애니메이션은 **신원**으로 재생 위치를
  인계한다. 신원은 `data-fx-id` · `data-card-id` · `data-boss-gimmick-cell` · `data-hand-uid`다.
- 렌더를 넘어 이어져야 하는 연출을 새로 만들면 그 요소에 **`data-fx-id` 한 줄**을 단다.
  그것 말고 다른 배선은 없다.
- 렌더가 지우면 안 되는 임시 클래스는 라이브 DOM에만 붙이지 말고 **만료 시각을 상태로 두고
  render가 다시 심는다**(예: `hpDamagePulseUntil` → `is-hp-damaged`).
- CSS **트랜지션**은 요소가 교체되면 되살릴 수 없다. 렌더를 넘어 이어져야 하는 값
  (게이지 폭 등)은 트랜지션에 기대지 말고 현재 표시값에서 다시 그린다
  (`hudCounterVisibleStartValue` · `syncHudCounterLinkedVisuals`).
- 불가피하게 되감기거나 끊기는 연출이 남으면 **그 사실을 알고 남긴다** — 조용히 두지 않는다.
- 무대는 `--stage-ratio-max`/`--stage-ratio-min`(`index.html`) 범위를 벗어나면 늘리지 않고
  남는 쪽을 배경으로 남긴다. `#app`이 확정 높이를 갖게 되므로 그 아래는 `flex`로 채워야
  `height: 100%`가 해석된다(안 그러면 레일이 콘텐츠 높이로 접힌다). 모바일 가로는 예외.
- 화면에 보이는 버전 문자열의 단일 출처는 `package.json`이다(`VERSION.md`가 같은 값을 쓴다).
  우측 하단 배지에 손으로 적어 두지 않는다.
- z-index 레이어 대역을 지킨다: 보드 내부 ≤50(셔터 35 < 보스 타일 40) < 이펙트/오버레이 70~160(직업 선택·이벤트 씬 120~140, 보스 인트로 145, 유물 확대 160) < 정산·상위 오버레이 200~470 < 특수 최상단 9100~9999(클러치 배너 9998, 말풍선 9999) < 도감/경험 탭 10500. 새 오버레이는 기존 대역 사이에 끼워 넣고 극단값(99999 등)을 새로 만들지 않는다. `position: fixed` 오버레이는 전용 host 요소를 만들고 닫을 때 반드시 제거한다.
- **이모지 신규 추가 금지.** `src/ui/Icons.ts`의 플랫 inline-SVG path 아이콘을 쓰거나 같은 방식(`currentColor`, 단색 fill/stroke, 작은 크기 가독성)으로 추가한다. 경험 아이콘은 불빛/재화에 쓰는 네 꼭짓점 반짝 다이아로 통일한다.
- 아이콘은 24 박스 **한가운데에 잉크가 오게** 그린다. 어긋나면 CSS `vertical-align`을 자리마다 손보지 말고 `Icons.ts`의 `fitBox()`로 보정한다 — 정렬 단일 출처다.
- **이펙트 어휘는 사각 블라스트(`SquareBurst`) 하나다.** 새 이펙트가 필요하면 테마 팔레트·개수·확산·크기·박자로 말한다. 파편 도형(clip-path 조각)·물방울·원형 섬광 같은 **전용 도형을 새로 만들지 않는다** — 한 번 만들면 화면에 정체 모를 것이 늘고 톤이 갈라진다.
- 블라스트로 표현 못 하는 강조는 **발광**으로 낸다(밝기·글로우·확대). 딱딱한 테두리 선은 '선택된 UI'로 읽히므로 상태 표시에 쓰지 않는다.
- 촛불/밀랍/낡은 종이 테마 유지(색·테두리·그림자·스크롤바 일관성). 새 UI는 `GameBoardRenderer.ts`의 양식을 먼저 참고한다.
- 폰트는 `FontManager`와 OkDanDan을 쓰고 **최소 12px 원칙**을 깨지 않는다.
- 수치 피드백(불빛/화폐/체력/방패/불씨/게이지/피해/회복/처치)은 출처 블라스트 → 목적지 버스트 → 수치 롤링 → 사망 축소가 **같은 beat 안에서** 처리돼 분리돼 보이지 않게 한다. 꽃 관련 버스트는 공통 `flower-bloom`만 쓰지 말고 꽃 종류별 팔레트를 우선한다.
- 플레이어 피격은 체력 게이지가 직접 말한다: 확대 → 흔들림 → 붉은 잔광 → 복귀 +
  수치 롤링을 **한 beat 안에서**(`playPlayerHealthDamageFeedback`). 방패가 전부 막아
  체력이 그대로면 발동하지 않는다 — 안 깎였는데 게이지가 비명을 지르면 방패가 막아 준
  사실이 지워진다. 붉은빛은 전부 inset으로 낸다(`.player-card`가 `overflow: hidden`).
- 상점/제단 콘텐츠는 `shop-content-bundle` 레이어로 **동시 공개**한다. 구매 공통 임팩트(흔들림 + 사각 버스트)가 먼저 나가고 그다음 각 액션(무료/리롤/유물/카드팩)이 실행된다.
- 레일 대기 행(dist-1/2)은 투명도 + 밝기·채도 감쇠로 시선을 덜 끌게 하되 판독은 유지한다. 손패 타겟팅 중에는 행 감쇠를 풀고 칸 단위 대비로 전환한다: 유효 타깃은 어느 행이든 발광·확대, 불가 칸은 X 마크와 함께 더 어둡게, 안내 배너·체인 토스트는 반투명(`body.is-hand-targeting`)으로 눌러 칸을 가리지 않는다.
- 일반 레일 상단 안쪽 예고선은 `CardSpawner`의 실제 다음 리필 큐를 읽어 표시하며, 이미 노출된 큐는 소비 전까지 바꾸지 않는다. 이벤트 문·별빛은 흰색 예고선이고, 에나 학습 관측에도 같은 정보가 들어간다.
- 손패 카드는 열 높이를 넘으면 **줄어든다**(`flex-shrink: 1` + `flex-basis: 78px`). 스택이
  `justify-content: flex-end`라 넘치는 방향이 위쪽이어서, 안 줄이면 콤보 게이지를 덮는다.
  화면 크기로 가르지 않는다 — 뷰포트 조건으로 눌러 두면 조건 밖 해상도에서 다시 덮는다.
- 손패 hover 미리보기는 읽기용 **고정 표면**이다. 같은 손패 UID 위에 커서가 머무는 동안 레일 하강/HUD/효과 리렌더가 일어나도 기존 미리보기 DOM을 되심고 stable 클래스로 앞면 열린 상태를 고정해 flip·뒷면 애니메이션을 재실행하지 않는다.
- 한 번 본 이벤트/보스는 레일 우하단 `SKIP >>` 버튼으로 연출·대사만 건너뛴다(효과·정산은 그대로). 이벤트는 마무리 대사까지, 보스는 인트로 대사만 생략하고 등장 애니/타이틀은 유지한다.

## 코드 규칙

- TypeScript만 쓴다. import 주위에 try/catch를 두지 않는다.
- 새 코드에는 다음 작업자가 **의도**를 알 수 있는 짧은 주석을 남긴다. 자명한 문법 설명은 쓰지 않는다.
- 상태 변경은 가능한 한 시스템/엔티티에 두고, 렌더러는 표시/애니메이션 책임을 유지한다.
- 테스트용 시드, 더미 UI, 완료된 과거 실험 코드는 남기지 않는다. 발견 시 제거하거나 최종 보고에 명시한다.
- 오디오는 **긴 BGM = 스트리밍, 짧은 SFX = 버퍼 + 보이스 풀**로 나눈다. 60초 넘는 트랙을 `decodeAudioData`로 전량 디코딩하지 않는다(3분 스테레오 ≈ 60MB PCM). 트랙 하나가 막혀도 게임 진입이 막히지 않게 로드 실패를 격리한다.
- 반복 생성되는 리소스(이미지·오디오·DOM 복제)는 `src/tools/resource-lifecycle-check.ts`의 허용 표를 지난다. 표를 고치지 않고 우회하지 않는다.

## 문서 규칙

- `CLAUDE.md`는 **현재 지켜야 하는 규칙·불변식·경계**만 담는다. 완료된 작업 서술을 추가하지 않는다. 변경 이력은 `VERSION.md`에만 쌓는다.
- 한 항목은 한 규칙이다. 한 줄에 여러 사실을 이어 붙이지 않는다(목표 200자 이내).
- 코드를 읽으면 바로 알 수 있는 내용은 옮겨 적지 않는다. 대신 **어느 파일이 그 규칙의 단일 출처인지**를 적는다.
- 절을 추가하기 전에 기존 절에 자리가 있는지 먼저 본다. 중복 절을 만들지 않는다.
- 상세 설계는 전용 문서로 보내고 여기엔 불변식과 참조 경로만 남긴다(에나 → `Ena_Companion_AI_Design.md`).
- `CLAUDE.md`가 20KB를 넘으면 위 기준으로 다시 압축한다.

## 스폰 가중치 표기 메커니즘 (`{{spawn}}`)

- 유물/시련 효과 문자열의 `{{spawn}}` 토큰은 `GameBoardRenderer.relicEffectHtml()`이 실시간 ±% 수치로 치환한다.
- 기준값은 **항상 bright 티어** 고정(`CardSpawner.getEffectiveWeightsForDisplay()`) — 빛 게이지 변동은 표기에 영향을 주지 않는다.
- 영구 수정자(직업·유물·시련)만 반영한다. 유물 장착/시련 적용 시점에 % 표기가 갱신된다.
- 단일 출처: `src/systems/CardSpawner.ts` → `getEffectiveWeightsForDisplay()` / `src/ui/GameBoardRenderer.ts` → `relicEffectHtml()`

## 불씨 티어별 스폰 압박

- **용어**: 유저 텍스트의 `빛 게이지` = 코드 식별자 `ember`/`emberMax`(같은 것). `불씨 태그`(flame 손패 시너지)와는 별개 개념이다. 게이지를 뜻할 땐 `빛 게이지`로 표기한다.
- 티어는 절대 ember 수치 기준(bright ≥7, dim ≥4, flickering ≥1, extinguished =0)이며 `emberMax`와 무관하다. 선공(적 우선 공격)은 ember < 4에서 발동한다.
- 티어 하강은 **적·함정 가중치를 높이는** 방식으로 표현한다. 보물은 extinguished에서도 최소 5를 유지한다.
- `CardSpawner`의 `Math.max(1, ...)` 하한으로 보물 가중치가 0이 되는 상황을 추가 방지한다.
